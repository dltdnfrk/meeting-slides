$ bun test tests/public-active-shell.test.ts
bun test v1.3.14 (0d9b296a)

tests/public-active-shell.test.ts:
24 |   "public/review-panel-render 2.js",
25 | ] as const;
26 | 
27 | describe("Todo 18 active shell convergence", () => {
28 |   test("loads one generated-slide source and one operator source", () => {
29 |     expect(stylesheetHrefs).toEqual(["/style.css", "/caret-operator.css"]);
                                 ^
error: expect(received).toEqual(expected)

  [
    "/style.css",
+   "/workspace-shell.css",
+   "/operational-liquid.css",
+   "/caret-shell.css",
+   "/caret-foundation.css",
    "/caret-operator.css",
  ]

- Expected  - 0
+ Received  + 4

      at <anonymous> (/Users/hyunjun/Documents/MUNI/meeting-slides/tests/public-active-shell.test.ts:29:29)
(fail) Todo 18 active shell convergence > loads one generated-slide source and one operator source [0.19ms]
38 |     expect(new Set(stylesheetHrefs).size).toBe(stylesheetHrefs.length);
39 |     expect(new Set(scriptSources).size).toBe(scriptSources.length);
40 |   });
41 | 
42 |   test("deletes obsolete shell layers, duplicate artifacts, and browser overlay drivers", () => {
43 |     expect(obsoleteFiles.filter((path) => existsSync(join(root, path)))).toEqual([]);
                                                                              ^
error: expect(received).toEqual(expected)

- []
+ [
+   "public/workspace-shell.css",
+   "public/operational-liquid.css",
+   "public/caret-shell.css",
+   "public/caret-foundation.css",
+   "public/transcript-overlay.css",
+   "public/transcript-overlay.js",
+   "public/transcript-overlay 2.css",
+   "public/transcript-overlay 2.js",
+   "public/review-panel 2.js",
+   "public/review-panel-render 2.js",
+ ]

- Expected  - 1
+ Received  + 12

      at <anonymous> (/Users/hyunjun/Documents/MUNI/meeting-slides/tests/public-active-shell.test.ts:43:74)
(fail) Todo 18 active shell convergence > deletes obsolete shell layers, duplicate artifacts, and browser overlay drivers [0.14ms]
49 |     }
50 |   });
51 | 
52 |   test("the generated-slide source contains no appended TIRO operator shell", () => {
53 |     const slides = read("public/style.css");
54 |     expect(slides).not.toMatch(/TIRO|Operational Liquid|caret-shell/i);
                            ^
error: expect(received).not.toMatch(expected)

Expected substring or pattern: not /TIRO|Operational Liquid|caret-shell/i
Received: "/* ============================================================\n   style.css — Meeting Slides · aside 대시보드 (vanilla CSS)\n   단일 세로 열: 상태 스트립 → 현재 슬라이드(히어로) → 캡션 → 하단 도크\n   넓은 화면에서는 같은 열이 중앙 정렬되어 확장됨.\n   ============================================================ */\n\n:root {\n  --z950: #0c0c0e;\n  --z900: #161618;\n  --z850: #1c1c1f;\n  --z800: #242428;\n  --z700: #62626b;\n  --z500: #9a9aa3;\n  --z300: #d4d4d8;\n  --z100: #f4f4f5;\n\n  /* Tiro-like calm accent: warm coral, not neon emerald */\n  --live: #e85d4c;\n  --live-soft: rgba(232, 93, 76, 0.14);\n  --live-line: rgba(232, 93, 76, 0.4);\n  --warn: #d4a017;\n  --warn-soft: rgba(212, 160, 23, 0.13);\n  --err: #e85d4c;\n  --ok: #4f9d69;\n  --ok-soft: rgba(79, 157, 105, 0.14);\n\n  /* Semantic surfaces/lines used by settings cards */\n  --line-faint: rgba(255, 255, 255, 0.07);\n  --line-soft: rgba(255, 255, 255, 0.09);\n  --line-strong: rgba(255, 255, 255, 0.25);\n\n  /* Apple 글래스: 반투명 플로팅 패널 */\n  --glass-bg: rgba(22, 22, 26, 0.58);\n  --glass-bg-strong: rgba(22, 22, 26, 0.78);\n  --glass-blur: blur(28px) saturate(1.35);\n  --glass-border: rgba(255, 255, 255, 0.12);\n  --glass-highlight: rgba(255, 255, 255, 0.22);\n  --glass-shadow: 0 20px 56px rgba(0, 0, 0, 0.38);\n  --glass-inset: inset 0 1px 0 rgba(255, 255, 255, 0.1);\n  --glass-radius: 18px;\n  --island-gap: 10px;\n  --focus-ring: rgba(244, 244, 245, 0.9);\n  --motion-quick: 120ms;\n  /* DESIGN 9.6 motion budget: shell/tab/sheet transitions are 200ms. The\n     previous 220ms was over the approved token by 20ms with no contract\n     backing it. */\n  --motion-state: 200ms;\n  --ease-out: cubic-bezier(0.22, 1, 0.36, 1);\n\n  /* 4px spacing scale */\n  --sp-1: 4px;\n  --sp-2: 8px;\n  --sp-3: 12px;\n  --sp-4: 16px;\n\n  --r-sm: 7px;\n  --r-md: 10px;\n  --r-lg: 14px;\n\n  /* Mono label ramp (badges, section heads) */\n  --fs-badge: 9px;\n  --fs-micro: 10.5px;\n  --fs-sm: 11px;\n  --fs-body: 13px;\n\n  --ease-smooth: cubic-bezier(0.22, 1, 0.36, 1);\n  --hairline: rgba(255, 255, 255, 0.06);\n  --topbar-h: 56px;\n\n  --font-display: \"Inter\", \"Pretendard Variable\", Pretendard, -apple-system, BlinkMacSystemFont, \"Apple SD Gothic Neo\", sans-serif;\n  --font-body: \"Inter\", \"Pretendard Variable\", Pretendard, -apple-system, BlinkMacSystemFont, \"Apple SD Gothic Neo\", sans-serif;\n  --font-mono: \"JetBrains Mono\", \"SF Mono\", ui-monospace, SFMono-Regular, Menlo, monospace;\n  --font-ui: var(--font-body);\n}\n\n* { margin: 0; padding: 0; box-sizing: border-box; }\nhtml, body { height: 100%; }\n\nbody {\n  font-family: var(--font-body);\n  background: var(--z950);\n  color: var(--z100);\n  overflow: hidden;\n  -webkit-font-smoothing: antialiased;\n}\n\n/* 앰비언트: 유리 섬의 깊이만 드러내는 저채도 캔버스 */\n.ambient {\n  position: fixed;\n  inset: 0;\n  z-index: 0;\n  pointer-events: none;\n  background:\n    radial-gradient(circle at 16% 0%, rgba(232, 93, 76, 0.08), transparent 32%),\n    radial-gradient(circle at 82% 4%, rgba(255, 255, 255, 0.045), transparent 28%),\n    var(--z950);\n}\n.ambient__glow,\n.ambient__grid,\n.ambient__noise { display: none; }\n\n\n/* ── 앱 셸: 단일 세로 열, 중앙 정렬(넓은 화면) ── */\n.app {\n  position: relative;\n  z-index: 1;\n  height: 100dvh;\n  display: flex;\n  flex-direction: column;\n  width: 100%;\n  max-width: none;\n  margin: 0;\n  background: transparent;\n}\n\n/* ── 상단 바 ── */\n.topbar {\n  position: relative;\n  z-index: 5;\n  flex-shrink: 0;\n  height: var(--topbar-h);\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);\n  align-items: center;\n  column-gap: 16px;\n  padding: 0 20px;\n  border-bottom: 1px solid var(--hairline);\n  background: rgba(12, 12, 14, 0.94);\n  backdrop-filter: blur(16px) saturate(1.05);\n}\n.topbar__brand {\n  justify-self: start;\n  display: flex;\n  align-items: center;\n  gap: 10px;\n  min-width: 0;\n}\n.brand__mark {\n  width: 10px;\n  height: 10px;\n  border-radius: 50%;\n  background: var(--live);\n  color: transparent;\n  font-size: 0;\n  box-shadow: none;\n}\n.brand__name {\n  font-family: var(--font-display);\n  font-weight: 600;\n  font-size: 14px;\n  letter-spacing: -0.02em;\n  color: var(--z100);\n}\n.brand__name em {\n  font-style: normal;\n  font-weight: 500;\n  color: var(--z500);\n}\n.topbar__center {\n  justify-self: center;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n}\n.topbar__right {\n  justify-self: end;\n  display: flex;\n  align-items: center;\n  justify-content: flex-end;\n  gap: 10px;\n  min-width: 0;\n}\n\n/* recording state chip */\n.onair {\n  display: inline-flex;\n  align-items: center;\n  gap: 7px;\n  padding: 5px 10px;\n  border: 1px solid var(--hairline);\n  border-radius: 999px;\n  background: rgba(255, 255, 255, 0.03);\n}\n.onair__dot {\n  width: 7px;\n  height: 7px;\n  border-radius: 50%;\n  background: var(--z500);\n  box-shadow: none;\n}\n.onair__label {\n  font-family: var(--font-body);\n  font-size: 12px;\n  font-weight: 500;\n  letter-spacing: -0.01em;\n  color: var(--z500);\n}\n.onair--idle .onair__dot { background: var(--z500); box-shadow: none; }\n.onair--idle .onair__label { color: var(--z500); }\n.onair:not(.onair--idle) {\n  border-color: rgba(232, 93, 76, 0.28);\n  background: rgba(232, 93, 76, 0.1);\n}\n.onair:not(.onair--idle) .onair__dot { background: var(--live); }\n.onair:not(.onair--idle) .onair__label { color: #ffd0c9; }\n\n/* 상태 */\n.topbar__status { display: flex; align-items: center; gap: 7px; min-width: 0; }\n/* Colour-only tone change on a decorative dot: 150ms per the DESIGN 9.6\n   press/focus/hover-tone row, not the 300ms it used to take. */\n.status__indicator { width: 7px; height: 7px; border-radius: 50%; background: var(--z500); flex-shrink: 0; transition: background 150ms var(--ease-smooth), box-shadow 150ms var(--ease-smooth); }\n.status__indicator--ok { background: var(--live); box-shadow: 0 0 8px rgba(232,93,76,0.6); }\n.status__indicator--warn { background: var(--warn); box-shadow: 0 0 8px rgba(245,158,11,0.5); }\n.status__indicator--error { background: var(--err); box-shadow: 0 0 8px rgba(239,68,68,0.6); }\n.status__text { font-size: 11px; color: var(--z500); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 180px; }\n\n.icon-btn { margin-left: 6px; padding: 4px 7px; border: 1px solid rgba(255,255,255,0.1); border-radius: 7px; background: transparent; color: var(--z500); font-size: 13px; cursor: pointer; transition: color 0.2s var(--ease-smooth), border-color 0.2s var(--ease-smooth); }\n.icon-btn:hover { color: var(--z100); border-color: rgba(255,255,255,0.25); }\n.icon-btn--sm { padding: 1px 6px; margin-left: 6px; font-size: 11px; }\n\n/* ── 프로바이더 패널 ── */\n.provider-panel { position: fixed; top: 58px; right: 18px; z-index: 60; width: 320px; max-width: calc(100vw - 24px); max-height: calc(100vh - 76px); overflow-y: auto; overscroll-behavior: contain; padding: var(--sp-4); border: 1px solid var(--line-soft); border-radius: var(--r-lg); background: var(--glass-bg); -webkit-backdrop-filter: var(--glass-blur); backdrop-filter: var(--glass-blur); box-shadow: 0 18px 56px rgba(0,0,0,0.55); }\n.settings-section + .settings-section { margin-top: var(--sp-4); padding-top: var(--sp-4); border-top: 1px solid var(--line-faint); }\n.provider-panel__head { display: flex; align-items: center; justify-content: space-between; gap: 10px; font-family: var(--font-body); font-size: 12px; font-weight: 700; letter-spacing: -0.01em; color: var(--z300); margin-bottom: 10px; }\n.settings-section__actions { display: flex; align-items: center; gap: 6px; }\n.provider-panel__hint { margin-top: 10px; font-size: 10.5px; color: var(--z500); }\n.provider-row { display: flex; flex-direction: column; gap: 3px; width: 100%; padding: 6px; margin-bottom: 6px; border: 1px solid rgba(255,255,255,0.07); border-radius: 10px; background: var(--z850); transition: border-color 0.2s var(--ease-smooth), background 0.2s var(--ease-smooth); }\n.provider-row:hover { border-color: rgba(232,93,76,0.3); }\n.provider-row--current { border-color: rgba(232,93,76,0.55); background: rgba(232,93,76,0.07); }\n.provider-row--disabled { opacity: 0.55; }\n.provider-row__select { display: flex; flex-direction: column; gap: 3px; width: 100%; padding: 6px; border: none; background: transparent; text-align: left; cursor: pointer; }\n.provider-row__select:disabled { cursor: not-allowed; }\n.provider-row__name { font-size: 13px; font-weight: 700; color: var(--z100); }\n.provider-row__detail { font-size: 10.5px; color: var(--z500); }\n.provider-row__badge { margin-top: 2px; font-family: var(--font-mono); font-size: 9px; font-weight: 700; letter-spacing: 0.1em; color: var(--live); }\n.provider-row__badge--off { color: var(--z500); }\n.provider-row__actions { display: flex; gap: 6px; align-items: center; padding: 2px 6px 4px; }\n.provider-row__connect, .provider-row__save { padding: 4px 10px; border: 1px solid rgba(232,93,76,0.4); border-radius: 7px; background: rgba(232,93,76,0.08); color: var(--live); font-family: var(--font-mono); font-size: 9px; font-weight: 700; letter-spacing: 0.08em; cursor: pointer; }\n.provider-row__connect:hover, .provider-row__save:hover { background: rgba(232,93,76,0.16); }\n.provider-row__key { flex: 1; min-width: 0; padding: 4px 8px; border: 1px solid rgba(255,255,255,0.12); border-radius: 7px; background: var(--z900); color: var(--z100); font-size: 11px; }\n\n.provider-config { margin-top: 10px; padding-top: 10px; border-top: 1px solid rgba(255,255,255,0.07); display: flex; flex-direction: column; gap: 8px; }\n.provider-config__row { display: flex; align-items: center; justify-content: space-between; gap: 10px; font-family: var(--font-body); font-size: 11px; font-weight: 600; letter-spacing: -0.01em; color: var(--z300); }\n.provider-config__select { flex: 1; max-width: 190px; padding: 5px 8px; border: 1px solid rgba(255,255,255,0.12); border-radius: 7px; background: var(--z900); color: var(--z100); font-size: 11px; }\n\n/* ── 프로바이더 상태 배지: auth 상태를 진실하게 구분 ── */\n.provider-row__badge--ok { color: var(--ok); }\n.provider-row__badge--unknown { color: var(--warn); }\n.provider-row__badge--absent { color: var(--z500); }\n\n/* ── STT 모델 카드 ── */\n.stt-list { display: flex; flex-direction: column; gap: var(--sp-2); }\n.stt-row { display: flex; flex-direction: column; gap: var(--sp-1); padding: var(--sp-2); border: 1px solid var(--line-faint); border-radius: var(--r-md); background: var(--z850); transition: border-color 0.2s var(--ease-smooth), background 0.2s var(--ease-smooth); }\n.stt-row--selected { border-color: var(--live-line); background: var(--live-soft); }\n.stt-row--failed { border-color: rgba(232,93,76,0.35); }\n.stt-row__head { display: flex; align-items: baseline; justify-content: space-between; gap: var(--sp-2); }\n.stt-row__name { min-width: 0; overflow-wrap: anywhere; font-size: var(--fs-body); font-weight: 700; color: var(--z100); }\n.stt-row__badge { flex-shrink: 0; font-family: var(--font-mono); font-size: var(--fs-badge); font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; }\n.stt-row__badge--selected { color: var(--live); }\n.stt-row__badge--installed { color: var(--ok); }\n.stt-row__badge--downloading { color: var(--warn); }\n.stt-row__badge--failed { color: var(--err); }\n.stt-row__badge--absent { color: var(--z500); }\n.stt-row__meta { font-family: var(--font-mono); font-size: var(--fs-micro); color: var(--z500); }\n.stt-row__error { font-size: var(--fs-micro); color: var(--err); overflow-wrap: anywhere; }\n.stt-row__actions { display: flex; flex-wrap: wrap; gap: var(--sp-1); margin-top: var(--sp-1); }\n.stt-btn { padding: var(--sp-1) var(--sp-2); border: 1px solid var(--live-line); border-radius: var(--r-sm); background: var(--live-soft); color: var(--live); font-family: var(--font-mono); font-size: var(--fs-badge); font-weight: 700; letter-spacing: 0.08em; cursor: pointer; transition: background 0.2s var(--ease-smooth); }\n.stt-btn:hover { background: rgba(232,93,76,0.22); }\n.stt-btn--quiet { border-color: var(--line-strong); background: transparent; color: var(--z300); }\n.stt-btn--quiet:hover { background: rgba(255,255,255,0.06); }\n.stt-btn[disabled] { opacity: 0.5; cursor: default; }\n\n/* 진행률: 텍스트 퍼센트가 진실의 원본이고 바는 보조 표시다 */\n.stt-progress { height: 3px; border-radius: 999px; background: var(--z800); overflow: hidden; }\n.stt-progress__fill { height: 100%; border-radius: 999px; background: var(--warn); transition: width 0.25s var(--ease-smooth); }\n\n/* ── 글랜서블 상태 스트립 ── */\n.glance {\n  display: flex;\n  align-items: stretch;\n  gap: 8px;\n  padding: 8px 18px;\n  border-bottom: 1px solid rgba(255,255,255,0.06);\n  background: rgba(19,19,22,0.4);\n  flex-shrink: 0;\n}\n.glance__chip, .glance__metric {\n  display: flex; align-items: center; gap: 7px;\n  padding: 0 10px;\n  border: 1px solid rgba(255,255,255,0.06);\n  border-radius: 8px;\n  background: var(--z900);\n  height: 34px;\n}\n.glance__chip--capturing { min-width: 0; }\n.glance__chip--detect { min-width: 0; }\n.glance__chip--detect:not([hidden]) { border-color: rgba(96,165,250,0.35); background: rgba(96,165,250,0.08); }\n.glance__chip--detect:not([hidden]) .glance__label { color: #60a5fa; }\n.glance__chip--detect:not([hidden]) .glance__dot { background: #60a5fa; box-shadow: 0 0 8px rgba(96,165,250,0.6); animation: recpulse 1s ease-in-out infinite; }\n.glance__chip, .glance__metric { padding: 0 10px; }\n.glance__metric[hidden] { display: none; }\n.glance__metric--rec .glance__metric-value { color: var(--err); }\n.glance__label { font-family: var(--font-mono); font-size: 10px; font-weight: 700; letter-spacing: 0.14em; color: var(--z500); }\n.glance__dot { width: 7px; height: 7px; border-radius: 50%; background: var(--z500); flex-shrink: 0; transition: background 0.3s var(--ease-smooth), box-shadow 0.3s var(--ease-smooth); }\n.glance__metric { flex-direction: row; padding: 0 11px; }\n.glance__metric-label { font-family: var(--font-mono); font-size: 9px; font-weight: 700; letter-spacing: 0.16em; color: var(--z500); }\n.glance__metric-value { font-family: var(--font-mono); font-size: 13px; font-weight: 700; color: var(--z100); }\n.glance__metric--provider { flex: 1; min-width: 0; justify-content: flex-end; }\n.glance__metric--provider .glance__metric-value { white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }\n\n/* 녹음 중 상태 */\n.app--capturing .glance__chip--capturing { border-color: rgba(232,93,76,0.4); background: rgba(232,93,76,0.08); }\n.app--capturing .glance__chip--capturing .glance__label { color: var(--live); }\n.app--capturing .glance__chip--capturing .glance__dot { background: var(--live); box-shadow: 0 0 8px rgba(232,93,76,0.6); animation: recpulse 1.2s ease-in-out infinite; }\n\n/* 존재감 ── 진행 중에는 LIVES metric 도 살짝 강조 */\n.app--capturing .glance__metric .glance__metric-value { color: var(--z100); }\n\n/* ── 메인 스테이지 (히어로) ── */\n.stage {\n  flex: 1; min-height: 0;\n  display: flex; flex-direction: column;\n  padding: 18px;\n  gap: 14px;\n}\n.slide-frame { flex: 1; min-height: 0; display: flex; }\n.slide {\n  width: 100%;\n  display: flex; flex-direction: column;\n  padding: 30px 32px;\n  border: 1px solid rgba(255,255,255,0.08);\n  border-radius: 16px;\n  background: linear-gradient(160deg, rgba(255,255,255,0.045), rgba(255,255,255,0.012) 45%), var(--z900);\n  box-shadow: 0 20px 56px rgba(0,0,0,0.42);\n  overflow: hidden;\n  animation: slide-in 0.4s var(--ease-smooth);\n}\n@keyframes slide-in { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: translateY(0); } }\n.slide__inner { display: flex; flex-direction: column; width: 100%; }\n/* 머리말: 인덱스 배지 + kicker를 한 줄로 묶고, 제목은 본문과 같은 왼쪽 끝선을 공유한다. */\n.slide__header { display: flex; flex-direction: column; align-items: flex-start; gap: 10px; margin-bottom: 14px; }\n.slide__meta { display: flex; align-items: center; gap: 10px; min-width: 0; flex-wrap: wrap; }\n.slide__index { font-family: var(--font-mono); font-size: 13px; font-weight: 700; letter-spacing: 0.14em; color: var(--live); padding: 3px 9px; border: 1px solid rgba(232,93,76,0.3); border-radius: 6px; background: var(--live-soft); flex-shrink: 0; }\n.slide__kicker {\n  font-family: var(--font-mono);\n  font-size: 11px; font-weight: 700;\n  letter-spacing: 0.18em; text-transform: uppercase;\n  color: var(--live);\n  min-width: 0;\n  overflow-wrap: anywhere;\n}\n.slide__title { font-family: var(--font-display); font-weight: 700; font-size: clamp(22px, 5.5vw, 34px); line-height: 1.2; color: var(--z100); word-break: keep-all; overflow-wrap: break-word; }\n.slide__accent { height: 2px; width: 40px; background: linear-gradient(90deg, var(--live), transparent); border-radius: 2px; margin-bottom: 16px; }\n.slide__bullets { list-style: none; display: flex; flex-direction: column; gap: 9px; overflow-y: auto; }\n.slide__bullets li { font-size: clamp(14px, 3.4vw, 17px); line-height: 1.55; color: var(--z300); padding-left: 18px; position: relative; word-break: keep-all; overflow-wrap: break-word; }\n.slide__bullets li::before { content: \"\"; position: absolute; left: 2px; top: 9px; width: 5px; height: 5px; border-radius: 50%; background: var(--live); opacity: 0.7; }\n/* emphasis: 결정/리스크/다음 한 줄 — 불릿보다 한 단계 강한 카드 바닥 액센트 */\n.slide__emphasis {\n  display: flex; align-items: baseline; gap: 10px;\n  margin-top: 16px; padding: 12px 14px;\n  border: 1px solid rgba(232,93,76,0.28);\n  border-left-width: 3px;\n  border-radius: 10px;\n  background: var(--live-soft);\n  font-size: clamp(14px, 3.4vw, 17px); line-height: 1.5;\n  color: var(--z100);\n  overflow-wrap: anywhere;\n}\n.slide__emphasis-label {\n  font-family: var(--font-mono); font-size: 0.68em; font-weight: 700;\n  letter-spacing: 0.14em; color: var(--live); flex-shrink: 0;\n}\n.slide__notice { display: block; width: 100%; margin-bottom: 12px; padding: 8px 12px; border: 1px solid rgba(232,93,76,0.35); border-radius: 8px; background: rgba(232,93,76,0.08); color: var(--live); font-family: var(--font-mono); font-size: 10px; font-weight: 700; letter-spacing: 0.12em; text-align: left; cursor: pointer; transition: background 0.25s var(--ease-smooth); }\n.slide__notice:hover { background: rgba(232,93,76,0.16); }\n.slide__placeholder { margin: auto; text-align: center; display: flex; flex-direction: column; align-items: center; gap: 18px; }\n.placeholder__mic { position: relative; width: 90px; height: 90px; display: flex; align-items: center; justify-content: center; }\n.mic__core { width: 64px; height: 64px; border-radius: 50%; border: 1px solid rgba(232,93,76,0.25); background: var(--live-soft); display: flex; align-items: center; justify-content: center; }\n.mic__core svg { width: 30px; height: 30px; fill: none; stroke: var(--live); stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }\n.placeholder__title { font-family: var(--font-display); font-weight: 700; font-size: 20px; line-height: 1.3; color: var(--z100); }\n.placeholder__sub { max-width: 320px; font-size: 12px; line-height: 1.6; color: var(--z500); }\n\n/* ── 캡션 ── */\n.island {\n  align-self: center;\n  display: flex; align-items: center; gap: 10px;\n  min-height: 42px; padding: 9px 18px;\n  border: 1px solid rgba(255,255,255,0.1); border-radius: 999px;\n  background: var(--z900); box-shadow: 0 8px 24px rgba(0,0,0,0.38);\n  max-width: 100%;\n  /* Border tone swap on a state change: the DESIGN 9.6 150ms row. */\n  transition: border-color 150ms var(--ease-smooth);\n}\n.island--live { border-color: rgba(232,93,76,0.35); }\n.island__dot { width: 7px; height: 7px; border-radius: 50%; background: var(--live); flex-shrink: 0; animation: recpulse 1.6s ease-in-out infinite; }\n.island__text { font-size: 13px; line-height: 1.5; color: var(--z300); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }\n.speaker-chip { padding: 2px 8px; border-radius: 999px; background: color-mix(in srgb, var(--chip-color, #e85d4c) 18%, transparent); color: var(--chip-color, #e85d4c); border: 1px solid color-mix(in srgb, var(--chip-color, #e85d4c) 45%, transparent); font-family: var(--font-mono); font-size: 9px; font-weight: 700; letter-spacing: 0.1em; white-space: nowrap; }\n\n/* ── 하단 도크: 탭 + 가로 히스토리/세로 전사 ── */\n.dock {\n  flex-shrink: 0;\n  display: flex; flex-direction: column;\n  border-top: 1px solid rgba(255,255,255,0.08);\n  background: rgba(13,13,15,0.7);\n  backdrop-filter: blur(10px);\n  height: 200px;\n}\n.dock__tabs { display: flex; align-items: stretch; gap: 0; padding: 0; border-bottom: 1px solid rgba(255,255,255,0.06); flex-shrink: 0; }\n/* 전사가 우측 도킹 패널로 옮겨가 도크는 슬라이드 히스토리 단일 면이다 — 탭 대신 라벨 */\n.dock__label {\n  display: flex; align-items: center; gap: 7px;\n  padding: 10px 14px;\n  color: var(--z300);\n  font-family: var(--font-mono); font-size: 10px; font-weight: 700; letter-spacing: 0.12em;\n  border-bottom: 2px solid var(--live);\n}\n.dock__count { font-size: 9px; font-weight: 700; color: var(--z700); border: 1px solid rgba(255,255,255,0.1); border-radius: 999px; padding: 1px 6px; }\n.dock__spacer { flex: 1; }\n.dock__btn {\n  align-self: center;\n  display: flex; align-items: center; gap: 5px;\n  padding: 7px 10px; margin-right: 6px;\n  border: 1px solid rgba(255,255,255,0.08); border-radius: 8px;\n  background: transparent; color: var(--z500);\n  font-family: var(--font-mono); font-size: 10px; font-weight: 700; letter-spacing: 0.08em;\n  cursor: pointer;\n  transition: color 0.2s var(--ease-smooth), border-color 0.2s var(--ease-smooth), background 0.2s var(--ease-smooth);\n}\n.dock__btn span { color: var(--live); font-size: 12px; }\n.dock__btn:hover { color: var(--z100); border-color: rgba(255,255,255,0.18); background: rgba(255,255,255,0.03); }\n.dock__btn:disabled { cursor: progress; opacity: 0.55; }\n.dock__btn--compile { border-color: rgba(232,93,76,0.24); }\n.compile-status {\n  align-self: center; max-width: 150px; margin-right: 6px;\n  color: var(--z500); font-family: var(--font-mono); font-size: 9px;\n  white-space: nowrap; overflow: hidden; text-overflow: ellipsis;\n}\n.compile-status[data-state=\"success\"] { color: var(--live); }\n.compile-status[data-state=\"warning\"] { color: var(--warn); }\n.compile-status[data-state=\"error\"] { color: var(--err); }\n.dock__btn--danger { color: var(--err); }\n.dock__btn--danger:disabled { cursor: not-allowed; }\n.dock__btn--danger:hover { border-color: rgba(239,68,68,0.4); }\n\n.dock__panel { flex: 1; min-height: 0; display: flex; flex-direction: column; padding: 10px 14px; overflow: hidden; }\n.dock__panel[hidden] { display: none; }\n\n/* 히스토리: 가로 필름스트립 */\n.filmstrip { display: flex; gap: 10px; overflow-x: auto; overflow-y: hidden; padding-bottom: 4px; scroll-snap-type: x proximity; }\n.filmstrip__empty { display: flex; align-items: center; gap: 10px; color: var(--z500); font-size: 11px; padding: 24px 0; width: 100%; justify-content: center; }\n.filmstrip__empty-ring { width: 16px; height: 16px; border-radius: 50%; border: 2px dashed rgba(255,255,255,0.15); }\n.thumbnail { min-width: 170px; max-width: 170px; flex-shrink: 0; scroll-snap-align: start; padding: 10px 12px; border: 1px solid rgba(255,255,255,0.08); border-radius: 10px; background: var(--z850); cursor: pointer; transition: border-color 0.2s var(--ease-smooth), transform 0.2s var(--ease-smooth), background 0.2s var(--ease-smooth); }\n.thumbnail:hover { border-color: rgba(232,93,76,0.35); transform: translateY(-1px); }\n.thumbnail--viewing { border-color: rgba(232,93,76,0.6); background: var(--z800); }\n.thumbnail__index { font-family: var(--font-mono); font-size: 9px; font-weight: 700; letter-spacing: 0.12em; color: var(--z500); margin-bottom: 5px; }\n.thumbnail--viewing .thumbnail__index { color: var(--live); }\n.thumbnail__title { font-family: var(--font-display); font-weight: 700; font-size: 12px; line-height: 1.3; color: var(--z100); margin-bottom: 6px; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }\n.thumbnail__bullets { list-style: none; display: flex; flex-direction: column; gap: 3px; }\n.thumbnail__bullets li { font-size: 10px; line-height: 1.4; color: var(--z500); padding-left: 9px; position: relative; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }\n.thumbnail__bullets li::before { content: \"\"; position: absolute; left: 0; top: 6px; width: 3px; height: 3px; border-radius: 50%; background: var(--live); opacity: 0.6; }\n\n/* 전사 한 줄: 우측 도킹 패널(.transcript-stream)이 쓴다 */\n.feed-line { display: flex; flex-direction: column; gap: 3px; padding-left: 10px; border-left: 2px solid rgba(255,255,255,0.09); animation: line-in 0.3s var(--ease-smooth); }\n@keyframes line-in { from { opacity: 0; transform: translateX(-4px); } to { opacity: 1; transform: translateX(0); } }\n.feed-line__meta { display: flex; gap: 6px; align-items: center; }\n.feed-line__time { font-family: var(--font-mono); font-size: 9px; font-weight: 700; letter-spacing: 0.06em; color: var(--z500); }\n.feed-line__text { font-size: 12px; line-height: 1.55; color: var(--z100); opacity: 0.85; }\n\n/* ── 녹음 시작/중지 버튼 ── */\n.record-btn {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: 8px;\n  min-width: 128px;\n  height: 36px;\n  padding: 0 18px;\n  border: 1px solid var(--hairline);\n  border-radius: 999px;\n  background: rgba(255, 255, 255, 0.05);\n  color: var(--z100);\n  font-family: var(--font-body);\n  font-size: 13px;\n  font-weight: 600;\n  letter-spacing: -0.01em;\n  cursor: pointer;\n  transition: background 0.18s var(--ease-smooth), border-color 0.18s var(--ease-smooth), transform 0.15s var(--ease-smooth);\n  flex-shrink: 0;\n}\n.record-btn:hover {\n  background: rgba(255, 255, 255, 0.08);\n  border-color: rgba(255, 255, 255, 0.12);\n}\n.record-btn:active { transform: scale(0.985); }\n.record-btn:focus-visible {\n  outline: 2px solid rgba(232, 93, 76, 0.5);\n  outline-offset: 3px;\n}\n.record-btn__dot {\n  width: 8px;\n  height: 8px;\n  border-radius: 50%;\n  background: var(--live);\n  flex-shrink: 0;\n}\n.record-btn--on {\n  border-color: rgba(232, 93, 76, 0.42);\n  background: rgba(232, 93, 76, 0.14);\n  color: #ffd4ce;\n}\n.record-btn--on:hover { background: rgba(232, 93, 76, 0.2); }\n.record-btn--on .record-btn__dot {\n  background: var(--live);\n  border-radius: 50%;\n  animation: recpulse 1.35s ease-in-out infinite;\n}\n.record-btn__label { line-height: 1; }\n@keyframes recpulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.35; } }\n\n/* ── 스크롤바 ── */\n::-webkit-scrollbar { width: 6px; height: 6px; }\n::-webkit-scrollbar-track { background: transparent; }\n::-webkit-scrollbar-thumb { background: var(--z700); border-radius: 999px; }\n::-webkit-scrollbar-thumb:hover { background: var(--z500); }\n\n/* ── 포커스 가시성 (키보드 네비게이션) ── */\n:focus-visible {\n  outline: 2px solid var(--live);\n  outline-offset: 2px;\n}\nbutton:focus-visible { outline-offset: 1px; }\n.thumbnail:focus-visible { outline-offset: 0; }\n\n.session-item { position: relative; }\n.session-item .session-row { padding-right: 36px; }\n.session-delete {\n  position: absolute;\n  top: 50%;\n  right: var(--sp-2);\n  width: 28px;\n  height: 28px;\n  padding: 6px;\n  transform: translateY(-50%);\n  border: 1px solid transparent;\n  border-radius: var(--r-sm);\n  background: transparent;\n  color: var(--z500);\n  cursor: pointer;\n  opacity: 0.62;\n  transition: opacity 0.16s var(--ease-smooth), color 0.16s var(--ease-smooth), border-color 0.16s var(--ease-smooth), background 0.16s var(--ease-smooth);\n}\n.session-delete svg {\n  display: block;\n  width: 100%;\n  height: 100%;\n  fill: none;\n  stroke: currentColor;\n  stroke-width: 1.7;\n  stroke-linecap: round;\n  stroke-linejoin: round;\n}\n.session-item:hover .session-delete,\n.session-delete:focus-visible { opacity: 1; }\n.session-delete:hover {\n  border-color: var(--live-line);\n  background: var(--live-soft);\n  color: var(--err);\n}\n\n/* ── 반응형: 좁은 사이드 패널 최적화 ── */\n@media (max-width: 420px) {\n  .app { max-width: 100%; }\n  .topbar { padding: 10px 12px; gap: 8px; }\n  .topbar__status > :not(#btn-settings) { display: none; } /* 상태 텍스트만 글랜서블 스트립으로 이관 */\n  #btn-settings { margin-left: 0; }\n  .glance { padding: 7px 12px; gap: 6px; }\n  .glance__metric { padding: 0 8px; height: 32px; }\n  .stage { padding: 14px; }\n  .slide { padding: 24px 20px; }\n  .slide__title { font-size: clamp(20px, 6vw, 28px); }\n  .slide__header { gap: 8px; }\n  .slide__emphasis { flex-direction: column; gap: 4px; }\n  .dock { height: 180px; }\n  .dock__btn { padding: 6px 8px; font-size: 9px; gap: 4px; }\n  .attendee-btn { padding: 6px 9px; gap: 5px; }\n  .attendee-btn__label { display: none; }\n  .attendee-panel { top: 52px; width: auto; left: 12px; right: 12px; transform: none; max-width: none; }\n  .dock__btn span { font-size: 11px; }\n  .brand__name { display: none; }\n}\n\n@media (max-width: 340px) {\n  .onair__label { display: none; }\n  .glance__metric--provider .glance__metric-value { font-size: 11px; }\n}\n\n/* 좁은 폭: 설정 패널은 화면 폭에 맞춰 접히고, 셀렉트는 전체 폭을 쓴다 */\n@media (max-width: 420px) {\n  .provider-panel { top: 52px; right: 12px; left: 12px; width: auto; padding: var(--sp-3); }\n  .provider-config__row { flex-direction: column; align-items: stretch; gap: var(--sp-1); }\n  .provider-config__select { max-width: none; }\n  .stt-row__head { flex-direction: column; gap: 2px; }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .ambient__glow--a, .ambient__glow--b, .island__dot, .record-btn--on .record-btn__dot,\n  .app--capturing .glance__dot, .slide, .feed-line { animation: none !important; }\n  .stt-progress__fill { transition: none; }\n}\n\n/* ================================================================\n   v2 문서형 와이드 리디자인 (Granola 원칙: 문서가 주인공, 크롬은 무대 뒤)\n   ================================================================ */\n\n/* 글랜스 스트립은 상태 필로 통합 — DOM은 남기고 시각만 숨긴다 */\n.glance { display: none; }\n\n/* 앱 전체를 전폭으로 (기존 760px 캡 해제) */\n.app { max-width: none; margin: 0; }\n\n/* ── 스테이지를 전폭으로 ── */\n.stage {\n  width: 100%;\n  max-width: none;\n  padding: 10px clamp(20px, 3.5vw, 56px) 24px;\n}\n.slide-frame { width: 100%; flex-direction: column; }\n\n/* ── 문서 헤드 (Granola의 제목+메타 행) ── */\n.doc-head {\n  margin: 6px 6px 16px;\n  display: flex;\n  align-items: baseline;\n  gap: 14px;\n  flex-wrap: wrap;\n}\n.doc-title {\n  font-family: var(--font-display);\n  font-size: clamp(24px, 2.8vw, 36px);\n  font-weight: 700;\n  letter-spacing: -0.01em;\n  color: var(--z100);\n}\n.doc-meta {\n  font-family: var(--font-mono);\n  font-size: 11px;\n  letter-spacing: 0.04em;\n  color: var(--z500);\n}\n\n/* ── 히어로 문서 카드: 전폭·깊은 라운드·소프트 섀도 ── */\n.slide {\n  border-radius: 22px;\n  border: 1px solid rgba(255,255,255,0.055);\n  box-shadow: 0 24px 80px rgba(0,0,0,0.42), inset 0 1px 0 rgba(255,255,255,0.04);\n  padding: clamp(30px, 4.5vw, 64px);\n  min-height: 54vh;\n}\n.slide__title {\n  font-size: clamp(30px, 4.4vw, 52px);\n  line-height: 1.14;\n  letter-spacing: -0.015em;\n}\n.slide__kicker {\n  font-size: clamp(11px, 1vw, 13px);\n}\n.slide__bullets li {\n  font-size: clamp(16px, 1.8vw, 20px);\n  line-height: 1.65;\n}\n.slide__emphasis {\n  margin-top: clamp(18px, 2vw, 28px);\n  padding: clamp(12px, 1.4vw, 18px) clamp(14px, 1.6vw, 20px);\n  border-radius: 14px;\n  font-size: clamp(15px, 1.7vw, 19px);\n}\n.slide__placeholder { min-height: 38vh; }\n.placeholder__sub { max-width: 520px; }\n\n/* ── 상태 필 (아일랜드 확장): 캡처/AI/자막 + 메트릭 한 줄 ── */\n.island {\n  gap: 10px;\n  padding: 10px 20px;\n  border-radius: 999px;\n  background: var(--z900, #17171a);\n  border: 1px solid rgba(255,255,255,0.08);\n  box-shadow: 0 14px 44px rgba(0,0,0,0.42);\n  max-width: min(940px, 94vw);\n}\n.island__text {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n.island__sep {\n  width: 1px;\n  align-self: stretch;\n  background: rgba(255,255,255,0.09);\n  flex-shrink: 0;\n}\n.island__meta {\n  margin-left: auto;\n  font-family: var(--font-mono);\n  font-size: 9px; font-weight: 700;\n  letter-spacing: 0.1em;\n  color: var(--z500);\n  white-space: nowrap;\n  flex-shrink: 0;\n}\n.island--recording { border-color: rgba(239,68,68,0.4); }\n.island--recording .island__dot { background: #ef4444; animation: recpulse 1.2s ease-in-out infinite; }\n.island--detecting { border-color: rgba(232,93,76,0.4); }\n.island--detecting .island__dot { background: var(--live); animation: recpulse 1.2s ease-in-out infinite; }\n\n/* ── 크롬 슬림화: ON AIR와 상태 텍스트는 무대 뒤로 ── */\n.onair { display: none; }\n.status__text {\n  font-size: 10px;\n  opacity: 0.55;\n  max-width: 240px;\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n\n/* ── 도크 가독성: 탭과 버튼을 조금 더 여유 있게 ── */\n.dock__tabs { gap: 4px; }\n.dock__label { font-size: 12px; padding: 8px 14px; }\n.dock__btn { font-size: 11px; }\n\n/* === LIVE MULTI-KIND SLIDE DESIGN ===\n   컴파일 덱의 kind 다양성을 라이브 무대에도 투영.\n   .slide__title 클래스는 기존 테스트 호환을 위해 모든 kind에 유지. */\n.slide__inner--live {\n  position: relative;\n  width: 100%;\n  height: 100%;\n  box-sizing: border-box;\n  overflow: hidden;\n}\n.slide__kindchip { display: none; }\n.slide__inner--cover {\n  position: relative;\n  display: flex;\n  flex-direction: column;\n  justify-content: flex-end;\n  gap: 18px;\n  padding: clamp(28px, 5%, 56px);\n  background: var(--z900);\n}\n.slide__inner--cover > :not(.slide__cover-number) {\n  position: relative;\n  z-index: 1;\n  max-width: 78%;\n}\n.slide__cover-number {\n  position: absolute;\n  top: 8%;\n  right: 7%;\n  color: rgba(255, 255, 255, 0.07);\n  font-family: var(--font-display);\n  font-size: clamp(92px, 16vw, 180px);\n  font-weight: 700;\n  line-height: 1;\n  letter-spacing: -0.08em;\n}\n.slide__title--hero {\n  font-size: clamp(36px, 5.2vw, 64px);\n  line-height: 1.08;\n  letter-spacing: -0.03em;\n  max-width: 14ch;\n}\n.slide__cover-eyebrow { margin: 0; }\n.slide__cover-foot {\n  display: flex;\n  gap: 14px;\n  align-items: center;\n  color: var(--z500);\n  font-family: var(--font-mono);\n  font-size: 11px;\n  letter-spacing: 0.08em;\n}\n.slide__inner--section {\n  display: flex;\n  flex-direction: column;\n  justify-content: center;\n  gap: 16px;\n  padding: clamp(32px, 6%, 72px);\n  background: linear-gradient(135deg, #10141c, #0b0e14 60%);\n}\n.slide__title--section {\n  font-size: clamp(32px, 4.4vw, 52px);\n  max-width: 16ch;\n}\n.slide__inner--decision {\n  display: flex;\n  flex-direction: column;\n  gap: 16px;\n  padding: clamp(28px, 4.5%, 48px);\n  background:\n    linear-gradient(180deg, rgba(232, 93, 76, 0.08), transparent 40%),\n    #0d1118;\n}\n.slide__inner--decision .slide__emphasis {\n  margin-top: 4px;\n  padding: 16px 18px;\n  border: 1px solid rgba(232, 93, 76, 0.35);\n  border-radius: 16px;\n  background: rgba(232, 93, 76, 0.1);\n  font-size: clamp(16px, 1.6vw, 22px);\n}\n.slide__inner--actions {\n  display: flex;\n  flex-direction: column;\n  gap: 14px;\n  padding: clamp(28px, 4.5%, 48px);\n}\n.slide__actions {\n  list-style: none;\n  margin: 0;\n  padding: 0;\n  display: flex;\n  flex-direction: column;\n  gap: 10px;\n}\n.slide__action {\n  display: grid;\n  grid-template-columns: auto 1fr;\n  gap: 14px;\n  align-items: start;\n  padding: 12px 14px;\n  border-radius: 12px;\n  border: 1px solid rgba(255, 255, 255, 0.06);\n  background: rgba(255, 255, 255, 0.03);\n}\n.slide__action-no {\n  font-family: var(--font-mono);\n  font-size: 11px;\n  font-weight: 700;\n  color: #e85d4c;\n  letter-spacing: 0.08em;\n  padding-top: 3px;\n}\n.slide__action-text {\n  font-size: clamp(14px, 1.3vw, 18px);\n  line-height: 1.45;\n  color: var(--z100);\n}\n.slide__inner--summary {\n  display: flex;\n  flex-direction: column;\n  gap: 14px;\n  padding: clamp(28px, 4.5%, 48px);\n}\n.slide__summary-grid .slide__bullets {\n  columns: 2;\n  column-gap: 28px;\n}\n.slide__summary-grid .slide__bullets li {\n  break-inside: avoid;\n}\n.slide__inner--topic {\n  display: grid;\n  grid-template-columns: minmax(0, 1.25fr) minmax(140px, 0.75fr);\n  gap: 0;\n  min-height: 100%;\n  padding: 0;\n  overflow: hidden;\n}\n.slide__topic-main {\n  display: flex;\n  flex-direction: column;\n  gap: 14px;\n  padding: clamp(24px, 4%, 44px);\n  min-width: 0;\n}\n.slide__topic-visual {\n  position: relative;\n  border-left: 1px solid rgba(255, 255, 255, 0.06);\n  display: grid;\n  place-items: center;\n  background: var(--z900);\n  overflow: hidden;\n}\n.slide__topic-number {\n  color: rgba(255, 255, 255, 0.09);\n  font-family: var(--font-display);\n  font-size: clamp(88px, 12vw, 150px);\n  font-weight: 700;\n  line-height: 1;\n  letter-spacing: -0.08em;\n}\n.live-scene {\n  position: relative;\n  width: 100%;\n  height: 100%;\n  overflow: hidden;\n  font-family: var(--font-display);\n}\n.live-scene__text,\n.live-scene__shape {\n  position: absolute;\n}\n.live-scene__text {\n  display: flex;\n  align-items: center;\n  line-height: 1.24;\n  word-break: keep-all;\n  overflow: hidden;\n}\n.live-scene__line {\n  height: 0 !important;\n}\n@media (max-width: 900px) {\n  .slide__inner--topic {\n    grid-template-columns: 1fr;\n    overflow: hidden;\n  }\n  .slide__topic-visual {\n    display: none !important;\n    width: 0 !important;\n    min-width: 0 !important;\n    border: 0 !important;\n    padding: 0 !important;\n    margin: 0 !important;\n  }\n  .slide__summary-grid .slide__bullets { columns: 1; }\n  .slide__inner--live { overflow: hidden; }\n}\n\n/* ── 참석자 패널 (provider-panel 패턴 계승: fixed 오버레이 + hidden 토글) ── */\n.attendee-panel { position: fixed; top: 58px; left: 50%; transform: translateX(-50%); z-index: 60; width: 320px; max-width: calc(100vw - 24px); padding: 14px; border: 1px solid var(--line-soft); border-radius: var(--r-lg); background: var(--glass-bg); -webkit-backdrop-filter: var(--glass-blur); backdrop-filter: var(--glass-blur); box-shadow: 0 18px 56px rgba(0,0,0,0.55); }\n.attendee-panel[hidden] { display: none; }\n.attendee-panel__head { font-family: var(--font-mono); font-size: var(--fs-micro); font-weight: 700; letter-spacing: 0.16em; color: var(--z500); margin-bottom: 10px; }\n.attendee-panel__hint { margin-top: 10px; font-size: var(--fs-micro); line-height: 1.5; color: var(--z500); }\n.attendee-panel__error { margin-top: var(--sp-2); padding: 6px 10px; border: 1px solid var(--live-line); border-radius: 8px; background: var(--live-soft); color: var(--err); font-size: var(--fs-micro); line-height: 1.5; }\n.attendee-panel__error[hidden] { display: none; }\n.attendee-panel__save { width: 100%; margin-top: 10px; padding: var(--sp-2) 10px; border: 1px solid var(--live-line); border-radius: 8px; background: color-mix(in srgb, var(--live) 10%, transparent); color: var(--live); font-family: var(--font-mono); font-size: var(--fs-micro); font-weight: 700; letter-spacing: 0.1em; cursor: pointer; transition: background 0.2s var(--ease-smooth); }\n.attendee-panel__save:hover:not(:disabled) { background: color-mix(in srgb, var(--live) 18%, transparent); }\n.attendee-panel__save:disabled { opacity: 0.45; cursor: not-allowed; }\n\n.attendee-form { display: flex; flex-direction: column; gap: var(--sp-2); }\n.attendee-form__row { display: flex; align-items: center; justify-content: space-between; gap: 10px; }\n.attendee-form__label { font-family: var(--font-mono); font-size: var(--fs-micro); font-weight: 700; letter-spacing: 0.1em; color: var(--z500); flex-shrink: 0; }\n.attendee-form__input { flex: 1; min-width: 0; padding: 5px var(--sp-2); border: 1px solid rgba(255,255,255,0.12); border-radius: var(--r-sm); background: var(--z950); color: var(--z100); font-family: var(--font-body); font-size: var(--fs-body); }\n.attendee-form__input:disabled { opacity: 0.45; cursor: not-allowed; }\n.attendee-form__input::placeholder { color: var(--z700); }\n.attendee-form__add { padding: 6px 10px; border: 1px dashed var(--line-strong); border-radius: 8px; background: transparent; color: var(--z300); font-family: var(--font-mono); font-size: var(--fs-micro); font-weight: 700; letter-spacing: 0.08em; cursor: pointer; transition: border-color 0.2s var(--ease-smooth), color 0.2s var(--ease-smooth); }\n.attendee-form__add:hover:not(:disabled) { border-color: color-mix(in srgb, var(--live) 45%, transparent); color: var(--live); }\n.attendee-form__add:disabled { opacity: 0.45; cursor: not-allowed; }\n\n.attendee-list { display: flex; flex-direction: column; gap: 6px; margin-top: 10px; padding-top: 10px; border-top: 1px solid var(--line-faint); max-height: 216px; overflow-y: auto; }\n.attendee-list__empty { font-size: var(--fs-micro); color: var(--z500); padding: var(--sp-1) 2px; }\n.attendee-row { display: flex; align-items: center; gap: var(--sp-2); padding: 6px var(--sp-2); border: 1px solid var(--line-faint); border-radius: var(--r-md); background: var(--z850); transition: border-color 0.2s var(--ease-smooth), background 0.2s var(--ease-smooth); }\n.attendee-row:hover { border-color: color-mix(in srgb, var(--live) 30%, transparent); }\n.attendee-row--saved { border-color: var(--live-line); background: color-mix(in srgb, var(--live) 7%, transparent); }\n.attendee-row__body { display: flex; flex-direction: column; gap: 2px; min-width: 0; flex: 1; }\n.attendee-row__name { font-size: var(--fs-body); font-weight: 700; color: var(--z100); overflow-wrap: anywhere; }\n.attendee-row__crm { font-family: var(--font-mono); font-size: 9px; letter-spacing: 0.06em; color: var(--z500); overflow-wrap: anywhere; }\n.attendee-row__edit, .attendee-row__remove { flex-shrink: 0; padding: 3px 7px; border: 1px solid rgba(255,255,255,0.1); border-radius: var(--r-sm); background: transparent; color: var(--z500); font-family: var(--font-mono); font-size: 9px; font-weight: 700; letter-spacing: 0.08em; cursor: pointer; transition: color 0.2s var(--ease-smooth), border-color 0.2s var(--ease-smooth); }\n.attendee-row__edit:hover { color: var(--live); border-color: var(--live-line); }\n.attendee-row__remove:hover { color: var(--err); border-color: var(--live-line); }\n\n/* 참석자 토글 버튼: record-btn과 같은 필 언어, 눈길은 녹음이 가져가도록 중립 톤 */\n.topbar__center { display: flex; align-items: center; gap: var(--sp-2); min-width: 0; }\n.attendee-btn { display: flex; align-items: center; gap: 7px; padding: 6px 13px; border: 1px solid rgba(255,255,255,0.12); border-radius: 999px; background: var(--z900); color: var(--z300); font-family: var(--font-mono); font-size: var(--fs-micro); font-weight: 700; letter-spacing: 0.1em; cursor: pointer; transition: color 0.2s var(--ease-smooth), border-color 0.2s var(--ease-smooth), opacity 0.2s var(--ease-smooth); flex-shrink: 0; }\n.attendee-btn:hover:not(:disabled) { color: var(--z100); border-color: rgba(255,255,255,0.28); }\n.attendee-btn:disabled { opacity: 0.4; cursor: not-allowed; }\n.attendee-btn__icon { color: var(--z500); font-size: var(--fs-sm); }\n.attendee-btn[aria-expanded=\"true\"] { border-color: color-mix(in srgb, var(--live) 45%, transparent); color: var(--live); }\n.attendee-btn[aria-expanded=\"true\"] .attendee-btn__icon { color: var(--live); }\n.attendee-btn__count { padding: 1px 6px; border: 1px solid color-mix(in srgb, var(--live) 30%, transparent); border-radius: 999px; color: var(--live); font-size: 9px; }\n.attendee-btn__count[hidden] { display: none; }\n/* ================================================================\n   검토 패널 (review-panel.js) — provider/attendee 패널의 세 번째 형제.\n   후보 카드는 슬라이드 셸 위에 얹히는 작업면이라 폭·높이를 더 크게 잡되,\n   같은 표면 언어(--z900 배경, --line 테두리, --radius-lg)를 쓴다.\n   ================================================================ */\n.review-panel {\n  position: fixed;\n  top: 58px; right: 18px;\n  z-index: 70;\n  display: flex; flex-direction: column;\n  width: min(560px, calc(100vw - 36px));\n  max-height: calc(100vh - 88px);\n  padding: 14px;\n  border: 1px solid var(--line-soft);\n  border-radius: var(--r-lg);\n  background: var(--glass-bg);\n  -webkit-backdrop-filter: var(--glass-blur);\n  backdrop-filter: var(--glass-blur);\n  box-shadow: 0 18px 56px rgba(0,0,0,0.55);\n}\n.review-panel[hidden] { display: none; }\n\n.review-panel__head {\n  display: flex; align-items: center; gap: var(--sp-2);\n  margin-bottom: 10px;\n  flex-shrink: 0;\n}\n.review-panel__title {\n  font-family: var(--font-mono);\n  font-size: var(--fs-micro); font-weight: 700; letter-spacing: 0.16em;\n  color: var(--z500);\n  flex-shrink: 0;\n}\n.review-panel__version {\n  flex: 1; min-width: 0;\n  font-family: var(--font-mono);\n  font-size: 9px; letter-spacing: 0.06em;\n  color: var(--z700);\n  white-space: nowrap; overflow: hidden; text-overflow: ellipsis;\n}\n\n.review-panel__notice,\n.review-panel__error {\n  flex-shrink: 0;\n  margin-bottom: var(--sp-2);\n  padding: 6px 10px;\n  border-radius: var(--r-sm);\n  font-size: var(--fs-micro); line-height: 1.5;\n}\n.review-panel__notice { border: 1px solid color-mix(in srgb, var(--warn) 35%, transparent); background: var(--warn-soft); color: var(--warn); }\n.review-panel__error  { border: 1px solid var(--live-line);  background: var(--live-soft);  color: var(--err); }\n.review-panel__notice[hidden], .review-panel__error[hidden] { display: none; }\n\n/* 로딩: 추출은 회의 종료 후 일괄이라 몇 초~수십 초 걸린다 */\n.review-loading {\n  display: flex; align-items: center; gap: 10px;\n  padding: 28px 10px;\n  color: var(--z500); font-size: var(--fs-sm);\n}\n.review-loading[hidden] { display: none; }\n.review-loading__ring {\n  width: 16px; height: 16px; flex-shrink: 0;\n  border: 2px solid var(--line-strong);\n  border-top-color: var(--live);\n  border-radius: 50%;\n  animation: review-spin 0.9s linear infinite;\n}\n@keyframes review-spin { to { transform: rotate(360deg); } }\n\n.review-list {\n  flex: 1; min-height: 0;\n  display: flex; flex-direction: column; gap: var(--sp-2);\n  overflow-y: auto;\n  padding-right: var(--sp-1);\n}\n.review-list[hidden] { display: none; }\n.review-list__empty {\n  padding: 28px 6px;\n  font-size: var(--fs-sm); line-height: 1.7; color: var(--z500);\n  text-align: center;\n}\n\n/* ── 후보 카드 ── */\n.review-item {\n  display: flex; flex-direction: column; gap: 6px;\n  padding: 10px 14px;\n  border: 1px solid var(--line-faint);\n  border-left: 2px solid var(--z700);\n  border-radius: var(--r-md);\n  background: var(--z850);\n  transition: border-color 0.2s var(--ease-smooth), opacity 0.2s var(--ease-smooth);\n  animation: review-item-in 0.28s var(--ease-smooth) both;\n}\n@keyframes review-item-in { from { opacity: 0; transform: translateY(4px); } to { opacity: 1; transform: none; } }\n.review-item:hover { border-color: var(--line-strong); }\n.review-item--decision    { border-left-color: var(--live); }\n.review-item--action_item { border-left-color: #60a5fa; }\n.review-item--open_item   { border-left-color: var(--warn); }\n.review-item--dropped { opacity: 0.42; border-left-color: var(--err); }\n.review-item--dropped .review-item__description { text-decoration: line-through; }\n\n.review-item__head { display: flex; align-items: baseline; gap: var(--sp-2); }\n.review-item__kind {\n  flex-shrink: 0;\n  padding: 2px 6px;\n  border: 1px solid var(--line-strong);\n  border-radius: var(--r-sm);\n  font-family: var(--font-mono);\n  font-size: 9px; font-weight: 700; letter-spacing: 0.12em;\n  color: var(--z300);\n}\n.review-item--decision    .review-item__kind { border-color: var(--live-line); color: var(--live); }\n.review-item--action_item .review-item__kind { border-color: rgba(96,165,250,0.4); color: #60a5fa; }\n.review-item--open_item   .review-item__kind { border-color: color-mix(in srgb, var(--warn) 35%, transparent); color: var(--warn); }\n.review-item__coords {\n  flex: 1; min-width: 0;\n  font-family: var(--font-mono);\n  font-size: 9px; letter-spacing: 0.04em; color: var(--z500);\n  white-space: nowrap; overflow: hidden; text-overflow: ellipsis;\n  text-align: right;\n}\n\n.review-item__description {\n  font-size: var(--fs-body); font-weight: 700; line-height: 1.5;\n  color: var(--z100); overflow-wrap: anywhere;\n}\n.review-item__editor {\n  width: 100%; min-height: 62px; resize: vertical;\n  padding: 6px var(--sp-2);\n  border: 1px solid var(--live-line);\n  border-radius: var(--r-sm);\n  background: var(--z950); color: var(--z100);\n  font-family: var(--font-body); font-size: var(--fs-body); line-height: 1.5;\n}\n\n/* 근거: 인용은 카드의 사실 근거라 시각적으로 분리 */\n.review-item__quote {\n  padding-left: 10px;\n  border-left: 2px solid var(--line-soft);\n  font-size: var(--fs-body); line-height: 1.6;\n  color: var(--z300); font-style: italic;\n  overflow-wrap: anywhere;\n}\n.review-item__evidence { font-size: var(--fs-micro); }\n.review-item__evidence-summary {\n  cursor: pointer; color: var(--z500);\n  font-family: var(--font-mono); font-size: 9px; letter-spacing: 0.1em;\n}\n.review-item__evidence-summary:hover { color: var(--z300); }\n.review-item__segment {\n  margin-top: 6px;\n  padding: var(--sp-2);\n  border-radius: var(--r-sm);\n  background: var(--z950);\n  font-family: var(--font-body); font-size: var(--fs-sm); line-height: 1.6;\n  color: var(--z300);\n  white-space: pre-wrap; overflow-wrap: anywhere;\n}\n\n.review-item__deadline { font-size: var(--fs-micro); color: var(--z300); }\n.review-item__deadline-label {\n  margin-right: 6px;\n  font-family: var(--font-mono); font-size: 9px; font-weight: 700;\n  letter-spacing: 0.1em; color: var(--z500);\n}\n\n.review-item__foot {\n  display: flex; align-items: center; gap: 6px;\n  flex-wrap: wrap;\n  margin-top: var(--sp-1);\n  padding-top: var(--sp-2);\n  border-top: 1px solid var(--line-faint);\n}\n.review-item__spacer { flex: 1; }\n.review-item__field { display: flex; align-items: center; gap: 6px; min-width: 0; }\n.review-item__field-label {\n  font-family: var(--font-mono); font-size: 9px; font-weight: 700;\n  letter-spacing: 0.1em; color: var(--z500); flex-shrink: 0;\n}\n.review-item__select {\n  max-width: 148px;\n  padding: var(--sp-1) 6px;\n  border: 1px solid rgba(255,255,255,0.12);\n  border-radius: var(--r-sm);\n  background: var(--z950); color: var(--z100);\n  font-family: var(--font-body); font-size: var(--fs-sm);\n  cursor: pointer;\n}\n.review-item__select:disabled { opacity: 0.45; cursor: not-allowed; }\n.review-item__action {\n  flex-shrink: 0;\n  padding: 3px var(--sp-2);\n  border: 1px solid rgba(255,255,255,0.1);\n  border-radius: var(--r-sm);\n  background: transparent; color: var(--z500);\n  font-family: var(--font-mono); font-size: 9px; font-weight: 700; letter-spacing: 0.08em;\n  cursor: pointer;\n  transition: color 0.2s var(--ease-smooth), border-color 0.2s var(--ease-smooth);\n}\n.review-item__action:disabled { opacity: 0.4; cursor: not-allowed; }\n.review-item__edit:hover:not(:disabled), .review-item__save:hover { color: var(--live); border-color: var(--live-line); }\n.review-item__drop:hover { color: var(--err); border-color: var(--live-line); }\n.review-item--dropped .review-item__drop { color: var(--live); border-color: var(--live-line); }\n\n/* ── 확정 바 ── */\n.review-panel__foot {\n  display: flex; align-items: center; gap: var(--sp-2);\n  flex-shrink: 0;\n  margin-top: 10px;\n  padding-top: 10px;\n  border-top: 1px solid var(--line-faint);\n}\n.review-panel__hint {\n  flex: 1; min-width: 0;\n  font-size: var(--fs-micro); line-height: 1.4; color: var(--z500);\n}\n.review-panel__retry {\n  flex-shrink: 0;\n  padding: 6px 10px;\n  border: 1px dashed var(--line-strong);\n  border-radius: var(--r-sm);\n  background: transparent; color: var(--z300);\n  font-family: var(--font-mono); font-size: var(--fs-micro); font-weight: 700; letter-spacing: 0.08em;\n  cursor: pointer;\n  transition: border-color 0.2s var(--ease-smooth), color 0.2s var(--ease-smooth);\n}\n.review-panel__retry[hidden] { display: none; }\n.review-panel__retry:hover { border-color: var(--live-line); color: var(--live); }\n.review-panel__confirm {\n  flex-shrink: 0;\n  display: flex; align-items: center; gap: 6px;\n  padding: var(--sp-2) 14px;\n  border: 1px solid var(--live-line);\n  border-radius: var(--r-sm);\n  background: color-mix(in srgb, var(--live) 10%, transparent); color: var(--live);\n  font-family: var(--font-mono); font-size: var(--fs-micro); font-weight: 700; letter-spacing: 0.1em;\n  cursor: pointer;\n  transition: background 0.2s var(--ease-smooth);\n}\n.review-panel__confirm:hover:not(:disabled) { background: color-mix(in srgb, var(--live) 18%, transparent); }\n.review-panel__confirm:disabled { opacity: 0.45; cursor: not-allowed; }\n.review-panel__confirm-count {\n  padding: 1px 6px;\n  border: 1px solid color-mix(in srgb, var(--live) 30%, transparent);\n  border-radius: 999px;\n  font-size: 9px;\n}\n\n/* ── 검토 토글 버튼 (attendee-btn 언어 계승) ── */\n.review-btn {\n  display: flex; align-items: center; gap: 6px;\n  padding: 6px 13px;\n  border: 1px solid rgba(255,255,255,0.12);\n  border-radius: 999px;\n  background: var(--z900); color: var(--z300);\n  font-family: var(--font-mono); font-size: var(--fs-micro); font-weight: 700; letter-spacing: 0.1em;\n  cursor: pointer; flex-shrink: 0;\n  transition: color 0.2s var(--ease-smooth), border-color 0.2s var(--ease-smooth);\n}\n.review-btn[hidden] { display: none; }\n.review-btn:hover { color: var(--z100); border-color: rgba(255,255,255,0.28); }\n.review-btn__icon { color: var(--z500); font-size: var(--fs-sm); }\n.review-btn[aria-expanded=\"true\"] { border-color: var(--live-line); color: var(--live); }\n.review-btn[aria-expanded=\"true\"] .review-btn__icon { color: var(--live); }\n.review-btn__count {\n  padding: 1px 6px;\n  border: 1px solid color-mix(in srgb, var(--live) 30%, transparent);\n  border-radius: 999px;\n  color: var(--live); font-size: 9px;\n}\n.review-btn__count[hidden] { display: none; }\n\n/* ── 반응형: 좁은 폭에서는 전체 폭 시트로 ── */\n@media (max-width: 620px) {\n  .review-panel {\n    top: 52px; left: 12px; right: 12px;\n    width: auto;\n    max-height: calc(100vh - 76px);\n    padding: 10px;\n  }\n  .review-item__foot { gap: var(--sp-1); }\n  .review-item__select { max-width: 110px; }\n  .review-btn__label { display: none; }\n  .review-panel__hint { display: none; }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .review-item, .review-loading__ring { animation: none !important; }\n}\n\n/* ── Component responsiveness: attendee panel ── */\n@media (max-width: 420px) {\n  .attendee-btn { padding: 6px 9px; gap: 5px; }\n  .attendee-btn__label { display: none; }\n  .attendee-panel { top: 52px; width: auto; left: 12px; right: 12px; transform: none; max-width: none; }\n}\n\n/* ── Ask 회의 질문 패널 ── */\n.ask-panel {\n  position: fixed;\n  bottom: 64px; right: 18px;\n  z-index: 65;\n  display: flex; flex-direction: column;\n  width: min(420px, calc(100vw - 36px));\n  max-height: min(480px, calc(100vh - 140px));\n  border: 1px solid var(--line-soft);\n  border-radius: var(--r-lg);\n  background: var(--glass-bg);\n  -webkit-backdrop-filter: var(--glass-blur);\n  backdrop-filter: var(--glass-blur);\n  box-shadow: 0 18px 56px rgba(0,0,0,0.55);\n  overflow: hidden;\n}\n.ask-panel[hidden] { display: none; }\n.ask-panel__head {\n  display: flex; align-items: center; justify-content: space-between;\n  padding: 12px 14px 8px;\n  flex-shrink: 0;\n}\n.ask-panel__close {\n  border: none; background: none; color: var(--z500);\n  font-size: 18px; line-height: 1; cursor: pointer; padding: 4px 8px;\n}\n.ask-panel__close:hover { color: var(--ink); }\n.ask-panel__body {\n  flex: 1; min-height: 0;\n  overflow-y: auto;\n  padding: 0 14px;\n}\n.ask-panel__conversation { display: flex; flex-direction: column; gap: 8px; }\n.ask-message {\n  padding: 8px 12px;\n  border-radius: 10px;\n  font-size: var(--fs-sm);\n  line-height: 1.55;\n  white-space: pre-wrap;\n  word-break: break-word;\n}\n.ask-message--user {\n  align-self: flex-end;\n  background: color-mix(in srgb, var(--live) 16%, transparent);\n  color: var(--ink);\n  max-width: 85%;\n}\n.ask-message--assistant {\n  align-self: flex-start;\n  background: color-mix(in srgb, var(--z700) 60%, transparent);\n  color: var(--ink);\n  max-width: 95%;\n}\n.ask-panel__foot {\n  display: flex; gap: 8px;\n  padding: 10px 14px 14px;\n  flex-shrink: 0;\n}\n.ask-panel__input {\n  flex: 1; min-width: 0;\n  padding: 8px 12px;\n  border: 1px solid var(--line-soft);\n  border-radius: 8px;\n  background: var(--z800);\n  color: var(--ink);\n  font-size: var(--fs-sm);\n}\n.ask-panel__input:focus { outline: 1px solid var(--live); }\n.ask-panel__send {\n  padding: 8px 14px;\n  border: 1px solid var(--live-line);\n  border-radius: 8px;\n  background: color-mix(in srgb, var(--live) 10%, transparent);\n  color: var(--live);\n  font-family: var(--font-mono);\n  font-size: var(--fs-micro);\n  font-weight: 700;\n  letter-spacing: 0.1em;\n  cursor: pointer;\n}\n.ask-panel__send:disabled { opacity: 0.4; cursor: default; }\n.ask-panel .pane__empty { padding: 18px 0; text-align: center; }\n\n/* ── 회의 중 메모 (AI 정리 반영) ── */\n.notes-box {\n  flex-shrink: 0;\n  border-top: 1px solid var(--line-soft);\n  padding: 8px 12px;\n}\n.notes-box__summary {\n  cursor: pointer;\n  font-family: var(--font-mono);\n  font-size: var(--fs-micro);\n  color: var(--z500);\n  letter-spacing: 0.06em;\n  user-select: none;\n}\n.notes-box__summary:hover { color: var(--ink); }\n.notes-box__input {\n  width: 100%;\n  min-height: 64px;\n  margin-top: 8px;\n  padding: 8px 10px;\n  border: 1px solid var(--line-soft);\n  border-radius: 8px;\n  background: var(--z800);\n  color: var(--ink);\n  font-size: var(--fs-sm);\n  line-height: 1.5;\n  resize: vertical;\n}\n.notes-box__input:focus { outline: 1px solid var(--live); }\n\n/* ================================================================\n   TIRO-inspired focused operator surface\n   Runtime interaction grammar only; project zinc/coral/glass stay authoritative.\n   ================================================================ */\n\n.pane__eyebrow {\n  display: block;\n  margin-bottom: 3px;\n  color: var(--live);\n  font-family: var(--font-mono);\n  font-size: 8px;\n  font-weight: 700;\n  letter-spacing: 0.16em;\n}\n\n.transcript-context,\n.transcript-tools {\n  flex-shrink: 0;\n  display: flex;\n  align-items: center;\n  gap: var(--sp-2);\n  padding: var(--sp-2) var(--sp-3);\n  border-bottom: 1px solid var(--line-faint);\n}\n\n.transcript-context {\n  background:\n    linear-gradient(180deg, rgba(255, 255, 255, 0.025), transparent),\n    var(--z900);\n}\n\n.language-field {\n  min-width: 0;\n  display: grid;\n  gap: 2px;\n}\n\n.language-field__label {\n  color: var(--z500);\n  font-family: var(--font-body);\n  font-size: 9px;\n  font-weight: 600;\n  letter-spacing: 0;\n}\n\n.language-field__select {\n  width: 74px;\n  min-width: 0;\n  border: 0;\n  background: transparent;\n  color: var(--z300);\n  font: 600 11px/1.4 var(--font-body);\n  appearance: none;\n}\n\n.language-field__select:disabled { opacity: 1; }\n\n.language-direction {\n  width: 18px;\n  height: 18px;\n  color: var(--z700);\n}\n\n.language-direction svg {\n  width: 100%;\n  height: 100%;\n  fill: none;\n  stroke: currentColor;\n  stroke-linecap: round;\n  stroke-linejoin: round;\n  stroke-width: 1.5;\n}\n\n.capability-btn {\n  min-width: 0;\n  padding: 5px 8px;\n  border: 1px solid var(--line-soft);\n  border-radius: var(--r-sm);\n  background: rgba(255, 255, 255, 0.025);\n  color: var(--z500);\n  font: 600 9px/1.2 var(--font-body);\n  white-space: nowrap;\n}\n\n.capability-btn span {\n  display: block;\n  margin-top: 2px;\n  color: var(--z500);\n  font-family: var(--font-mono);\n  font-size: 7px;\n  letter-spacing: 0.06em;\n}\n\n.capability-btn:disabled { cursor: not-allowed; opacity: 0.9; }\n.transcript-tools {\n  flex-wrap: wrap;\n  overflow-x: auto;\n}\n.capability-reason {\n  flex: 1 0 100%;\n  margin: 0;\n  color: var(--z500);\n  font-family: var(--font-body);\n  font-size: 10px;\n  line-height: 1.4;\n  white-space: normal;\n}\n\n.provider-panel {\n  top: 68px;\n  right: 20px;\n  bottom: 20px;\n  width: min(420px, calc(100vw - 40px));\n  max-height: none;\n  padding: 20px;\n  border-color: var(--glass-border);\n  border-radius: 20px;\n  background:\n    linear-gradient(180deg, rgba(255, 255, 255, 0.045), transparent 14%),\n    var(--glass-bg);\n  box-shadow: var(--glass-shadow), var(--glass-inset);\n}\n\n.provider-panel__head {\n  margin-bottom: var(--sp-3);\n  font-size: 13px;\n}\n\n.provider-row {\n  padding: var(--sp-2);\n  border-color: var(--line-faint);\n  background: rgba(12, 12, 14, 0.5);\n}\n\n.provider-row--current {\n  border-color: var(--live-line);\n  background: var(--live-soft);\n}\n\n.dock {\n  position: relative;\n  z-index: 20;\n  height: 210px;\n  padding: 0 14px 12px;\n  overflow: visible;\n  border-top: 1px solid var(--hairline);\n  background: rgba(12, 12, 14, 0.96);\n  backdrop-filter: none;\n}\n\n.dock__commandbar {\n  position: relative;\n  z-index: 2;\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);\n  align-items: center;\n  gap: var(--sp-3);\n  min-width: 0;\n  min-height: 58px;\n  padding: 8px;\n  border: 1px solid var(--glass-border);\n  border-radius: 24px;\n  background:\n    linear-gradient(180deg, rgba(255, 255, 255, 0.055), transparent 46%),\n    var(--glass-bg);\n  backdrop-filter: var(--glass-blur);\n  box-shadow: var(--glass-shadow), var(--glass-inset);\n}\n\n.output-switcher {\n  justify-self: start;\n  display: inline-flex;\n  align-items: center;\n  min-width: 0;\n  padding: 3px;\n  border: 1px solid var(--line-faint);\n  border-radius: 999px;\n  background: rgba(12, 12, 14, 0.52);\n}\n\n.output-switcher__item {\n  min-width: max-content;\n  padding: 7px 10px;\n  border: 0;\n  border-radius: 999px;\n  background: transparent;\n  color: var(--z500);\n  font: 600 10px/1 var(--font-body);\n  text-decoration: none;\n  cursor: pointer;\n  transition:\n    color var(--motion-quick) var(--ease-out),\n    background var(--motion-state) var(--ease-out),\n    transform var(--motion-quick) var(--ease-out);\n}\n\n.output-switcher__item:hover:not(:disabled) { color: var(--z100); }\n.output-switcher__item:active:not(:disabled) { transform: scale(0.97); }\n.output-switcher__item[aria-current=\"page\"] {\n  background: var(--z800);\n  color: var(--z100);\n  box-shadow: var(--glass-inset);\n}\n.output-switcher__item:disabled { color: var(--z500); cursor: not-allowed; }\n.output-switcher__reason {\n  margin-inline-start: 4px;\n  color: var(--z500);\n  font-family: var(--font-body);\n  font-size: 9px;\n  font-weight: 600;\n  letter-spacing: 0;\n}\n\n.capture-pill {\n  justify-self: center;\n  display: inline-flex;\n  align-items: center;\n  gap: 8px;\n  min-width: 0;\n  padding: 4px;\n  border: 1px solid rgba(232, 93, 76, 0.26);\n  border-radius: 999px;\n  background: rgba(12, 12, 14, 0.72);\n  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3), inset 0 1px 0 rgba(255, 255, 255, 0.07);\n}\n\n.capture-pill__mode {\n  padding-inline-start: 10px;\n  color: var(--z500);\n  font: 700 9px/1 var(--font-mono);\n  letter-spacing: 0.12em;\n}\n\n.capture-pill .record-btn {\n  min-width: 118px;\n  height: 40px;\n  padding: 0 14px;\n  border-color: transparent;\n  background: var(--live);\n  color: var(--z950);\n}\n\n.capture-pill .record-btn:hover:not(:disabled) {\n  border-color: rgba(255, 255, 255, 0.22);\n  background: #f06b5a;\n}\n\n.capture-pill .record-btn__dot {\n  width: 10px;\n  height: 10px;\n  border: 2px solid var(--z950);\n  background: transparent;\n}\n\n.capture-pill .record-btn--on {\n  background: var(--z100);\n  color: var(--z950);\n}\n\n.capture-pill .record-btn--on .record-btn__dot {\n  border: 0;\n  border-radius: 2px;\n  background: var(--live);\n  animation: none;\n}\n\n.record-waveform {\n  display: none;\n  align-items: center;\n  gap: 3px;\n  height: 24px;\n  padding-inline: 2px;\n}\n\n.record-waveform__bar {\n  width: 3px;\n  height: 18px;\n  border-radius: 999px;\n  background: var(--live);\n  transform: scaleY(0.36);\n  animation: record-level 760ms ease-in-out infinite alternate;\n}\n\n.record-waveform__bar:nth-child(2) { animation-delay: -420ms; }\n.record-waveform__bar:nth-child(3) { animation-delay: -180ms; }\n.record-waveform__bar:nth-child(4) { animation-delay: -560ms; }\n.record-waveform__bar:nth-child(5) { animation-delay: -280ms; }\n\n@keyframes record-level {\n  from { transform: scaleY(0.28); opacity: 0.56; }\n  to { transform: scaleY(1); opacity: 1; }\n}\n\n.app--capturing .record-waveform { display: inline-flex; }\n\n.capture-live {\n  display: inline-flex;\n  align-items: center;\n  gap: 6px;\n  padding-inline-end: 10px;\n}\n.capture-live[hidden] { display: none; }\n.capture-live__label {\n  color: var(--live);\n  font: 700 8px/1 var(--font-mono);\n  letter-spacing: 0.12em;\n}\n.capture-live__timer {\n  color: var(--z100);\n  font: 700 11px/1 var(--font-mono);\n  font-variant-numeric: tabular-nums;\n}\n\n.dock__context {\n  justify-self: end;\n  display: grid;\n  grid-template-columns: auto auto 1px auto auto;\n  align-items: center;\n  gap: 6px;\n  min-width: 0;\n  color: var(--z500);\n  font: 600 9px/1.2 var(--font-body);\n}\n.dock__context strong { color: var(--z300); font-size: 10px; }\n.dock__context-separator { width: 1px; height: 18px; background: var(--line-soft); }\n\n.dock__tabs {\n  min-width: 0;\n  overflow-x: auto;\n  overflow-y: hidden;\n  flex-wrap: nowrap;\n  border-bottom-color: var(--line-faint);\n  scrollbar-width: thin;\n}\n\n.dock__tabs > * { flex-shrink: 0; }\n.dock__spacer { min-width: 14px; }\n.dock__panel { padding-top: 8px; }\n.filmstrip__empty { padding-block: 18px; }\n\n.stage-pane:focus,\n.transcript-pane:focus { outline: none; }\n.stage-pane:focus-visible,\n.transcript-pane:focus-visible {\n  box-shadow: inset 0 0 0 2px var(--focus-ring);\n}\n\n.slide-frame { overflow: hidden; }\n.slide-frame > .slide {\n  flex: 1 1 auto;\n  min-height: 0;\n}\n\n@media (max-width: 900px) {\n  .dock { height: 224px; }\n  .dock__commandbar { grid-template-columns: minmax(0, 1fr) auto; }\n  .dock__context { display: none; }\n  .transcript-context { flex-wrap: wrap; }\n  .capability-reason { font-size: 9px; }\n  .slide-frame > .slide { padding: 22px; }\n}\n\n@media (max-width: 600px) {\n  .topbar { grid-template-columns: auto 1fr auto; }\n  .topbar__center { justify-self: center; }\n  .onair { display: none; }\n  .dock {\n    height: 230px;\n    padding-inline: 10px;\n  }\n  .dock__commandbar {\n    display: flex;\n    flex-wrap: wrap;\n    justify-content: center;\n    gap: 4px;\n    padding: 4px;\n    border-radius: 20px;\n  }\n  .capture-pill { order: 1; }\n  .capture-pill .record-btn { height: 36px; }\n  .output-switcher {\n    order: 2;\n    justify-content: center;\n    width: 100%;\n    overflow-x: auto;\n  }\n  .output-switcher__item { padding: 6px 8px; }\n  .dock__label { padding-inline: 10px; }\n  .dock__panel {\n    padding: 4px 10px 0;\n  }\n  .filmstrip__empty { padding-block: 10px; }\n  .provider-panel {\n    inset: 60px 10px 10px;\n    width: auto;\n    padding: 16px;\n  }\n  .transcript-context,\n  .transcript-tools { padding-inline: 10px; }\n  .language-field__select { width: 66px; }\n  .slide-frame > .slide { padding: 14px; }\n  .slide__placeholder { gap: 10px; }\n  .placeholder__mic {\n    width: 54px;\n    height: 54px;\n  }\n  .placeholder__mic .mic__core {\n    width: 42px;\n    height: 42px;\n  }\n  .placeholder__mic svg {\n    width: 22px;\n    height: 22px;\n  }\n  .placeholder__title {\n    font-size: 16px;\n    line-height: 1.35;\n  }\n  .placeholder__sub { display: none; }\n  .notes-box { display: none; }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .output-switcher__item,\n  .record-btn,\n  .record-waveform__bar { transition: none !important; animation: none !important; }\n  .record-waveform__bar { transform: scaleY(0.58); opacity: 0.8; }\n}\n\n/* ── Floating glass island composition ───────────────────────── */\n.topbar {\n  height: 50px;\n  margin: 10px 14px 0;\n  padding-inline: 16px;\n  border: 1px solid var(--glass-border);\n  border-radius: var(--glass-radius);\n  background:\n    linear-gradient(180deg, rgba(255, 255, 255, 0.055), transparent 54%),\n    var(--glass-bg);\n  backdrop-filter: var(--glass-blur);\n  -webkit-backdrop-filter: var(--glass-blur);\n  box-shadow: var(--glass-shadow), var(--glass-inset);\n}\n\n.dock {\n  border-top: 0;\n  background: transparent;\n}\n\n.dock__commandbar,\n.dock__tabs,\n.dock__panel {\n  border: 1px solid var(--glass-border);\n  background:\n    linear-gradient(180deg, rgba(255, 255, 255, 0.045), transparent 52%),\n    var(--glass-bg);\n  backdrop-filter: var(--glass-blur);\n  -webkit-backdrop-filter: var(--glass-blur);\n  box-shadow: var(--glass-inset);\n}\n\n.dock__tabs {\n  margin-top: 6px;\n  padding-inline: 8px;\n  border-radius: 14px;\n}\n\n.dock__panel {\n  margin-top: 6px;\n  padding-inline: 12px;\n  border-radius: 16px;\n}\n\n.capture-pill {\n  background: rgba(12, 12, 14, 0.5);\n  backdrop-filter: blur(16px) saturate(1.2);\n  -webkit-backdrop-filter: blur(16px) saturate(1.2);\n}\n\n.provider-panel {\n  background:\n    linear-gradient(180deg, rgba(255, 255, 255, 0.07), transparent 18%),\n    var(--glass-bg-strong);\n  backdrop-filter: var(--glass-blur);\n  -webkit-backdrop-filter: var(--glass-blur);\n  z-index: 40;\n  box-sizing: border-box;\n  max-height: calc(100dvh - 88px);\n  overflow-y: auto;\n  overscroll-behavior: contain;\n}\n\nbody:has(#provider-panel:not([hidden])) .app::after {\n  content: \"\";\n  position: fixed;\n  inset: 0;\n  z-index: 30;\n  pointer-events: none;\n  background: rgba(5, 5, 7, 0.38);\n  backdrop-filter: blur(3px) saturate(0.86);\n  -webkit-backdrop-filter: blur(3px) saturate(0.86);\n}\n\n.status__text {\n  color: var(--z500);\n}\n\n.dock__btn:disabled {\n  border-color: var(--line-soft);\n  color: var(--z500);\n  opacity: 1;\n  cursor: not-allowed;\n}\n\n.transcript-context .capability-btn:disabled {\n  opacity: 1;\n}\n\n.transcript-context .capability-btn:disabled span {\n  color: var(--z500);\n}\n\n@media (max-width: 900px) {\n  .dock__tabs {\n    column-gap: 6px;\n    contain: paint;\n    clip-path: inset(0 round 14px);\n    scrollbar-color: var(--z500) transparent;\n    box-shadow:\n      inset -20px 0 18px -22px rgba(255, 255, 255, 0.8),\n      var(--glass-inset);\n  }\n  .dock__btn { padding-inline: 9px; }\n  #btn-ask { margin-inline-start: 44px; }\n  .dock__tabs::after {\n    content: \"→\";\n    position: sticky;\n    right: -1px;\n    z-index: 3;\n    flex: 0 0 44px;\n    align-self: stretch;\n    display: grid;\n    place-items: center;\n    color: var(--z300);\n    font: 700 11px/1 var(--font-mono);\n    border-left: 1px solid var(--line-soft);\n    background: var(--z900);\n    box-shadow: -12px 0 18px rgba(12, 12, 14, 0.86);\n    pointer-events: none;\n  }\n}\n\n@media (max-width: 600px) {\n  .topbar {\n    height: 48px;\n    margin: 8px 10px 0;\n    padding-inline: 10px;\n    border-radius: 15px;\n  }\n  .dock { height: 220px; }\n  .brand__name,\n  .attendee-btn__label { display: inline; }\n  .brand__name {\n    font-size: 11px;\n    letter-spacing: -0.01em;\n  }\n  .attendee-btn {\n    min-width: 0;\n    padding-inline: 8px;\n  }\n  .attendee-btn__label { font-size: 10px; }\n  .capture-pill__mode { display: none; }\n  .dock__tabs {\n    margin-top: 4px;\n    padding-inline: 6px;\n  }\n  .dock__panel {\n    margin-top: 4px;\n    padding-inline: 10px;\n  }\n  .provider-panel {\n    max-height: calc(100dvh - 70px);\n  }\n}\n"

      at <anonymous> (/Users/hyunjun/Documents/MUNI/meeting-slides/tests/public-active-shell.test.ts:54:24)
(fail) Todo 18 active shell convergence > the generated-slide source contains no appended TIRO operator shell [0.43ms]
56 |     expect(slides).toContain(".review-panel");
57 |   });
58 | 
59 |   test("the operator source owns tokens, geometry, narrow failures, and global reduced motion", () => {
60 |     const operator = read("public/caret-operator.css");
61 |     expect(operator).toContain("--cf-target-min: 44px");
                          ^
error: expect(received).toContain(expected)

Expected to contain: "--cf-target-min: 44px"
Received: "/* ============================================================================\n   caret-operator.css — the document-centric library shell (Todo 11).\n\n   Ownership: the LIBRARY shell geometry only — the meetings rail, the single\n   document surface, and the Overview / Notes / Transcript tabpanel swap.\n   Todo 12 owns focused live geometry; every rule here is scoped to\n   `.app[data-shell=\"library\"]` so it cannot reach into the live workspace.\n\n   Every colour, space, radius, type step, duration and easing below resolves a\n   `--cf-*` token from caret-foundation.css. This file declares NO literal colour\n   and no ad-hoc spacing value: if a value is missing, the token layer is\n   extended, never bypassed.\n\n   It is loaded last so it wins the superseded three-pane operator geometry from\n   style.css / workspace-shell.css / operational-liquid.css / caret-shell.css,\n   which Todo 18 removes outright.\n   ========================================================================== */\n\n/* ── library-shell tokens ─────────────────────────────────────────────────── */\n\n.app[data-shell=\"library\"] {\n  /* Rail: narrow by contract. It is navigation, never a second document.\n     These are DEFAULTS only. `workspace-split.js` owns the live value and writes\n     it to `--rail-w` on #workspace, so the rail splitter keeps working and\n     `workspace.layout.v1` keeps persisting exactly as before. */\n  --lib-rail-w: 268px;\n  --lib-rail-w-compact: 216px;\n  /* The measure the document body is allowed to reach before it stops growing. */\n  --lib-measure: 760px;\n  /* One shared gutter so rail, chrome and panels sit on the same rhythm. */\n  --lib-gutter: var(--cf-space-6);\n  --lib-gutter-compact: var(--cf-space-4);\n}\n\n/* ── shell frame ──────────────────────────────────────────────────────────── */\n\n/* The library shell is a quiet matte canvas: no ambient glow, no noise field,\n   no gradient. Rank comes from tone and spacing alone. */\n.app[data-shell=\"library\"] ~ .ambient,\nbody:has(.app[data-shell=\"library\"]) .ambient {\n  display: none;\n}\n\n.app[data-shell=\"library\"] {\n  background-color: var(--cf-canvas);\n  color: var(--cf-text);\n  font-family: var(--cf-font-body);\n  letter-spacing: var(--cf-tracking-tight);\n}\n\n/* Two columns: rail, then exactly one document surface. The splitter keeps its\n   binding ID and stays operable; it is the boundary between the two.\n\n   Track assignment is EXPLICIT. `workspace-shell.css:303` hides `.splitter--rail`\n   below 1180px; with auto-placement that removed grid item shifted the document\n   surface into the 5px splitter track, collapsing the whole document to 5px at\n   1100/960/820. Naming each child's column makes the layout independent of how\n   many children a superseded layer happens to display. */\n.app[data-shell=\"library\"] .workspace {\n  display: grid;\n  /* `--rail-w` is the splitter-owned value; `--lib-rail-w` is only the fallback\n     used before the script has written one. */\n  grid-template-columns: var(--rail-w, var(--lib-rail-w)) var(--splitter-w, 5px) minmax(0, 1fr);\n  align-items: stretch;\n  gap: 0;\n  margin: 0;\n  padding: 0;\n  width: auto;\n  max-width: none;\n  min-height: 0;\n  border: 0;\n  border-radius: 0;\n  background: transparent;\n  box-shadow: none;\n  overflow: hidden;\n}\n\n/* ── meetings rail ────────────────────────────────────────────────────────── */\n\n.app[data-shell=\"library\"] #session-rail {\n  min-width: 0;\n  min-block-size: 0;\n  display: flex;\n  flex-direction: column;\n  background-color: var(--cf-rail);\n  border-right: var(--cf-rule-width) solid var(--cf-rule);\n  border-radius: 0;\n  box-shadow: none;\n  backdrop-filter: none;\n  -webkit-backdrop-filter: none;\n}\n\n.app[data-shell=\"library\"] #session-rail .pane__head {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: var(--cf-space-2);\n  padding: var(--lib-gutter) var(--lib-gutter) var(--cf-space-3);\n  border-bottom: var(--cf-rule-width) solid var(--cf-rule);\n  background: transparent;\n}\n\n.app[data-shell=\"library\"] #session-rail .pane__title {\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-md);\n  font-weight: var(--cf-weight-semibold);\n  color: var(--cf-text);\n  letter-spacing: var(--cf-tracking-tight);\n}\n\n.app[data-shell=\"library\"] #session-count {\n  font-family: var(--cf-font-mono);\n  font-size: var(--cf-text-xs);\n  font-weight: var(--cf-weight-medium);\n  letter-spacing: var(--cf-tracking-mono);\n  font-variant-numeric: tabular-nums;\n  color: var(--cf-text-faint);\n  background: transparent;\n  border: 0;\n  padding: 0;\n}\n\n.app[data-shell=\"library\"] #session-rail .pane__body {\n  flex: 1;\n  min-block-size: 0;\n  overflow-y: auto;\n  overflow-x: hidden;\n  padding: var(--cf-space-2);\n}\n\n.app[data-shell=\"library\"] #session-list {\n  display: flex;\n  flex-direction: column;\n  gap: var(--cf-space-1);\n  margin: 0;\n  padding: 0;\n  list-style: none;\n}\n\n.app[data-shell=\"library\"] .session-item {\n  position: relative;\n  display: flex;\n  align-items: stretch;\n  gap: var(--cf-space-1);\n}\n\n/* Anchored to the ROW, not to the list item.\n   `.session-item` is one hairline taller than `.session-row` (the row carries a\n   transparent selection border), so centring a 44px target against the item\n   left the control's hit area hanging above the row it acts on. Centring it on\n   the row keeps the whole target inside the thing it deletes. */\n.app[data-shell=\"library\"] .session-delete {\n  position: absolute;\n  inset-block-start: 50%;\n  transform: translateY(-50%);\n  inset-inline-end: var(--cf-space-1);\n}\n\n.app[data-shell=\"library\"] .session-row {\n  position: relative;\n  flex: 1;\n  min-width: 0;\n  display: grid;\n  gap: var(--cf-space-1);\n  padding: var(--cf-space-3);\n  /* Reserve the delete affordance's width inside the row so revealing it can\n     never overlap the title it belongs to. The reserve is the TARGET size plus\n     the control's own end inset, because the button is pinned `--cf-space-1`\n     from the row's edge and therefore starts that much further inboard than\n     its width alone accounts for.\n\n     This lives INSIDE the padding shorthand's own rule on purpose: declared in\n     a separate earlier block it was silently reset by `padding:` here, which\n     left a 12px reserve under a 44px control and ran the Korean title 29px\n     beneath the delete button at 320px. */\n  padding-inline-end: calc(var(--cf-target-min) + var(--cf-space-1));\n  border: var(--cf-rule-width) solid transparent;\n  border-radius: var(--cf-radius-sm);\n  background: transparent;\n  color: var(--cf-text-muted);\n  text-align: left;\n  cursor: pointer;\n  transition-property: background-color, border-color, color;\n  transition-duration: var(--cf-motion-quick);\n  transition-timing-function: var(--cf-motion-ease);\n}\n\n.app[data-shell=\"library\"] .session-row:hover {\n  background-color: var(--cf-surface);\n  color: var(--cf-text);\n}\n\n/* Selection is tone plus a persistent marker, never colour alone. */\n.app[data-shell=\"library\"] .session-row--selected,\n.app[data-shell=\"library\"] .session-row[aria-pressed=\"true\"] {\n  background-color: var(--cf-raised);\n  border-color: var(--cf-rule);\n  color: var(--cf-text);\n}\n\n.app[data-shell=\"library\"] .session-row--selected::before {\n  content: \"\";\n  position: absolute;\n  inset-block: var(--cf-space-2);\n  inset-inline-start: 0;\n  width: 2px;\n  border-radius: var(--cf-radius-pill);\n  background-color: var(--cf-accent);\n}\n\n.app[data-shell=\"library\"] .session-row__title {\n  font-size: var(--cf-text-md);\n  font-weight: var(--cf-weight-medium);\n  line-height: var(--cf-leading-tight);\n  color: inherit;\n  word-break: keep-all;\n  overflow-wrap: anywhere;\n}\n\n.app[data-shell=\"library\"] .session-row__meta {\n  display: flex;\n  align-items: center;\n  gap: var(--cf-space-2);\n  font-family: var(--cf-font-mono);\n  font-size: var(--cf-text-xs);\n  letter-spacing: var(--cf-tracking-mono);\n  color: var(--cf-text-faint);\n}\n\n.app[data-shell=\"library\"] .session-row__status {\n  padding: 0 var(--cf-space-2);\n  border-radius: var(--cf-radius-pill);\n  border: var(--cf-rule-width) solid var(--cf-rule);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-xs);\n  letter-spacing: normal;\n}\n\n.app[data-shell=\"library\"] .session-row__status--open {\n  color: var(--cf-record);\n  border-color: var(--cf-record-line);\n  background-color: var(--cf-record-soft);\n}\n\n.app[data-shell=\"library\"] .session-delete {\n  align-self: center;\n  display: grid;\n  place-items: center;\n  /* The GLYPH stays a quiet 32px square; the TARGET is the 44px floor from the\n     foundation (DESIGN 9.12 / WCAG 2.2 AA). Sizing the box to the target and\n     sizing the icon inside it keeps the destructive control comfortably\n     hittable without making the rail row look heavier. */\n  width: var(--cf-target-min);\n  height: var(--cf-target-min);\n  min-width: var(--cf-target-min);\n  min-height: var(--cf-target-min);\n  padding: 0;\n  border: 0;\n  border-radius: var(--cf-radius-sm);\n  background: transparent;\n  color: var(--cf-text-faint);\n  opacity: 0;\n  cursor: pointer;\n  transition-property: opacity, color, background-color;\n  transition-duration: var(--cf-motion-quick);\n  transition-timing-function: var(--cf-motion-ease);\n}\n\n/* The glyph keeps its original 32px square inside the 44px target, so growing\n   the hit area does not grow the drawn icon. */\n.app[data-shell=\"library\"] .session-delete svg {\n  width: var(--cf-space-8);\n  height: var(--cf-space-8);\n}\n\n/* Progressive disclosure: destructive affordance appears on intent, and is always\n   revealed for keyboard users so it can never become mouse-only. */\n.app[data-shell=\"library\"] .session-item:hover .session-delete,\n.app[data-shell=\"library\"] .session-delete:focus-visible {\n  opacity: 1;\n  color: var(--cf-record);\n}\n\n.app[data-shell=\"library\"] #session-empty {\n  margin: var(--cf-space-4) var(--cf-space-3);\n  color: var(--cf-text-faint);\n  font-size: var(--cf-text-sm);\n  line-height: var(--cf-leading-normal);\n  word-break: keep-all;\n}\n\n/* ── splitters ────────────────────────────────────────────────────────────── */\n\n.app[data-shell=\"library\"] #session-rail { grid-column: 1; grid-row: 1; }\n.app[data-shell=\"library\"] #splitter-rail { grid-column: 2; grid-row: 1; }\n.app[data-shell=\"library\"] #document-surface { grid-column: 3; grid-row: 1; }\n\n/* Both adjacent panes are present at these widths, so the splitter stays\n   pointer- and keyboard-resizable (DESIGN 9.9). The superseded layer collapses\n   it at 1180px, which this restores for the library shell only. */\n.app[data-shell=\"library\"] #splitter-rail {\n  display: block;\n  background-color: var(--cf-rule);\n  border: 0;\n}\n\n/* The transcript splitter belongs to the live split only. In library shell it is\n   inert and out of the tab order: there is no second pane for it to resize. */\n.app[data-shell=\"library\"] #splitter-transcript {\n  display: none;\n}\n\n/* ── the single document surface ──────────────────────────────────────────── */\n\n.app[data-shell=\"library\"] #document-surface {\n  min-width: 0;\n  min-block-size: 0;\n  display: grid;\n  grid-template-rows: auto minmax(0, 1fr);\n  background-color: var(--cf-canvas);\n}\n\n.app[data-shell=\"library\"] #meeting-chrome {\n  display: grid;\n  gap: var(--cf-space-3);\n  padding: var(--lib-gutter) var(--lib-gutter) 0;\n  border: 0;\n  background: transparent;\n}\n\n.app[data-shell=\"library\"] #meeting-chrome-title {\n  margin: 0;\n  font-family: var(--cf-font-display);\n  font-size: var(--cf-text-2xl);\n  font-weight: var(--cf-weight-semibold);\n  line-height: var(--cf-leading-tight);\n  letter-spacing: -0.02em;\n  color: var(--cf-text);\n  text-wrap: balance;\n  word-break: keep-all;\n}\n\n.app[data-shell=\"library\"] .meeting-chrome__props {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--cf-space-3);\n  margin: 0;\n}\n\n.app[data-shell=\"library\"] .meeting-chrome__prop {\n  padding: 0;\n  border: 0;\n  background: transparent;\n  font-family: var(--cf-font-mono);\n  font-size: var(--cf-text-xs);\n  letter-spacing: var(--cf-tracking-mono);\n  color: var(--cf-text-faint);\n}\n\n/* ── detail tabs ──────────────────────────────────────────────────────────── */\n\n.app[data-shell=\"library\"] .detail-tabs {\n  display: flex;\n  align-items: stretch;\n  gap: var(--cf-space-4);\n  margin: 0;\n  padding: 0;\n  border: 0;\n  border-bottom: var(--cf-rule-width) solid var(--cf-rule);\n  background: transparent;\n  border-radius: 0;\n  overflow-x: auto;\n  overflow-y: hidden;\n  scrollbar-width: none;\n}\n\n.app[data-shell=\"library\"] .detail-tabs::-webkit-scrollbar { display: none; }\n\n.app[data-shell=\"library\"] .detail-tabs__btn {\n  flex: 0 0 auto;\n  position: relative;\n  padding: var(--cf-space-3) 0 var(--cf-space-3);\n  border: 0;\n  border-radius: 0;\n  background: transparent;\n  color: var(--cf-text-faint);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-md);\n  font-weight: var(--cf-weight-medium);\n  line-height: var(--cf-leading-tight);\n  cursor: pointer;\n  transition-property: color;\n  transition-duration: var(--cf-motion-quick);\n  transition-timing-function: var(--cf-motion-ease);\n}\n\n.app[data-shell=\"library\"] .detail-tabs__btn:hover { color: var(--cf-text-muted); }\n\n/* Selected tab: full-strength text plus a 2px rule. Never colour alone. */\n.app[data-shell=\"library\"] .detail-tabs__btn[aria-selected=\"true\"] {\n  color: var(--cf-text);\n}\n\n.app[data-shell=\"library\"] .detail-tabs__btn[aria-selected=\"true\"]::after {\n  content: \"\";\n  position: absolute;\n  inset-inline: 0;\n  bottom: calc(var(--cf-rule-width) * -1);\n  height: 2px;\n  border-radius: var(--cf-radius-pill);\n  background-color: var(--cf-text);\n}\n\n/* Focus is always painted and is independent of the recording and AI colours.\n   `:focus` is included alongside `:focus-visible` so a control that receives\n   focus programmatically (tab activation moves focus for us) is still ringed. */\n.app[data-shell=\"library\"] .detail-tabs__btn:focus,\n.app[data-shell=\"library\"] .detail-tabs__btn:focus-visible,\n.app[data-shell=\"library\"] .session-row:focus-visible,\n.app[data-shell=\"library\"] .session-delete:focus-visible,\n.app[data-shell=\"library\"] #notes-input:focus-visible,\n.app[data-shell=\"library\"] #splitter-rail:focus-visible {\n  outline: var(--cf-focus-width) solid var(--cf-focus);\n  outline-offset: var(--cf-focus-offset);\n}\n\n/* ── the tabpanels: exactly one is present at a time ──────────────────────── */\n\n/* `hidden` is the single source of panel visibility; the shell script owns it.\n   `display: none !important` guarantees a hidden panel is out of the layout and\n   the accessibility tree even if a superseded layer sets `display` on the node. */\n.app[data-shell=\"library\"] .detail-panel[hidden] {\n  display: none !important;\n}\n\n.app[data-shell=\"library\"] .detail-panel:not([hidden]) {\n  min-width: 0;\n  min-block-size: 0;\n  overflow-y: auto;\n  overflow-x: hidden;\n  padding: var(--cf-space-5) var(--lib-gutter) var(--lib-gutter);\n  /* The superseded layers ring the panes; in library shell the tab rule is the\n     only boundary the document needs. */\n  border: 0;\n  border-radius: 0;\n  background: transparent;\n  box-shadow: none;\n  backdrop-filter: none;\n  -webkit-backdrop-filter: none;\n}\n\n/* Overview panel ---------------------------------------------------------- */\n\n/* Flow layout, not grid: an aspect-ratio child inside a grid track resolves its\n   height after the track is sized, which let the slide paint over the block\n   below it. A block formatting context sizes the frame from its own ratio. */\n.app[data-shell=\"library\"] #stage-pane:not([hidden]) {\n  display: flex;\n  flex-direction: column;\n  align-items: stretch;\n  gap: var(--cf-space-6);\n}\n\n/* The live status pill belongs to the live shell alone. */\n.app[data-shell=\"library\"] #island {\n  display: none;\n}\n\n/* DESIGN §9.7: the meeting chrome shows #meeting-chrome-title and\n   #meeting-chrome-date \"with #doc-title and #doc-meta inside it\". Hiding\n   .doc-head dropped two contracted binding elements out of the surface\n   entirely. Instead #doc-title is the ONE visible document title and the chrome\n   title is retired, so nothing is duplicated and no binding ID is lost. */\n.app[data-shell=\"library\"] .doc-head {\n  display: grid;\n  gap: var(--cf-space-1);\n  margin: 0;\n  padding: 0;\n}\n\n.app[data-shell=\"library\"] #meeting-chrome-title {\n  display: none;\n}\n\n.app[data-shell=\"library\"] #doc-title {\n  margin: 0;\n  font-family: var(--cf-font-display);\n  font-size: var(--cf-text-2xl);\n  font-weight: var(--cf-weight-semibold);\n  line-height: var(--cf-leading-tight);\n  letter-spacing: -0.02em;\n  color: var(--cf-text);\n  text-wrap: balance;\n  word-break: keep-all;\n}\n\n.app[data-shell=\"library\"] #doc-meta {\n  margin: 0;\n  font-size: var(--cf-text-sm);\n  line-height: var(--cf-leading-normal);\n  color: var(--cf-text-muted);\n  word-break: keep-all;\n}\n\n/* `.app--capturing` is excluded as well: it is the legacy compatibility signal a\n   phase-less `capture` frame still produces, and this hide rule would otherwise\n   outrank the show rule below on specificity alone. */\n.app[data-shell=\"library\"]:not(.app--capturing):not([data-capture-phase=\"starting\"]):not([data-capture-phase=\"capturing\"]):not([data-capture-phase=\"stopping\"]) #live-topbar {\n  display: none;\n}\n\n/* DESIGN §9.8: \"Stop and timer stay visible and enabled through starting,\n   capturing, and stopping.\"\n\n   The superseded layer gated the topbar on `.app--capturing`, which app.js only\n   sets when the server reports `capturing: true`. During the `starting` phase the\n   server sends `capturing: false, phase: \"starting\"`, so the class is absent and\n   the base `display: none` won - Stop and the timer disappeared exactly while a\n   recording was being established. Driving the topbar from the authoritative\n   capture phase fixes that for all three phases, and also covers the legacy\n   class so a phase-less `capture` frame still shows it. */\n.app[data-capture-phase=\"starting\"] #live-topbar,\n.app[data-capture-phase=\"capturing\"] #live-topbar,\n.app[data-capture-phase=\"stopping\"] #live-topbar,\n.app.app--capturing #live-topbar {\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: var(--cf-space-3);\n  padding: var(--cf-space-2) var(--cf-space-4);\n}\n\n/* Stop is never disabled by a job while a capture is live. */\n.app[data-capture-phase=\"starting\"] #btn-live-stop,\n.app[data-capture-phase=\"capturing\"] #btn-live-stop,\n.app[data-capture-phase=\"stopping\"] #btn-live-stop {\n  display: inline-flex;\n  pointer-events: auto;\n}\n\n/* The slide keeps the deck system's bright paper look (DESIGN 9.5): the contrast\n   against the matte operator canvas is the point. It is presented complete at\n   16:9 and is never cropped. */\n/* `flex: 1` from the legacy stage let the frame settle shorter than the slide it\n   contains, so the next block overlapped it. In library shell the frame is a\n   plain block that takes exactly the height of the 16:9 paper. */\n/* The frame carries the 16:9 ratio itself. Leaving the ratio on the child alone\n   made the grid size this track from a pre-ratio contribution (78px), so the\n   slide painted outside its own frame and over the block beneath it. */\n.app[data-shell=\"library\"] #slide-frame {\n  display: block;\n  flex: none;\n  width: 100%;\n  max-width: var(--lib-measure);\n  aspect-ratio: 16 / 9;\n  height: auto;\n  min-height: 0;\n  margin: 0;\n  padding: 0;\n  border: 0;\n  background: transparent;\n  box-shadow: none;\n  overflow: visible;\n}\n\n.app[data-shell=\"library\"] #current-slide {\n  display: block;\n  flex: none;\n  width: 100%;\n  height: 100%;\n  min-height: 0;\n  padding: 0;\n  border: var(--cf-rule-width) solid var(--cf-rule);\n  border-radius: var(--cf-radius-md);\n  background: var(--cf-surface);\n  box-shadow: none;\n  overflow: hidden;\n  animation: none;\n  container-type: inline-size;\n}\n\n/* The slide body scales with the paper, so a 16:9 preview stays legible at every\n   width instead of overflowing a fixed type scale. One layout for every slide\n   kind: the library preview reads a meeting document, not a deck design. */\n.app[data-shell=\"library\"] #current-slide .slide__inner,\n.app[data-shell=\"library\"] #current-slide .slide__topic-main {\n  display: flex;\n  flex-direction: column;\n  gap: 2cqw;\n  width: 100%;\n  height: 100%;\n  min-height: 0;\n  padding: 5cqw 6cqw;\n  box-sizing: border-box;\n  overflow: hidden;\n  grid-template-columns: none;\n}\n\n.app[data-shell=\"library\"] #current-slide .slide__topic-main { padding: 0; height: auto; }\n\n/* Decorative deck furniture the document preview does not need. */\n.app[data-shell=\"library\"] #current-slide .slide__topic-visual,\n.app[data-shell=\"library\"] #current-slide .slide__accent,\n.app[data-shell=\"library\"] #current-slide .slide__cover-number,\n.app[data-shell=\"library\"] #current-slide .slide__cover-foot {\n  display: none;\n}\n\n.app[data-shell=\"library\"] #current-slide .slide__title {\n  margin: 0;\n  font-size: 5cqw;\n  line-height: 1.15;\n  letter-spacing: -0.02em;\n  color: var(--cf-text);\n  text-wrap: balance;\n  word-break: keep-all;\n}\n\n.app[data-shell=\"library\"] #current-slide .slide__kicker,\n.app[data-shell=\"library\"] #current-slide .slide__index,\n.app[data-shell=\"library\"] #current-slide .slide__kindchip {\n  font-family: var(--cf-font-mono);\n  font-size: 2cqw;\n  letter-spacing: var(--cf-tracking-mono);\n  color: var(--cf-text-faint);\n}\n\n.app[data-shell=\"library\"] #current-slide .slide__bullets {\n  margin: 0;\n  padding-inline-start: 3cqw;\n  display: grid;\n  gap: 1.4cqw;\n}\n\n.app[data-shell=\"library\"] #current-slide .slide__bullets li {\n  font-size: 2.6cqw;\n  line-height: 1.45;\n  color: var(--cf-text-muted);\n  word-break: keep-all;\n}\n\n.app[data-shell=\"library\"] #current-slide .slide__emphasis {\n  margin: auto 0 0;\n  padding: 2cqw 2.4cqw;\n  border-radius: var(--cf-radius-sm);\n  border: var(--cf-rule-width) solid var(--cf-accent-line);\n  background-color: var(--cf-accent-soft);\n  color: var(--cf-accent-text);\n  font-size: 2.4cqw;\n  line-height: 1.4;\n  word-break: keep-all;\n}\n\n.app[data-shell=\"library\"] #current-slide .slide__emphasis-label {\n  margin-inline-end: 1cqw;\n  font-family: var(--cf-font-mono);\n  font-size: 1.8cqw;\n  letter-spacing: var(--cf-tracking-mono);\n}\n\n/* Empty stage: one explanation, no decorative blank card (DESIGN 9.13). */\n.app[data-shell=\"library\"] #current-slide .slide__placeholder {\n  display: grid;\n  place-content: center;\n  gap: var(--cf-space-2);\n  height: 100%;\n  padding: var(--cf-space-6);\n  text-align: center;\n}\n\n.app[data-shell=\"library\"] #current-slide .placeholder__mic { display: none; }\n\n.app[data-shell=\"library\"] #current-slide .placeholder__title {\n  margin: 0;\n  font-size: var(--cf-text-lg);\n  font-weight: var(--cf-weight-medium);\n  color: var(--cf-text-muted);\n  word-break: keep-all;\n}\n\n.app[data-shell=\"library\"] #current-slide .placeholder__sub {\n  margin: 0;\n  font-size: var(--cf-text-sm);\n  color: var(--cf-text-faint);\n  word-break: keep-all;\n}\n\n.app[data-shell=\"library\"] #meeting-overview {\n  display: grid;\n  gap: var(--cf-space-6);\n  max-width: var(--lib-measure);\n  margin: 0;\n  padding: 0;\n  border: 0;\n  background: transparent;\n}\n\n.app[data-shell=\"library\"] .meeting-overview__label {\n  margin: 0 0 var(--cf-space-2);\n  font-size: var(--cf-text-xs);\n  font-weight: var(--cf-weight-semibold);\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  color: var(--cf-text-faint);\n}\n\n.app[data-shell=\"library\"] .meeting-overview__empty {\n  margin: 0;\n  color: var(--cf-text-muted);\n  font-size: var(--cf-text-md);\n  line-height: var(--cf-leading-normal);\n}\n\n.app[data-shell=\"library\"] .action-card {\n  display: grid;\n  gap: var(--cf-space-3);\n  padding: var(--cf-space-4);\n  border: var(--cf-rule-width) solid var(--cf-rule);\n  border-radius: var(--cf-radius-md);\n  background-color: var(--cf-surface);\n  box-shadow: none;\n}\n\n.app[data-shell=\"library\"] .action-card__head {\n  display: flex;\n  align-items: flex-start;\n  justify-content: space-between;\n  gap: var(--cf-space-4);\n}\n\n.app[data-shell=\"library\"] .action-card__title {\n  margin: 0;\n  font-size: var(--cf-text-md);\n  font-weight: var(--cf-weight-semibold);\n  color: var(--cf-text);\n}\n\n.app[data-shell=\"library\"] .action-card__sub {\n  margin: var(--cf-space-1) 0 0;\n  font-size: var(--cf-text-sm);\n  line-height: var(--cf-leading-normal);\n  color: var(--cf-text-muted);\n  word-break: keep-all;\n}\n\n.app[data-shell=\"library\"] .action-card__actions {\n  display: flex;\n  gap: var(--cf-space-2);\n  flex-shrink: 0;\n}\n\n.app[data-shell=\"library\"] .action-card__btn {\n  padding: var(--cf-space-1) var(--cf-space-3);\n  border: var(--cf-rule-width) solid var(--cf-rule);\n  border-radius: var(--cf-radius-sm);\n  background-color: var(--cf-raised);\n  color: var(--cf-text-muted);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-sm);\n  cursor: pointer;\n}\n\n/* A gated capability stays visible and muted, never invisible. */\n.app[data-shell=\"library\"] .action-card__btn:disabled {\n  color: var(--cf-text-faint);\n  cursor: not-allowed;\n}\n\n.app[data-shell=\"library\"] .action-card__body {\n  margin: 0;\n  padding-top: var(--cf-space-3);\n  border-top: var(--cf-rule-width) solid var(--cf-rule);\n  color: var(--cf-text-muted);\n  font-size: var(--cf-text-sm);\n  line-height: var(--cf-leading-normal);\n  white-space: pre-wrap;\n  text-wrap: pretty;\n}\n\n/* Notes panel ------------------------------------------------------------- */\n\n.app[data-shell=\"library\"] #notes-panel:not([hidden]) {\n  display: grid;\n  align-content: start;\n  gap: var(--cf-space-3);\n}\n\n/* `style.css:1796` hides `.notes-box` below 600px, which left the Notes tab an\n   empty panel with an unfocusable `#notes-input` at 375 and 320. Notes is a\n   primary library surface at every width, so the library shell restores it. */\n.app[data-shell=\"library\"] #notes-box {\n  display: block;\n  max-width: var(--lib-measure);\n  margin: 0;\n  padding: 0;\n  border: 0;\n  background: transparent;\n}\n\n.app[data-shell=\"library\"] #notes-box > #notes-input { display: block; }\n\n.app[data-shell=\"library\"] .notes-box__summary {\n  margin-bottom: var(--cf-space-3);\n  font-size: var(--cf-text-xs);\n  font-weight: var(--cf-weight-semibold);\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  color: var(--cf-text-faint);\n  cursor: pointer;\n  list-style: none;\n}\n\n.app[data-shell=\"library\"] .notes-box__summary::-webkit-details-marker { display: none; }\n\n.app[data-shell=\"library\"] #notes-input {\n  display: block;\n  width: 100%;\n  min-height: 320px;\n  padding: var(--cf-space-4);\n  border: var(--cf-rule-width) solid var(--cf-rule);\n  border-radius: var(--cf-radius-md);\n  background-color: var(--cf-surface);\n  color: var(--cf-text);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-lg);\n  line-height: var(--cf-leading-normal);\n  resize: vertical;\n  text-wrap: pretty;\n  word-break: keep-all;\n}\n\n.app[data-shell=\"library\"] #notes-input::placeholder { color: var(--cf-text-faint); }\n\n/* The live scratch note is a live-shell affordance; notes live in #notes-input. */\n.app[data-shell=\"library\"] #live-note { display: none; }\n\n.app[data-shell=\"library\"] .notes-panel__hint {\n  max-width: var(--lib-measure);\n  margin: 0;\n  color: var(--cf-text-faint);\n  font-size: var(--cf-text-xs);\n  word-break: keep-all;\n}\n\n/* Transcript panel -------------------------------------------------------- */\n\n.app[data-shell=\"library\"] #transcript-pane:not([hidden]) {\n  display: grid;\n  align-content: start;\n  border: 0;\n  background: transparent;\n}\n\n/* Flow layout, not grid. A fixed row template starved #transcript-body to 0px\n   once the capability rows were restored, and its empty-state text then rendered\n   outside its own box. A flex column sizes every child to its content and lets\n   the panel above own the scrolling. */\n.app[data-shell=\"library\"] #transcript-card {\n  display: flex;\n  flex-direction: column;\n  align-items: stretch;\n  gap: var(--cf-space-3);\n  max-width: var(--lib-measure);\n  min-block-size: 0;\n  margin: 0;\n  padding: 0;\n  border: 0;\n  border-radius: 0;\n  background: transparent;\n  box-shadow: none;\n  backdrop-filter: none;\n  -webkit-backdrop-filter: none;\n}\n\n.app[data-shell=\"library\"] #transcript-card .pane__head {\n  display: flex;\n  align-items: baseline;\n  justify-content: space-between;\n  gap: var(--cf-space-3);\n  padding: 0 0 var(--cf-space-2);\n  border-bottom: var(--cf-rule-width) solid var(--cf-rule);\n  background: transparent;\n}\n\n.app[data-shell=\"library\"] #transcript-card .pane__eyebrow { display: none; }\n\n.app[data-shell=\"library\"] #transcript-card .pane__title {\n  font-size: var(--cf-text-xs);\n  font-weight: var(--cf-weight-semibold);\n  letter-spacing: 0.08em;\n  text-transform: uppercase;\n  color: var(--cf-text-faint);\n}\n\n.app[data-shell=\"library\"] #transcript-count {\n  font-family: var(--cf-font-mono);\n  font-size: var(--cf-text-xs);\n  letter-spacing: var(--cf-tracking-mono);\n  font-variant-numeric: tabular-nums;\n  color: var(--cf-text-faint);\n  background: transparent;\n  border: 0;\n  padding: 0;\n}\n\n/* Server-gated capabilities stay visible and muted with their exact machine\n   reason (DESIGN 9.11): \"Disabled is muted, never invisible.\" Hiding these rows\n   removed a real, contracted affordance, so they keep their place under the\n   transcript header and are only quietened. */\n.app[data-shell=\"library\"] .transcript-context,\n.app[data-shell=\"library\"] .transcript-tools {\n  display: flex;\n  flex-wrap: wrap;\n  align-items: center;\n  gap: var(--cf-space-2);\n  margin: 0;\n  padding: 0 0 var(--cf-space-3);\n  border: 0;\n  background: transparent;\n}\n\n.app[data-shell=\"library\"] .capability-btn {\n  min-height: 28px;\n  padding: var(--cf-space-1) var(--cf-space-3);\n  border: var(--cf-rule-width) solid var(--cf-rule);\n  border-radius: var(--cf-radius-sm);\n  background: transparent;\n  color: var(--cf-text-faint);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-xs);\n}\n\n.app[data-shell=\"library\"] .capability-reason {\n  color: var(--cf-text-faint);\n  font-size: var(--cf-text-xs);\n}\n\n/* The resize grips belong to the live docked pane, which library shell has not. */\n.app[data-shell=\"library\"] .transcript-grip {\n  display: none;\n}\n\n.app[data-shell=\"library\"] #transcript-body {\n  flex: 0 0 auto;\n  min-block-size: 0;\n  height: auto;\n  padding: 0;\n  overflow: visible;\n  background: transparent;\n}\n\n.app[data-shell=\"library\"] #transcript-stream {\n  display: grid;\n  gap: var(--cf-space-4);\n  padding: 0;\n}\n\n.app[data-shell=\"library\"] .feed-line {\n  display: grid;\n  gap: var(--cf-space-1);\n  padding: 0;\n  border: 0;\n  background: transparent;\n}\n\n.app[data-shell=\"library\"] .feed-line__meta {\n  display: flex;\n  align-items: center;\n  gap: var(--cf-space-2);\n  font-family: var(--cf-font-mono);\n  font-size: var(--cf-text-xs);\n  letter-spacing: var(--cf-tracking-mono);\n  color: var(--cf-text-faint);\n}\n\n/* Finished transcript prose is primary reading matter, so it is painted at full\n   strength. The superseded layers dimmed it to 0.85 opacity for the live feed,\n   which would drop the measured contrast below the AA body-text floor. */\n.app[data-shell=\"library\"] .feed-line,\n.app[data-shell=\"library\"] .feed-line__text {\n  opacity: 1;\n  animation: none;\n}\n\n.app[data-shell=\"library\"] .feed-line__text {\n  font-size: var(--cf-text-lg);\n  line-height: var(--cf-leading-normal);\n  color: var(--cf-text);\n  text-wrap: pretty;\n  word-break: keep-all;\n}\n\n.app[data-shell=\"library\"] #transcript-empty {\n  margin: var(--cf-space-4) 0;\n  color: var(--cf-text-faint);\n  font-size: var(--cf-text-sm);\n  line-height: var(--cf-leading-normal);\n}\n\n.app[data-shell=\"library\"] #transcript-trunc {\n  margin-bottom: var(--cf-space-3);\n  padding: var(--cf-space-2) var(--cf-space-3);\n  border: var(--cf-rule-width) solid var(--cf-rule);\n  border-radius: var(--cf-radius-sm);\n  color: var(--cf-text-muted);\n  font-size: var(--cf-text-xs);\n  background-color: var(--cf-surface);\n}\n\n/* ── dock: only the actions that belong to the current context ────────────── */\n\n/* The dock is exactly as tall as the controls it holds. The legacy fixed 200px\n   height existed for the slide filmstrip, which library shell does not show. */\n.app[data-shell=\"library\"] .dock {\n  display: flex;\n  flex-direction: column;\n  height: auto;\n  /* Bounded and scrollable: opening the save/export disclosure must never push a\n     control below a fold the user cannot reach. `.app` is a `100dvh` flex column,\n     so `flex-shrink: 0` let the grown dock overflow the viewport entirely - at\n     1100x800 that put #btn-reset at y=801, one pixel past the fold and\n     unhittable. Allowing the dock to shrink and own its own scroll keeps every\n     control reachable however tall the disclosed set becomes. */\n  flex-shrink: 1;\n  min-height: 0;\n  /* Two fifths, not a half. At 50vh the action surface became a wall: measured\n     at 1244x836 it took exactly 0.5 of the viewport and pushed the document\n     surface into a sliver. The set below is a real multi-column grid, so it no\n     longer NEEDS half the screen to show nine capabilities. */\n  max-height: 40vh;\n  overflow-y: auto;\n  overscroll-behavior: contain;\n  border-top: var(--cf-rule-width) solid var(--cf-rule);\n  /* The superseded layers give the dock a 999px pill radius. A rounded corner on\n     a full-bleed rail CLIPS the outer columns of the action grid: measured at\n     375px, the first 3px of #btn-export-md and the last 3px of #btn-export-json\n     were covered, so the leftmost and rightmost controls in every row lost part\n     of their own pointer target. The operator rail is a flat edge-to-edge\n     surface (DESIGN 9.4), so the radius is cleared rather than compensated for\n     with a gutter. */\n  border-radius: 0;\n  background-color: var(--cf-rail);\n  box-shadow: none;\n  backdrop-filter: none;\n  -webkit-backdrop-filter: none;\n}\n\n/* The language readout and the slide filmstrip duplicate what the document\n   surface and its tabs already state. */\n.app[data-shell=\"library\"] .dock__context,\n.app[data-shell=\"library\"] .dock__panel,\n.app[data-shell=\"library\"] .glance {\n  display: none;\n}\n\n/* The three-way output control is a real capability (DESIGN 9.11): it reveals or\n   focuses an existing surface and never duplicates content. Here it selects the\n   tab that already owns the surface, so it stays available - quiet, not loud. */\n.app[data-shell=\"library\"] .output-switcher {\n  display: flex;\n  align-items: center;\n  gap: var(--cf-space-1);\n  margin: 0;\n  padding: 0;\n  border: 0;\n  background: transparent;\n}\n\n.app[data-shell=\"library\"] .output-switcher__item {\n  display: inline-flex;\n  align-items: center;\n  gap: var(--cf-space-1);\n  min-height: 28px;\n  padding: var(--cf-space-1) var(--cf-space-3);\n  border: var(--cf-rule-width) solid transparent;\n  border-radius: var(--cf-radius-sm);\n  background: transparent;\n  color: var(--cf-text-faint);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-xs);\n  text-decoration: none;\n  cursor: pointer;\n}\n\n.app[data-shell=\"library\"] .output-switcher__item[aria-current=\"page\"] {\n  border-color: var(--cf-rule);\n  background-color: var(--cf-surface);\n  color: var(--cf-text);\n}\n\n.app[data-shell=\"library\"] .output-switcher__item:disabled {\n  color: var(--cf-text-faint);\n  cursor: not-allowed;\n}\n\n.app[data-shell=\"library\"] .output-switcher__item:focus-visible {\n  outline: var(--cf-focus-width) solid var(--cf-focus);\n  outline-offset: var(--cf-focus-offset);\n}\n\n.app[data-shell=\"library\"] .dock__commandbar {\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: var(--cf-space-3);\n  padding: var(--cf-space-3) var(--lib-gutter);\n  border: 0;\n  background: transparent;\n}\n\n/* Exactly one dominant primary action in library shell: start recording. */\n.app[data-shell=\"library\"] .capture-pill {\n  display: flex;\n  align-items: center;\n  gap: var(--cf-space-3);\n  padding: 0;\n  border: 0;\n  background: transparent;\n  box-shadow: none;\n}\n\n.app[data-shell=\"library\"] .capture-pill__mode,\n.app[data-shell=\"library\"] .record-waveform {\n  display: none;\n}\n\n.app[data-shell=\"library\"] #btn-record {\n  display: inline-flex;\n  align-items: center;\n  gap: var(--cf-space-2);\n  min-height: 44px;\n  padding: var(--cf-space-2) var(--cf-space-5);\n  border: var(--cf-rule-width) solid var(--cf-record-line);\n  border-radius: var(--cf-radius-pill);\n  background-color: var(--cf-record-soft);\n  color: var(--cf-record);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-md);\n  font-weight: var(--cf-weight-semibold);\n  cursor: pointer;\n  transition-property: background-color, border-color, color;\n  transition-duration: var(--cf-motion-quick);\n  transition-timing-function: var(--cf-motion-ease);\n}\n\n.app[data-shell=\"library\"] #btn-record:focus-visible {\n  outline: var(--cf-focus-width) solid var(--cf-focus);\n  outline-offset: var(--cf-focus-offset);\n}\n\n.app[data-shell=\"library\"] #btn-record:disabled {\n  color: var(--cf-text-faint);\n  border-color: var(--cf-rule);\n  background-color: transparent;\n  cursor: not-allowed;\n}\n\n/* Secondary actions: quiet, uniform, and only the ones this context can use.\n\n   This row is a GRID, not a flex line. As a flex container it sized\n   `.dock__more` to its content, so the disclosure's own `grid-column: 1 / -1`\n   never applied and its auto-fit set collapsed to a single 110.5px column -\n   nine capabilities stacked vertically down a narrow strip, which is exactly the\n   dashboard wall this task removes. */\n.app[data-shell=\"library\"] .dock__tabs {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));\n  align-items: center;\n  gap: var(--cf-space-2);\n  padding: 0 var(--lib-gutter) var(--cf-space-3);\n  border: 0;\n  background: transparent;\n}\n\n.app[data-shell=\"library\"] .dock__label,\n.app[data-shell=\"library\"] .dock__spacer {\n  display: none;\n}\n\n/* The save/export disclosure is ONE real control in both shells.\n\n   It must not be dissolved with `display: contents` + a hidden summary: a CLOSED\n   <details> still hides its content (the UA applies `content-visibility: hidden`\n   to the slot), so removing the summary removes the only thing that could open\n   it. Measured at 1440x900, 1244x836 and 1100x800: 8 of 9 dock capabilities were\n   unreachable and 7 sat below an unscrollable fold.\n\n   Keeping the same disclosure here keeps every capability reachable by mouse,\n   keyboard and AT, and keeps ONE progressive-disclosure pattern across shells. */\n.app[data-shell=\"library\"] .dock__more {\n  grid-column: 1 / -1;\n  min-inline-size: 0;\n}\n\n.app[data-shell=\"library\"] .dock__more-summary {\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  min-block-size: 32px;\n  padding: var(--cf-space-1) var(--cf-space-3);\n  border: var(--cf-rule-width) solid var(--cf-rule);\n  border-radius: var(--cf-radius-sm);\n  background-color: transparent;\n  color: var(--cf-text-muted);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-sm);\n  list-style: none;\n  cursor: pointer;\n  transition-property: background-color, border-color, color;\n  transition-duration: var(--cf-motion-quick);\n  transition-timing-function: var(--cf-motion-ease);\n}\n\n.app[data-shell=\"library\"] .dock__more-summary::-webkit-details-marker { display: none; }\n\n.app[data-shell=\"library\"] .dock__more-summary::after {\n  content: \"\";\n  margin-inline-start: var(--cf-space-2);\n  inline-size: 6px;\n  block-size: 6px;\n  border-inline-end: var(--cf-rule-width) solid currentColor;\n  border-block-end: var(--cf-rule-width) solid currentColor;\n  transform: rotate(45deg) translate(-2px, -2px);\n}\n\n.app[data-shell=\"library\"] .dock__more[open] .dock__more-summary::after {\n  transform: rotate(-135deg) translate(-2px, -2px);\n}\n\n.app[data-shell=\"library\"] .dock__more-summary:hover {\n  background-color: var(--cf-surface);\n  color: var(--cf-text);\n}\n\n.app[data-shell=\"library\"] .dock__more-summary:focus-visible {\n  outline: var(--cf-focus-width) solid var(--cf-focus);\n  outline-offset: var(--cf-focus-offset);\n}\n\n/* The disclosed set composes across the full width of the action surface rather\n   than down one column. `auto-fit` + a 120px floor gives eight capabilities a\n   readable multi-column layout at every width the library shell supports. */\n.app[data-shell=\"library\"] .dock__more-set {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));\n  gap: var(--cf-space-2);\n  margin-block-start: var(--cf-space-2);\n}\n\n/* The superseded layers append a \"→\" scroll hint to the action row. The library\n   dock wraps instead of scrolling, so that arrow would advertise an interaction\n   that does not exist. */\n.app[data-shell=\"library\"] .dock__tabs::after {\n  content: none;\n  display: none;\n}\n\n.app[data-shell=\"library\"] .dock__btn {\n  min-height: 32px;\n  padding: var(--cf-space-1) var(--cf-space-3);\n  border: var(--cf-rule-width) solid var(--cf-rule);\n  border-radius: var(--cf-radius-sm);\n  background-color: transparent;\n  color: var(--cf-text-muted);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-sm);\n  cursor: pointer;\n  transition-property: background-color, border-color, color;\n  transition-duration: var(--cf-motion-quick);\n  transition-timing-function: var(--cf-motion-ease);\n}\n\n.app[data-shell=\"library\"] .dock__btn:hover:not(:disabled) {\n  background-color: var(--cf-surface);\n  color: var(--cf-text);\n}\n\n.app[data-shell=\"library\"] .dock__btn:focus-visible {\n  outline: var(--cf-focus-width) solid var(--cf-focus);\n  outline-offset: var(--cf-focus-offset);\n}\n\n.app[data-shell=\"library\"] .dock__btn:disabled {\n  color: var(--cf-text-faint);\n  cursor: not-allowed;\n}\n\n.app[data-shell=\"library\"] .dock__btn--danger { color: var(--cf-record); }\n\n.app[data-shell=\"library\"] #compile-status,\n.app[data-shell=\"library\"] #last-saved {\n  font-family: var(--cf-font-mono);\n  font-size: var(--cf-text-xs);\n  letter-spacing: var(--cf-tracking-mono);\n  color: var(--cf-text-muted);\n}\n\n/* ── topbar ───────────────────────────────────────────────────────────────── */\n\n.app[data-shell=\"library\"] .topbar {\n  border-bottom: var(--cf-rule-width) solid var(--cf-rule);\n  background-color: var(--cf-rail);\n  box-shadow: none;\n  backdrop-filter: none;\n  -webkit-backdrop-filter: none;\n}\n\n/* The capability rows must never spill out of the transcript column they live\n   in, at any width and in any shell. The superseded layers laid them out for a\n   wide dock, so in a narrow transcript column the trailing reason text pushed\n   past the pane edge. Wrapping is shell-independent on purpose. */\n#transcript-card .transcript-context,\n#transcript-card .transcript-tools {\n  flex-wrap: wrap;\n  max-width: 100%;\n  min-width: 0;\n  overflow-wrap: anywhere;\n}\n\n#transcript-card .capability-btn,\n#transcript-card .capability-reason {\n  max-width: 100%;\n  min-width: 0;\n}\n\n/* ══ live shell (Todo 12) ═════════════════════════════════════════════════════\n\n   Ownership: the focused live workspace — the complete generated 16:9 PPT stage\n   and the complete transcript, together, plus the persistent Stop/timer and the\n   live action dock.\n\n   Contract (DESIGN 9.8/9.9):\n     * >= 900px  stage and transcript side by side, tops aligned;\n     * <  900px  stage stacked ABOVE transcript;\n     * the 16:9 slide is always fully contained and never illegibly cropped;\n     * Stop and the timer stay visible and enabled through starting/capturing/\n       stopping and survive reconnect;\n     * no control is ever clipped or pushed horizontally offscreen.\n\n   Every value below resolves a `--cf-*` token. The slide body is typed in `cqw`\n   against the slide's own container, which is what makes one 16:9 layout stay\n   legible from 1440px down to 320px without a per-width type scale.\n   ════════════════════════════════════════════════════════════════════════════ */\n\n/* Restructuring the library shell moved #stage-pane and #transcript-pane inside\n   #document-surface. The live split therefore has to be expressed on that\n   wrapper instead of on .workspace. */\n/* In live shell the workspace holds ONE child that matters: the document\n   surface, which performs the stage|transcript split itself. The superseded\n   layer still declares a three-column workspace grid with `!important`, so the\n   surface is pinned across every track instead of landing in one of them. */\n.app[data-shell=\"live\"] #document-surface,\n.app.app--capturing #document-surface {\n  grid-column: 1 / -1;\n  grid-row: 1;\n  display: grid;\n  /* Stacked by default: chrome, then the complete stage, then the transcript.\n     Explicit rows keep the two panes from sharing one implicit track. */\n  grid-template-rows: auto minmax(0, 1fr) minmax(0, 1fr);\n  grid-template-columns: minmax(0, 1fr);\n  min-block-size: 0;\n}\n\n.app[data-shell=\"live\"] #splitter-rail,\n.app.app--capturing #splitter-rail {\n  display: none;\n}\n\n.app[data-shell=\"live\"] #meeting-chrome,\n.app.app--capturing #meeting-chrome { grid-row: 1; grid-column: 1; }\n\n.app[data-shell=\"live\"] #stage-pane,\n.app.app--capturing #stage-pane { grid-row: 2; grid-column: 1; min-block-size: 0; }\n\n.app[data-shell=\"live\"] #transcript-pane,\n.app.app--capturing #transcript-pane { grid-row: 3; grid-column: 1; min-block-size: 0; }\n\n/* At and above the 900px seam the split is side by side. */\n@media (min-width: 900px) {\n  .app[data-shell=\"live\"] #document-surface,\n  .app.app--capturing #document-surface {\n    grid-template-rows: auto minmax(0, 1fr);\n    /* `--transcript-w` is owned by workspace-split.js, so the transcript splitter\n       keeps resizing the transcript column and `workspace.layout.v1` keeps\n       persisting exactly as before the document surface was introduced. */\n    grid-template-columns: minmax(0, 1fr) var(--splitter-w, 5px) var(--transcript-w, minmax(280px, 0.9fr));\n  }\n\n  .app[data-shell=\"live\"] #meeting-chrome,\n  .app.app--capturing #meeting-chrome { grid-row: 1; grid-column: 1 / -1; }\n\n  .app[data-shell=\"live\"] #stage-pane,\n  .app.app--capturing #stage-pane { grid-row: 2; grid-column: 1; }\n\n  .app[data-shell=\"live\"] #splitter-transcript,\n  .app.app--capturing #splitter-transcript { grid-row: 2; grid-column: 2; display: block; }\n\n  .app[data-shell=\"live\"] #transcript-pane,\n  .app.app--capturing #transcript-pane { grid-row: 2; grid-column: 3; }\n}\n\n/* Below the 900px seam the complete stage stacks above the transcript. */\n@media (max-width: 899px) {\n  .app[data-shell=\"live\"] #splitter-transcript,\n  .app.app--capturing #splitter-transcript { display: none; }\n}\n\n/* The Notes panel is a library affordance; live keeps the scratch note instead. */\n.app[data-shell=\"live\"] #notes-panel,\n.app.app--capturing #notes-panel { display: none; }\n\n/* ── live: the two regions ───────────────────────────────────────────────────── */\n\n.app[data-shell=\"live\"] {\n  /* The live gutter is tighter than the library's: the stage is the document\n     here, so chrome gives its space to the slide. */\n  --live-gutter: var(--cf-space-4);\n  --live-gutter-compact: var(--cf-space-3);\n  /* Reading floor for the transcript column, matching the splitter's own\n     MIN_TRANSCRIPT in workspace-split.js so the two never disagree. */\n  --live-transcript-min: 240px;\n}\n\n.app[data-shell=\"live\"] .workspace {\n  /* One row, one column: the document surface IS the live workspace. The\n     superseded three-track grid left the surface pinned across every track. */\n  display: grid;\n  grid-template-rows: minmax(0, 1fr);\n  grid-template-columns: minmax(0, 1fr);\n  min-block-size: 0;\n  margin: 0;\n  padding: 0;\n  border: 0;\n  background-color: transparent;\n  overflow: hidden;\n}\n\n/* The surface must FILL its workspace row, otherwise its own `1fr` transcript\n   track resolves against the surface's content height and collapses to 0. */\n.app[data-shell=\"live\"] #document-surface {\n  block-size: 100%;\n  min-block-size: 0;\n}\n\n/* The dock is content-sized in live. The superseded layers pin it to a fixed\n   200-231px, which stole that height from the stage and the transcript. */\n.app[data-shell=\"live\"] .dock {\n  flex: none;\n  block-size: auto;\n}\n\n/* Both panes are scroll owners, so both must be allowed to shrink. Without\n   `min-block-size: 0` a grid item floors at its content height and pushes the\n   root into overflow instead of scrolling its own body. */\n.app[data-shell=\"live\"] #stage-pane,\n.app[data-shell=\"live\"] #transcript-pane {\n  min-inline-size: 0;\n  min-block-size: 0;\n  margin: 0;\n  border: 0;\n  border-radius: 0;\n  background-color: transparent;\n  box-shadow: none;\n}\n\n.app[data-shell=\"live\"] #stage-pane {\n  display: flex;\n  flex-direction: column;\n  gap: var(--cf-space-3);\n  padding: var(--cf-space-3) var(--live-gutter) var(--live-gutter);\n  /* The stage never scrolls: the slide is sized to fit it (see #slide-frame). */\n  overflow: hidden;\n}\n\n/* Live drops the library's meeting chrome entirely: the meetings rail, detail\n   tabs, document title block and Overview cards are library affordances\n   (DESIGN 9.8 \"library chrome is not present in live shell\"). */\n.app[data-shell=\"live\"] #meeting-chrome,\n.app[data-shell=\"live\"] #session-rail,\n.app[data-shell=\"live\"] .detail-tabs,\n.app[data-shell=\"live\"] .doc-head,\n.app[data-shell=\"live\"] .meeting-chrome__props,\n.app[data-shell=\"live\"] #meeting-overview,\n.app[data-shell=\"live\"] .glance,\n.app[data-shell=\"live\"] .live-note,\n.app[data-shell=\"live\"] .transcript-context,\n.app[data-shell=\"live\"] .transcript-tools,\n.app[data-shell=\"live\"] .transcript-grip,\n.app[data-shell=\"live\"] .dock__panel,\n.app[data-shell=\"live\"] .dock__context,\n.app[data-shell=\"live\"] .dock__label,\n.app[data-shell=\"live\"] .dock__spacer {\n  display: none;\n}\n\n/* ── live: persistent Stop + timer ────────────────────────────────────────── */\n\n/* The topbar is a flex item with a fixed height, declared BEFORE the stage so\n   the slide frame below it can claim exactly the remaining space. */\n.app[data-shell=\"live\"] #live-topbar {\n  display: flex;\n  flex: none;\n  align-items: center;\n  justify-content: center;\n  gap: var(--cf-space-3);\n  min-block-size: 36px;\n  margin: 0;\n  padding: 0;\n  border: 0;\n  background-color: transparent;\n  box-shadow: none;\n  position: static;\n}\n\n.app[data-shell=\"live\"] .live-topbar__nav { display: none; }\n\n.app[data-shell=\"live\"] .live-topbar__capsule {\n  display: inline-flex;\n  align-items: center;\n  gap: var(--cf-space-3);\n  min-block-size: 36px;\n  padding: var(--cf-space-1) var(--cf-space-4) var(--cf-space-1) var(--cf-space-1);\n  border: var(--cf-rule-width) solid var(--cf-record-line);\n  border-radius: var(--cf-radius-pill);\n  background-color: var(--cf-surface);\n  box-shadow: none;\n}\n\n.app[data-shell=\"live\"] #btn-live-stop {\n  display: inline-flex;\n  align-items: center;\n  gap: var(--cf-space-2);\n  /* 28px is the visual pill at comfortable widths. This is NOT a 44px target: an\n     earlier comment here claimed the padded capsule around it satisfied DESIGN\n     9.12, which credits a decorative parent with the child's target size. A\n     pointer that lands on the capsule but outside this button does not stop the\n     recording. The real 44px floor is applied to the button itself in the\n     max-width: 375px block below and verified by an edge-band hit test. */\n  min-block-size: 28px;\n  padding: 0 var(--cf-space-3);\n  border: 0;\n  border-radius: var(--cf-radius-pill);\n  background-color: var(--cf-record);\n  color: var(--cf-canvas);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-xs);\n  font-weight: var(--cf-weight-semibold);\n  cursor: pointer;\n  transition: background-color var(--cf-motion-quick) var(--cf-motion-ease);\n}\n\n.app[data-shell=\"live\"] #btn-live-stop:focus-visible {\n  outline: var(--cf-focus-width) solid var(--cf-focus);\n  outline-offset: var(--cf-focus-offset);\n}\n\n.app[data-shell=\"live\"] .live-topbar__stop-dot {\n  inline-size: 8px;\n  block-size: 8px;\n  border: 2px solid currentColor;\n  border-radius: 2px;\n}\n\n.app[data-shell=\"live\"] .live-topbar__sep {\n  inline-size: var(--cf-rule-width);\n  block-size: 16px;\n  background-color: var(--cf-rule);\n}\n\n.app[data-shell=\"live\"] #live-topbar-timer {\n  color: var(--cf-text);\n  font-family: var(--cf-font-mono);\n  font-size: var(--cf-text-xs);\n  font-variant-numeric: tabular-nums;\n  letter-spacing: var(--cf-tracking-mono);\n}\n\n/* ── live: the complete 16:9 generated PPT stage ────────────────────────────── */\n\n/* THE containment rule. The frame is a flex item that may shrink (`min-height:\n   0`), and the slide inside it is sized by `aspect-ratio` against BOTH axes:\n   `max-inline-size: 100%` bounds it by the available width, and\n   `max-block-size: 100%` bounds it by the available height. Whichever runs out\n   first wins, so the complete 16:9 slide is always fully contained — it is\n   never cropped by a short viewport and never overflows a narrow one.\n\n   The superseded layer instead stretched #current-slide to `height: 100%` of a\n   free-height frame, which is why the characterized baseline measured aspect\n   ratios from 1.257 to 13.317 and painted the deck's bullets outside the slide. */\n.app[data-shell=\"live\"] #slide-frame {\n  /* THE containment rule. The frame carries the 16:9 ratio itself and is bounded\n     on BOTH axes, so the slide inside it only has to fill it.\n\n     `height: 100%` of the stage's remaining space is the height bound;\n     `aspect-ratio` derives the width from it; `max-width: 100%` bounds that\n     width when the stage is the narrower constraint, and because the ratio is on\n     the frame, the HEIGHT then follows the clamped width automatically. That is\n     what makes the complete slide contained whichever axis runs out first.\n\n     `margin-inline: auto` centres it once width is the binding axis.\n\n     Physical properties, for the same cascade reason as the slide: `style.css`\n     already declares `.slide-frame { width: 100% }` and `flex: 1`. */\n  /* A size-query container. Declaring `container-type: size` makes BOTH of the\n     frame's axes queryable, which is what lets the slide inside it be sized as\n     `min(100cqw, 100cqh * 16/9)` - a real contain-on-both-axes expression.\n\n     This is needed because CSS does not back-propagate a clamped axis: with\n     `height: 100%` + `aspect-ratio` + `max-width: 100%`, a narrower frame clamps\n     the width and leaves the height where it was, breaking the ratio (measured\n     at 960px: 603x382.5, aspect 1.576). Asking the container for both of its own\n     dimensions and picking the smaller result avoids that entirely. */\n  container-type: size;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  flex: 1 1 0;\n  width: 100%;\n  height: auto;\n  min-width: 0;\n  min-height: 0;\n  margin: 0;\n  padding: 0;\n  border: 0;\n  background-color: transparent;\n  box-shadow: none;\n  overflow: hidden;\n  aspect-ratio: auto;\n}\n\n/* Containment on BOTH axes, without a definite size on either.\n\n   `inline-size: 100%` alone is a definite width, so `aspect-ratio` derives the\n   height from it and `max-block-size` can only clip - the slide leaves 16:9 the\n   moment height is the binding axis (measured: 1083x523, aspect 2.07).\n\n   Sizing from the HEIGHT instead makes both bounds real: the block axis is the\n   basis (`block-size: 100%` of the frame's content box), `aspect-ratio` derives\n   the width from it, and `max-inline-size: 100%` bounds that derived width. The\n   frame is a `place-items: center` grid, so whichever axis runs out first wins\n   and the complete 16:9 slide is centred in the space that remains. */\n.app[data-shell=\"live\"] #current-slide {\n  /* Contain on BOTH axes, in one expression. The width is the smaller of the\n     frame's own width and the width a full-height 16:9 slide would need; the\n     height then follows from `aspect-ratio`. Whichever axis runs out first wins,\n     so the COMPLETE slide is always inside the frame and never cropped.\n\n     `cqw`/`cqh` resolve against #slide-frame, which is a `container-type: size`\n     container precisely so both of its axes are available here.\n\n     PHYSICAL properties on purpose: `style.css` declares `.slide { width: 100% }`\n     and `.slide-frame > .slide { flex: 1 1 auto }`, and a logical `inline-size`\n     is a separate declaration that only wins on order. */\n  display: block;\n  flex: 0 0 auto;\n  width: min(100cqw, calc(100cqh * 16 / 9));\n  height: auto;\n  min-width: 0;\n  min-height: 0;\n  aspect-ratio: 16 / 9;\n  min-block-size: 0;\n  margin: 0;\n  padding: 0;\n  border: var(--cf-rule-width) solid var(--cf-rule);\n  border-radius: var(--cf-radius-md);\n  background-color: var(--cf-surface);\n  box-shadow: none;\n  overflow: hidden;\n  animation: none;\n  /* Typography below is expressed in `cqw` against THIS box, so one layout stays\n     proportionate at every stage width instead of needing a per-width scale. */\n  container-type: inline-size;\n}\n\n.app[data-shell=\"live\"] #current-slide .slide__inner,\n.app[data-shell=\"live\"] #current-slide .slide__topic-main {\n  display: flex;\n  flex-direction: column;\n  gap: 2cqw;\n  inline-size: 100%;\n  block-size: 100%;\n  min-block-size: 0;\n  padding: 5cqw 6cqw;\n  box-sizing: border-box;\n  overflow: hidden;\n  grid-template-columns: none;\n}\n\n.app[data-shell=\"live\"] #current-slide .slide__topic-main {\n  padding: 0;\n  block-size: auto;\n}\n\n/* Deck furniture that only reads as decoration at live stage sizes. */\n.app[data-shell=\"live\"] #current-slide .slide__topic-visual,\n.app[data-shell=\"live\"] #current-slide .slide__accent,\n.app[data-shell=\"live\"] #current-slide .slide__cover-number,\n.app[data-shell=\"live\"] #current-slide .slide__cover-foot {\n  display: none;\n}\n\n.app[data-shell=\"live\"] #current-slide .slide__title {\n  margin: 0;\n  font-size: max(13px, 5cqw);\n  line-height: var(--cf-leading-tight);\n  letter-spacing: var(--cf-tracking-tight);\n  color: var(--cf-text);\n  text-wrap: balance;\n  word-break: keep-all;\n}\n\n.app[data-shell=\"live\"] #current-slide .slide__kicker,\n.app[data-shell=\"live\"] #current-slide .slide__index,\n.app[data-shell=\"live\"] #current-slide .slide__kindchip {\n  font-family: var(--cf-font-mono);\n  font-size: max(8px, 2cqw);\n  letter-spacing: var(--cf-tracking-mono);\n  color: var(--cf-text-faint);\n}\n\n.app[data-shell=\"live\"] #current-slide .slide__bullets {\n  display: grid;\n  gap: 1.4cqw;\n  margin: 0;\n  padding-inline-start: 3cqw;\n}\n\n/* A pure `cqw` scale has no floor: at 320px the slide is 296px wide, so `2.6cqw`\n   resolved to 7.6px - proportionate, but below the legibility floor. `max()`\n   keeps the proportional scale everywhere it is readable and stops it there. */\n.app[data-shell=\"live\"] #current-slide .slide__bullets li {\n  font-size: max(9px, 2.6cqw);\n  line-height: 1.45;\n  color: var(--cf-text-muted);\n  word-break: keep-all;\n}\n\n.app[data-shell=\"live\"] #current-slide .slide__emphasis {\n  margin: auto 0 0;\n  padding: 2cqw 2.4cqw;\n  border: var(--cf-rule-width) solid var(--cf-accent-line);\n  border-radius: var(--cf-radius-sm);\n  background-color: var(--cf-accent-soft);\n  color: var(--cf-accent-text);\n  font-size: max(9px, 2.4cqw);\n  line-height: 1.4;\n  word-break: keep-all;\n}\n\n.app[data-shell=\"live\"] #current-slide .slide__emphasis-label {\n  margin-inline-end: 1cqw;\n  font-family: var(--cf-font-mono);\n  font-size: max(8px, 1.8cqw);\n  letter-spacing: var(--cf-tracking-mono);\n}\n\n/* Waiting for the first generated slide: one honest line, no decorative card. */\n.app[data-shell=\"live\"] #current-slide .slide__placeholder {\n  display: grid;\n  place-content: center;\n  gap: var(--cf-space-2);\n  block-size: 100%;\n  padding: var(--cf-space-6);\n  text-align: center;\n}\n\n.app[data-shell=\"live\"] #current-slide .placeholder__mic { display: none; }\n\n.app[data-shell=\"live\"] #current-slide .placeholder__title {\n  margin: 0;\n  font-size: var(--cf-text-md);\n  font-weight: var(--cf-weight-medium);\n  color: var(--cf-text-muted);\n  word-break: keep-all;\n}\n\n.app[data-shell=\"live\"] #current-slide .placeholder__sub {\n  margin: 0;\n  font-size: var(--cf-text-sm);\n  color: var(--cf-text-faint);\n  word-break: keep-all;\n}\n\n/* History preview keeps its Return-to-live affordance on top of the slide. */\n.app[data-shell=\"live\"] #current-slide .slide__notice {\n  position: absolute;\n  inset-inline: 0;\n  inset-block-start: 0;\n  z-index: 2;\n  inline-size: 100%;\n  padding: var(--cf-space-2) var(--cf-space-3);\n  border: 0;\n  border-block-end: var(--cf-rule-width) solid var(--cf-accent-line);\n  background-color: var(--cf-accent-soft);\n  color: var(--cf-accent-text);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-xs);\n  text-align: start;\n  cursor: pointer;\n}\n\n.app[data-shell=\"live\"][data-stage-state=\"history-preview\"] #current-slide {\n  position: relative;\n  border-color: var(--cf-accent-line);\n}\n\n/* ── live: the complete transcript ───────────────────────────────────────── */\n\n.app[data-shell=\"live\"] #transcript-pane {\n  display: flex;\n  flex-direction: column;\n  padding: var(--cf-space-3) var(--live-gutter) var(--live-gutter) 0;\n  overflow: hidden;\n}\n\n.app[data-shell=\"live\"] #transcript-card {\n  display: flex;\n  flex: 1 1 auto;\n  flex-direction: column;\n  min-block-size: 0;\n  margin: 0;\n  padding: 0;\n  border: var(--cf-rule-width) solid var(--cf-rule);\n  border-radius: var(--cf-radius-md);\n  background-color: var(--cf-surface);\n  box-shadow: none;\n  overflow: hidden;\n}\n\n.app[data-shell=\"live\"] #transcript-card .pane__head {\n  display: flex;\n  flex: none;\n  align-items: baseline;\n  justify-content: space-between;\n  gap: var(--cf-space-2);\n  padding: var(--cf-space-3) var(--cf-space-4);\n  border-block-end: var(--cf-rule-width) solid var(--cf-rule);\n  background-color: transparent;\n}\n\n.app[data-shell=\"live\"] #transcript-card .pane__eyebrow { display: none; }\n\n.app[data-shell=\"live\"] #transcript-card .pane__title {\n  color: var(--cf-text);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-sm);\n  font-weight: var(--cf-weight-semibold);\n}\n\n.app[data-shell=\"live\"] #transcript-count {\n  color: var(--cf-text-faint);\n  font-family: var(--cf-font-mono);\n  font-size: var(--cf-text-xs);\n  font-variant-numeric: tabular-nums;\n}\n\n/* The transcript is the only scroll owner in its column, and it scrolls in ONE\n   dimension: long Korean sentences wrap rather than scrolling sideways. */\n.app[data-shell=\"live\"] #transcript-body {\n  flex: 1 1 auto;\n  min-block-size: 0;\n  padding: 0;\n  overflow-x: hidden;\n  overflow-y: auto;\n  overscroll-behavior: contain;\n}\n\n.app[data-shell=\"live\"] #transcript-stream {\n  display: flex;\n  flex-direction: column;\n  gap: var(--cf-space-3);\n  padding: var(--cf-space-3) var(--cf-space-4);\n  overflow-x: hidden;\n}\n\n/* Stacked, the transcript spans the whole width, which measured an 809px line at\n   899px - far past a readable measure. Capping it keeps Korean prose wrapping at\n   a sane column instead of running the full width of the screen. */\n.app[data-shell=\"live\"] .feed-line {\n  max-inline-size: 68ch;\n}\n\n.app[data-shell=\"live\"] .feed-line {\n  display: grid;\n  gap: var(--cf-space-1);\n  margin: 0;\n  padding: 0;\n  border: 0;\n  background-color: transparent;\n}\n\n/* Finalized transcript prose is primary reading matter: full opacity against\n   the opaque surface, so the composited contrast clears AA. */\n.app[data-shell=\"live\"] .feed-line__text {\n  opacity: 1;\n  color: var(--cf-text);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-sm);\n  line-height: var(--cf-leading-normal);\n  /* Korean multiline: wrap at word boundaries, never clip to one line. */\n  white-space: normal;\n  overflow-wrap: anywhere;\n  word-break: keep-all;\n}\n\n.app[data-shell=\"live\"] .feed-line__meta {\n  opacity: 1;\n  color: var(--cf-text-faint);\n  font-family: var(--cf-font-mono);\n  font-size: var(--cf-text-xs);\n  letter-spacing: var(--cf-tracking-mono);\n}\n\n.app[data-shell=\"live\"] #transcript-empty,\n.app[data-shell=\"live\"] #transcript-trunc {\n  padding: var(--cf-space-4);\n  color: var(--cf-text-faint);\n  font-size: var(--cf-text-sm);\n  text-align: center;\n}\n\n/* The provisional caption: one row, visually distinct from finalized prose,\n   pinned under the stream so it reads as \"still being said\". */\n.app[data-shell=\"live\"] #island {\n  display: flex;\n  flex: none;\n  align-items: center;\n  gap: var(--cf-space-2);\n  min-block-size: 0;\n  margin: 0;\n  padding: var(--cf-space-3) var(--cf-space-4);\n  border: 0;\n  border-block-start: var(--cf-rule-width) solid var(--cf-rule);\n  border-radius: 0;\n  background-color: var(--cf-raised);\n  box-shadow: none;\n}\n\n.app[data-shell=\"live\"] #island .island__text {\n  color: var(--cf-text-muted);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-sm);\n  font-style: italic;\n  line-height: var(--cf-leading-normal);\n  word-break: keep-all;\n}\n\n.app[data-shell=\"live\"] #island .island__sep,\n.app[data-shell=\"live\"] #island .island__meta { display: none; }\n\n.app[data-shell=\"live\"] #island .island__dot {\n  flex: none;\n  inline-size: 6px;\n  block-size: 6px;\n  border-radius: var(--cf-radius-pill);\n  background-color: var(--cf-record);\n}\n\n/* Below the seam the island belongs to the stacked transcript block, so it\n   moves inside the transcript card instead of floating over the stage. */\n.app[data-shell=\"live\"] #transcript-card #island { border-radius: 0; }\n\n/* ── live: the action dock ──────────────────────────────────────────────── */\n\n/* THE handoff fix. The superseded layer made the action row a nowrap horizontal\n   scroller; at 375px during `starting` that placed eight export controls\n   between x=367 and x=954 on a 375px viewport — real capabilities, silently\n   offscreen, with the last visible label cut mid-glyph.\n\n   The row now WRAPS onto an auto-fit grid. Nothing is hidden and nothing is\n   removed: every control keeps its ID, its action and its place in the tab\n   order; it simply flows onto the next line instead of off the screen. */\n.app[data-shell=\"live\"] .dock {\n  display: flex;\n  flex-direction: column;\n  gap: var(--cf-space-2);\n  inline-size: auto;\n  block-size: auto;\n  min-block-size: 0;\n  max-inline-size: none;\n  margin: 0;\n  padding: var(--cf-space-2) var(--live-gutter) var(--cf-space-3);\n  border: 0;\n  border-block-start: var(--cf-rule-width) solid var(--cf-rule);\n  border-radius: 0;\n  background-color: var(--cf-canvas);\n  box-shadow: none;\n  overflow: visible;\n}\n\n.app[data-shell=\"live\"] .dock__commandbar {\n  display: flex;\n  flex-wrap: wrap;\n  align-items: center;\n  justify-content: center;\n  gap: var(--cf-space-2);\n  inline-size: auto;\n  padding: 0;\n  border: 0;\n  background-color: transparent;\n  box-shadow: none;\n  backdrop-filter: none;\n  -webkit-backdrop-filter: none;\n}\n\n.app[data-shell=\"live\"] .dock__tabs {\n  /* auto-fit grid: as many complete labels per row as fit, then wrap. */\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(96px, 1fr));\n  gap: var(--cf-space-2);\n  min-block-size: 0;\n  margin: 0;\n  padding: 0;\n  border: 0;\n  border-radius: 0;\n  background: none;\n  background-color: transparent;\n  box-shadow: none;\n  backdrop-filter: none;\n  -webkit-backdrop-filter: none;\n  /* No scroller, therefore no clipped label behind an overflow fade. */\n  flex-wrap: wrap;\n  overflow: visible;\n  clip-path: none;\n  contain: none;\n}\n\n/* The superseded layers append a sticky \"→\" scroll hint. The row wraps now, so\n   that arrow would advertise an interaction that no longer exists. */\n.app[data-shell=\"live\"] .dock__tabs::after {\n  content: none;\n  display: none;\n}\n\n.app[data-shell=\"live\"] .dock__btn {\n  display: inline-flex;\n  flex: initial;\n  align-items: center;\n  justify-content: center;\n  /* 40px keeps the pointer target usable at narrow widths (DESIGN 9.12). */\n  min-block-size: 40px;\n  margin: 0;\n  padding: var(--cf-space-1) var(--cf-space-3);\n  border: var(--cf-rule-width) solid var(--cf-rule);\n  border-radius: var(--cf-radius-sm);\n  background-color: transparent;\n  color: var(--cf-text-muted);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-sm);\n  text-align: center;\n  white-space: nowrap;\n  cursor: pointer;\n  transition-property: background-color, border-color, color;\n  transition-duration: var(--cf-motion-quick);\n  transition-timing-function: var(--cf-motion-ease);\n}\n\n.app[data-shell=\"live\"] .dock__btn:hover:not(:disabled) {\n  background-color: var(--cf-surface);\n  color: var(--cf-text);\n}\n\n.app[data-shell=\"live\"] .dock__btn:focus-visible {\n  outline: var(--cf-focus-width) solid var(--cf-focus);\n  outline-offset: var(--cf-focus-offset);\n}\n\n.app[data-shell=\"live\"] .dock__btn:disabled {\n  color: var(--cf-text-faint);\n  cursor: not-allowed;\n}\n\n.app[data-shell=\"live\"] .dock__btn--danger { color: var(--cf-record); }\n\n/* Compile names a whole workflow, so it spans the row and is never abbreviated. */\n.app[data-shell=\"live\"] #btn-compile-deck {\n  grid-column: 1 / -1;\n  margin: 0;\n  border-color: var(--cf-accent-line);\n  color: var(--cf-accent-text);\n}\n\n/* The save/export disclosure. In live it is CLOSED by default, so the dock stays\n   one or two rows tall and the stage and transcript keep their height. Opening it\n   reveals the same wrapping grid, so nothing is ever clipped either way. */\n.app[data-shell=\"live\"] .dock__more {\n  grid-column: 1 / -1;\n  min-inline-size: 0;\n}\n\n.app[data-shell=\"live\"] .dock__more-summary {\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  min-block-size: 40px;\n  padding: var(--cf-space-1) var(--cf-space-3);\n  border: var(--cf-rule-width) solid var(--cf-rule);\n  border-radius: var(--cf-radius-sm);\n  background-color: transparent;\n  color: var(--cf-text-muted);\n  font-family: var(--cf-font-body);\n  font-size: var(--cf-text-sm);\n  list-style: none;\n  cursor: pointer;\n  transition-property: background-color, border-color, color;\n  transition-duration: var(--cf-motion-quick);\n  transition-timing-function: var(--cf-motion-ease);\n}\n\n.app[data-shell=\"live\"] .dock__more-summary::-webkit-details-marker { display: none; }\n\n.app[data-shell=\"live\"] .dock__more-summary::after {\n  content: \"\";\n  margin-inline-start: var(--cf-space-2);\n  inline-size: 6px;\n  block-size: 6px;\n  border-inline-end: var(--cf-rule-width) solid currentColor;\n  border-block-end: var(--cf-rule-width) solid currentColor;\n  transform: rotate(45deg) translate(-2px, -2px);\n}\n\n.app[data-shell=\"live\"] .dock__more[open] .dock__more-summary::after {\n  transform: rotate(-135deg) translate(-2px, -2px);\n}\n\n.app[data-shell=\"live\"] .dock__more-summary:hover {\n  background-color: var(--cf-surface);\n  color: var(--cf-text);\n}\n\n.app[data-shell=\"live\"] .dock__more-summary:focus-visible {\n  outline: var(--cf-focus-width) solid var(--cf-focus);\n  outline-offset: var(--cf-focus-offset);\n}\n\n.app[data-shell=\"live\"] .dock__more-set {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(96px, 1fr));\n  gap: var(--cf-space-2);\n  margin-block-start: var(--cf-space-2);\n}\n\n.app[data-shell=\"live\"] #compile-status,\n.app[data-shell=\"live\"] #last-saved {\n  grid-column: 1 / -1;\n  color: var(--cf-text-faint);\n  font-family: var(--cf-font-mono);\n  font-size: var(--cf-text-xs);\n  text-align: center;\n}\n\n/* ONE Stop, ONE timer (DESIGN 9.8).\n\n   `#live-topbar` inside `#stage-pane` owns the live persistent controls:\n   `#btn-live-stop` and `#live-topbar-timer`. The dock's capture pill carries the\n   OTHER Stop (`#btn-record`, whose label flips to \"녹음 중지\") and the OTHER clock\n   (`#capture-timer`). Painting both gave the operator two competing Stop\n   affordances and two clocks that can disagree - measured in every live phase at\n   1244, 375 and 320.\n\n   The pill is removed from the LIVE layout only. Every binding ID survives in the\n   DOM with its handler intact, `app.js` keeps driving `#capture-timer` as the\n   timer source that `#live-topbar-timer` mirrors, and the library shell restores\n   the pill as its capture-start control. Nothing is renamed and nothing is\n   deleted. */\n.app[data-shell=\"live\"] .capture-pill {\n  display: none;\n}\n\n.app[data-shell=\"live\"] .output-switcher {\n  display: flex;\n  flex-wrap: wrap;\n  gap: var(--cf-space-1);\n  inline-size: auto;\n  overflow: visible;\n}\n\n.app[data-shell=\"live\"] .output-switcher__item {\n  flex: initial;\n  min-block-size: 32px;\n  padding: var(--cf-space-1) var(--cf-space-3);\n  border-radius: var(--cf-radius-sm);\n  color: var(--cf-text-faint);\n  font-size: var(--cf-text-xs);\n  white-space: nowrap;\n}\n\n/* ── live: narrow seams ─────────────────────────────────────────────────── */\n\n/* Below the seam the two regions share the column. The stage takes the height\n   its complete 16:9 slide needs and no more (`auto`), and the transcript takes\n   everything that is left — which is what turns the baseline's dead space below\n   a 34px slide into a readable transcript. */\n@media (max-width: 899px) {\n  .app[data-shell=\"live\"] #document-surface {\n    /* `auto` for the stage, `1fr` for the transcript: the stage takes exactly\n       what its complete 16:9 slide needs and the transcript takes the rest.\n       Two `1fr` tracks would split the column evenly and leave the stage with\n       dead space above a short slide. */\n    grid-template-rows: auto minmax(0, 1fr);\n  }\n\n  .app[data-shell=\"live\"] #stage-pane {\n    grid-row: 1;\n    /* Content-sized: exactly the topbar plus the 16:9 slide. `flex: 1` would let\n       the pane grow into the `auto` track and starve the transcript below it. */\n    flex: none;\n    min-block-size: 0;\n    padding-block-end: var(--cf-space-3);\n  }\n\n  /* Below the seam the provisional caption belongs to the transcript block, not\n     to the stage: it is what is being said, next to what has been said. It is\n     still the same #island node in the same DOM position, only re-ordered. */\n  .app[data-shell=\"live\"] #island {\n    border-block-start: 0;\n    border-block-end: var(--cf-rule-width) solid var(--cf-rule);\n  }\n\n  .app[data-shell=\"live\"] #slide-frame {\n    /* Below the seam the stage is content-sized, so the frame states its own\n       definite height: `min()` of the 16:9 height its width implies and a share\n       of the viewport. The 34vh cap is what stops a tall narrow screen from\n       handing everything to the stage and starving the transcript underneath -\n       at 899x760 a 42vh cap left the transcript body 7px tall. */\n    flex: none;\n    width: 100%;\n    height: min(calc((100vw - 2 * var(--live-gutter)) * 9 / 16), 34vh);\n  }\n\n  /* The transcript is the other half of the live contract, so it states a floor\n     rather than taking whatever the stage leaves over. */\n  .app[data-shell=\"live\"] #transcript-card {\n    min-block-size: 0;\n  }\n\n  .app[data-shell=\"live\"] #transcript-body {\n    min-block-size: 120px;\n  }\n\n  .app[data-shell=\"live\"] #transcript-pane {\n    grid-row: 2;\n    padding: 0 var(--live-gutter) var(--cf-space-3);\n  }\n}\n\n@media (max-width: 599px) {\n  .app[data-shell=\"live\"] {\n    --live-gutter: var(--live-gutter-compact);\n  }\n\n  .app[data-shell=\"live\"] #slide-frame {\n    height: min(calc((100vw - 2 * var(--live-gutter)) * 9 / 16), 30vh);\n  }\n\n  /* The transcript header is chrome; at 320px it gives its height to the prose. */\n  .app[data-shell=\"live\"] #transcript-card .pane__head {\n    padding: var(--cf-space-2) var(--cf-space-3);\n  }\n\n  /* One complete label per row at the narrowest widths: 96px columns would\n     ellipsize the longest export labels, which DESIGN 9.9 forbids at 375/320. */\n  .app[data-shell=\"live\"] .dock__tabs {\n    grid-template-columns: repeat(auto-fit, minmax(88px, 1fr));\n  }\n\n  .app[data-shell=\"live\"] .dock__btn {\n    padding-inline: var(--cf-space-2);\n    font-size: var(--cf-text-xs);\n  }\n}\n\n/* ── responsive seams ─────────────────────────────────────────────────────── */\n\n/* Below 1024px the rail narrows before the document measure gives ground. */\n@media (max-width: 1023px) {\n  .app[data-shell=\"library\"] .workspace {\n    grid-template-columns: var(--rail-w, var(--lib-rail-w-compact)) var(--splitter-w, 5px) minmax(0, 1fr);\n  }\n\n  .app[data-shell=\"library\"] #meeting-chrome,\n  .app[data-shell=\"library\"] .detail-panel:not([hidden]),\n  .app[data-shell=\"library\"] .dock__commandbar,\n  .app[data-shell=\"library\"] .dock__tabs {\n    --lib-gutter: var(--lib-gutter-compact);\n  }\n}\n\n/* Below 720px the rail stops being a column and becomes an ordered strip above\n   the document. It stays first in DOM order, so keyboard order is unchanged. */\n@media (max-width: 719px) {\n  .app[data-shell=\"library\"] .workspace {\n    grid-template-columns: minmax(0, 1fr);\n    grid-template-rows: auto minmax(0, 1fr);\n  }\n\n  /* Single column: the explicit column assignments above are replaced by\n     explicit ROW assignments, so the stacked order stays deterministic. */\n  .app[data-shell=\"library\"] #session-rail { grid-column: 1; grid-row: 1; }\n  .app[data-shell=\"library\"] #document-surface { grid-column: 1; grid-row: 2; }\n\n  /* No second adjacent pane to resize once the rail becomes a strip, so the\n     splitter is inert and out of the tab order (DESIGN 9.9). */\n  .app[data-shell=\"library\"] #splitter-rail { display: none; }\n\n  .app[data-shell=\"library\"] #session-rail {\n    max-height: 34vh;\n    border-right: 0;\n    border-bottom: var(--cf-rule-width) solid var(--cf-rule);\n  }\n\n  .app[data-shell=\"library\"] #meeting-chrome {\n    padding-top: var(--cf-space-4);\n  }\n\n  .app[data-shell=\"library\"] #meeting-chrome-title {\n    font-size: var(--cf-text-xl);\n  }\n\n  .app[data-shell=\"library\"] #notes-input {\n    min-height: 220px;\n    font-size: var(--cf-text-md);\n  }\n\n  .app[data-shell=\"library\"] .feed-line__text {\n    font-size: var(--cf-text-md);\n  }\n\n  /* Secondary actions wrap onto a grid instead of scrolling. A scrolling row\n     leaves the last visible label clipped mid-glyph, which DESIGN 9.9 forbids at\n     375px and 320px. Wrapping keeps every label complete and tappable. */\n  .app[data-shell=\"library\"] .dock__tabs {\n    display: grid;\n    grid-template-columns: repeat(auto-fit, minmax(96px, 1fr));\n    gap: var(--cf-space-2);\n    flex-wrap: wrap;\n    overflow: visible;\n  }\n\n  .app[data-shell=\"library\"] .dock__btn {\n    justify-content: center;\n    text-align: center;\n    /* A label that still cannot fit shortens with an ellipsis, never a hard cut. */\n    overflow: hidden;\n    text-overflow: ellipsis;\n    white-space: nowrap;\n  }\n\n  /* The longest label is the compile action. It spans the row so the one command\n     that names a whole workflow is never abbreviated to an ellipsis. */\n  .app[data-shell=\"library\"] #btn-compile-deck { grid-column: 1 / -1; }\n}\n\n/* ── narrow: real touch targets ───────────────────────────────────────────────\n\n   DESIGN 9.12: \"Touch and pointer targets stay at least 44x44 CSS px at narrow\n   widths.\"\n\n   This rule covers EVERY painted interactive control the shell owns at <=375px,\n   not only the dock. An earlier revision claimed to cover \"every action in the\n   contextual surface\" while selecting `.dock__btn` alone, which left the four\n   persistent controls a narrow user reaches for most - live Stop, settings,\n   attendees and the library capture button - at 26-42px. They are enumerated\n   explicitly below.\n\n   Both axes are constrained here, because these controls are short-labelled or\n   icon-like and genuinely need the inline floor. `.output-switcher__item` is the\n   documented exception: an inline minimum does not grow a too-small target, it\n   inflates an already-wide one, and forcing it there widened those items until\n   they overlapped the neighbouring Ask button. */\n@media (max-width: 375px) {\n  /* Shell-qualified so this wins the `[data-shell=...] .dock__btn` rules above,\n     which set the comfortable 32/40px desktop height. */\n  .app[data-shell=\"library\"] .dock__btn,\n  .app[data-shell=\"live\"] .dock__btn,\n  .app[data-shell=\"library\"] .dock__more-summary,\n  .app[data-shell=\"live\"] .dock__more-summary,\n  .app[data-shell=\"library\"] .output-switcher__item,\n  .app[data-shell=\"live\"] .output-switcher__item {\n    min-block-size: 44px;\n  }\n\n  /* The persistent controls. `min-inline-size` is safe here: each sits in a flex\n     or grid line that lets it grow without displacing a neighbour, which the\n     edge-band hit test in tests/public-caret-context.test.ts verifies.\n\n     `#btn-live-stop` is shell-qualified to outrank\n     `.app[data-shell=\"live\"] #btn-live-stop` above, which pins the comfortable\n     28px pill. Specificity, not source order, decides this one. */\n  .app[data-shell=\"live\"] #btn-live-stop,\n  .app #btn-settings,\n  .app #btn-attendees,\n  .app[data-shell=\"library\"] #btn-record {\n    min-block-size: 44px;\n    min-inline-size: 44px;\n  }\n\n  /* The live capsule wraps Stop and the timer. It must grow with the 44px Stop\n     rather than clipping it back to the old 28px capsule height. */\n  .app[data-shell=\"live\"] .live-topbar__capsule {\n    min-block-size: 44px;\n    align-items: center;\n  }\n\n  /* The document's own interactive controls: the detail tabs that switch the one\n     document surface, and the follow-up actions on the Overview card. Both are\n     real controls a narrow-width user taps, so both carry the same floor. */\n  .app .detail-tabs__btn,\n  .app .action-card__btn {\n    min-block-size: 44px;\n    min-inline-size: 44px;\n  }\n\n  /* At 320x667 the dock's 40vh share plus the taller 44px controls left the\n     document surface too short for its own content, so the workspace overflowed\n     its flex track and the dock came to cover the bottom 15px of the detail\n     tabs - measured: tab bottom 423.5, dock top 409. Giving the workspace a real\n     minimum and letting the dock yield keeps every control's own box hittable. */\n  .app[data-shell=\"library\"] .workspace {\n    min-block-size: 0;\n    flex: 1 1 auto;\n  }\n\n  .app[data-shell=\"library\"] .dock {\n    flex: 0 1 auto;\n    max-block-size: 34vh;\n  }\n\n  /* The topbar was sized for 26-28px chrome. Once settings and attendees became\n     real 44px targets they extended past its 48px box, and because the topbar\n     clips (`overflow: hidden`) the rail header below owned their bottom edge.\n     The bar sizes to its controls instead of clipping them. */\n  .app .topbar {\n    block-size: auto;\n    min-block-size: 56px;\n    align-items: center;\n    overflow: visible;\n  }\n\n  /* Raising every control to 44px makes the command row taller than the single\n     non-wrapping flex line it used to fit in, so it overflowed its own container\n     and the output switcher came to overlap the first action row - `#btn-ask`\n     was painted but its top, left and right edges answered to the switcher.\n     Letting the row wrap keeps each control's own box hittable. */\n  .app[data-shell=\"library\"] .dock__commandbar,\n  .app[data-shell=\"live\"] .dock__commandbar {\n    flex-wrap: wrap;\n    row-gap: var(--cf-space-2);\n    /* The superseded narrow rules make the switcher a full-width flex line with\n       `order: 2`, so it wraps below the capture control. The legacy fixed height\n       sized the bar for ONE line, and the wrapped switcher escaped its own\n       container to overlap the first action row - `#btn-ask` was painted\n       underneath it at 320px.\n\n       `height`/`min-height` are written PHYSICALLY here on purpose. The\n       superseded layer declares `min-height: 66px`, and a logical\n       `min-block-size` in this rule does not override a physical `min-height`\n       from another sheet at equal specificity - the later physical declaration\n       wins. Matching the property the legacy layer actually used is what makes\n       this take effect.\n\n       `height: fit-content` (not `auto`) is required because the bar is itself a\n       flex item of the dock column: `auto` still lets the dock shrink it below\n       its wrapped content, which is how it collapsed to 44px while holding two\n       44px lines. `flex-shrink: 0` keeps that guarantee under pressure. */\n    height: fit-content;\n    min-height: fit-content;\n    flex-shrink: 0;\n  }\n\n  /* The switcher is a scroll owner by default; at these widths it must size to\n     its content instead of clipping a 44px control to its old height. */\n  .app[data-shell=\"library\"] .output-switcher,\n  .app[data-shell=\"live\"] .output-switcher {\n    flex-wrap: wrap;\n    row-gap: var(--cf-space-2);\n    overflow: visible;\n    block-size: auto;\n  }\n}\n\n/* ── narrow-width failure legibility ──────────────────────────────────────── */\n\n/* DESIGN 9.13: reconnecting, capture error, export error and provider failure\n   are all delivered by `#status-text`. The superseded narrow rule\n   (`style.css`: `.topbar__status > :not(#btn-settings) { display: none }`\n   below 420px) hid that sentence, so at 375 and 320 a dropped connection left\n   NO visible or announced trace - only the `data-connection` attribute, which\n   is exactly \"failure detail living only in a log\" at the widths where the\n   operator can least afford it.\n\n   The sentence is restored and bounded instead of being allowed to push the\n   settings control off the bar: it takes the space it can get and ellipsises\n   the rest, while `title` (set by `renderStatus`) keeps the full text\n   available. The status region's role/announcement are unchanged, so the\n   reason is both readable and spoken once. */\n@media (max-width: 420px) {\n  .app .topbar__status > #status-text {\n    display: block;\n    min-inline-size: 0;\n    overflow: hidden;\n    text-overflow: ellipsis;\n    white-space: nowrap;\n    font-size: var(--cf-text-xs);\n    color: var(--cf-text-muted);\n  }\n\n  /* `flex: 1` here would let the status bar CLAIM space and paint over\n     `#btn-attendees` in the topbar's centre slot. The status only needs\n     permission to shrink; `flex-shrink` plus a zero minimum gives it that\n     without letting it grow into a neighbour. */\n  .app .topbar__status {\n    min-inline-size: 0;\n    flex: 0 1 auto;\n  }\n\n  /* The superseded topbar grid hands the centre slot a fixed share that is\n     narrower than `#btn-attendees` itself (39.9px for a 60px control at 375),\n     so the button overflowed its own track and collided with the status column\n     beside it. Sizing the outer tracks to their content and giving the status\n     column the remainder keeps all three in their own lanes at every narrow\n     width, which is what makes the restored status sentence safe. */\n  .app[data-shell=\"library\"] .topbar,\n  .app[data-shell=\"live\"] .topbar {\n    grid-template-columns: auto auto minmax(0, 1fr);\n    column-gap: var(--cf-space-2);\n  }\n\n  .app .topbar__center,\n  .app .topbar__right {\n    min-inline-size: 0;\n  }\n\n  /* `justify-self: end` (style.css) lets this group size to its CONTENT and\n     then hang leftwards out of its own track, which is how a 187px status\n     group starting at x=167 came to sit on top of a centre-track button ending\n     at x=179. Stretching it makes the track the boundary, so the status text\n     inside it shrinks instead of escaping. */\n  .app .topbar__right {\n    justify-self: stretch;\n    justify-content: flex-end;\n    inline-size: 100%;\n  }\n}\n\n/* ── reduced motion ───────────────────────────────────────────────────────── */\n\n/* Reduced motion is handled once, for BOTH shells, in caret-shell.css. The rule\n   that lived here was scoped to `[data-shell=\"library\"]`, so entering the live\n   shell silently restored every transition and animation the contract requires\n   to stop (DESIGN 9.6). A single unscoped rule cannot regress that way. */\n"

      at <anonymous> (/Users/hyunjun/Documents/MUNI/meeting-slides/tests/public-active-shell.test.ts:61:22)
(fail) Todo 18 active shell convergence > the operator source owns tokens, geometry, narrow failures, and global reduced motion [0.40ms]
(pass) Todo 18 active shell convergence > the document has one app, topbar, live header, dock, and status owner [0.10ms]
79 | 
80 | describe("Todo 18 documentation and bundle truth", () => {
81 |   test("README documents only current run, build, verify, and usage surfaces", () => {
82 |     const readme = read("README.md");
83 |     expect(readme).toContain("bash scripts/build-app.sh");
84 |     expect(readme).toContain("bash scripts/verify-app.sh");
                        ^
error: expect(received).toContain(expected)

Expected to contain: "bash scripts/verify-app.sh"
Received: "# Meeting Slides\n\n> 실시간 회의 슬라이드 생성기 — 마이크 음성을 whisper.cpp로 전사하고, LLM이 주제 블록을 감지해 브라우저에 슬라이드를 한 장씩 밀어줍니다.\n>\n> Real-time meeting slides: Korean speech → whisper.cpp STT → LLM topic-block detection → live slides pushed to your browser over WebSocket.\n\n![stack](https://img.shields.io/badge/runtime-Bun-black) ![stt](https://img.shields.io/badge/STT-whisper.cpp-green) ![ui](https://img.shields.io/badge/UI-vanilla%20JS-blue)\n\n## 동작 방식\n\n```\n마이크 (또는 오디오 파일)\n   │  whisper-stream / whisper-cli (whisper.cpp, 한국어)\n   ▼\n전사 문장 스트림 ── src/whisper.ts ── ANSI/메타/중복 필터링\n   │\n   ▼\nMeetingSession (src/session.ts)\n   ├─ 실시간 자막 (200ms 디바운스)\n   └─ N문장마다 LLM 블록 감지 (src/llm.ts, OpenAI-compatible)\n        ├─ 주제 전환 + hysteresis(연속 2회) → 새 슬라이드 push\n        └─ LLM 장애 시 로컬 규칙 fallback 요약\n   ▼\nWebSocket /ws ── public/app.js ── 슬라이드·자막·히스토리 렌더\n```\n\n- **슬라이드 히스테리시스**: LLM이 \"주제 바뀜\"을 연속 2회 말해야 실제로 장표를 넘겨서, 오판 스팸을 막습니다.\n- **LLM 장애 내성**: API가 죽어도 로컬 규칙 기반 요약으로 계속 동작합니다.\n- **로컬 전용 설계**: 서버는 localhost만 바인드하고, WebSocket 업그레이드 시 Origin을 검사해 다른 웹페이지가 전사 내용을 엿보지 못하게 막습니다(CSWSH 방어).\n\n## 요구 사항\n\n- [Bun](https://bun.sh) ≥ 1.x\n- [whisper.cpp](https://github.com/ggml-org/whisper.cpp) 바이너리 2개:\n  ```bash\n  brew install whisper.cpp   # whisper-stream, whisper-cli 제공\n  ```\n- ggml 모델 파일 (**large-v3-turbo 권장** — large급 정확도 + 실시간 속도):\n  ```bash\n  mkdir -p models\n  curl -L -o models/ggml-large-v3-turbo.bin \\\n    https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-large-v3-turbo.bin\n  ```\n- LLM 프로바이더 1개 (택일):\n  - **구독 서비스 CLI** — API 키 없이 구독 인증 재사용: `claude`(Claude Pro/Max) 또는 `codex`(ChatGPT)\n  - **OpenAI** — gpt-4o-mini 등\n  - **로컬 llama.cpp 서버** — API 키 없이 완전 오프라인 가능\n\n## 설치 및 설정\n\n```bash\ngit clone https://github.com/dltdnfrk/meeting-slides.git\ncd meeting-slides\nbun install\ncp .env.example .env   # 열어서 LLM 키 등을 채워 넣기\n```\n\n`.env` 핵심 항목:\n\n| 변수 | 설명 | 기본값 |\n|---|---|---|\n| `LLM_PROVIDER` | `openai` \\| `local` \\| `cli` | `cli` |\n| `OPENAI_API_KEY` | OpenAI API 키 | — |\n| `LLM_CLI_BIN` | `cli` 모드 백엔드 CLI (`claude`/`codex`, 구독 인증) | `claude` |\n| `LLM_CLI_TIMEOUT_MS` | `cli` 모드 호출 상한 (ms) | `120000` |\n| `WHISPER_MODEL_PATH` | ggml 모델 경로 | `./models/ggml-medium.bin` |\n| `WHISPER_CAPTURE_ID` | 캡처 장치 ID (`-1`=기본 마이크) | `-1` |\n| `WHISPER_STEP_MS` | 오디오 스텝(ms). 작을수록 실시간성↑ 정확도↓ | `3000` |\n| `BLOCK_DETECT_SENTENCE_INTERVAL` | LLM 호출 간격(문장 수) | `4` |\n| `BLOCK_CONTEXT_WINDOW` | LLM에 보낼 최근 문장 수 | `12` |\n| `HTTP_PORT` | 웹 UI 포트 | `8787` |\n\n> **cli 모드 팁**: CLI는 호출마다 기동 비용(수 초)이 들므로 `BLOCK_DETECT_SENTENCE_INTERVAL=8` 정도로 올리는 걸 권장합니다.\n\n## 실행\n\n### macOS 앱으로 실행 (권장 — 터미널 불필요)\n\n```bash\nbun install\nbash scripts/build-app.sh      # Meeting Slides.app 생성 (1회)\n```\n\n1. Finder에서 `Meeting Slides.app` **우클릭 → 열기** (최초 1회, Gatekeeper)\n2. 마이크 권한 프롬프트 **허용** (앱 단위로 영구 귀속 — 이후 어떤 실행 경로로든 녹음 동작)\n3. 이후 **더블클릭/Spotlight**로 실행 → 서버 기동 + 브라우저 자동 오픈\n\n```bash\nopen -a \"Meeting Slides.app\" --args --mic-check   # 권한 프롬프트 미리 띄우기\nbash scripts/install-login-item.sh                # 로그인 시 자동 시작 (선택)\nbash scripts/install-login-item.sh --remove       # 자동 시작 해제\n```\n\n### 터미널에서 실행\n\n```bash\n# 마이크 실시간 모드 (브라우저 자동 오픈)\nbun run dev\n\n# 캡처 장치 목록 확인 (기본 마이크가 아닐 때)\nbun run devices\n\n# 오디오 파일로 데모/테스트 (마이크 불필요)\nbun run server.ts --file ./sample.m4a\n```\n\n브라우저에서 `http://localhost:8787` 접속 후 발언을 시작하면:\n\n- 하단 **아일랜드**에 실시간 자막이 흐르고\n- 상단 **⚙ 버튼**에서 LLM 프로바이더를 언제든 교체 (구독 Claude / 구독 GPT / OpenAI / 로컬 — 사용 가능 여부 자동 감지, \"사용 중\" 배지)\n- 주제가 잡히면 **슬라이드**가 만들어지고, 주제가 바뀌면 새 장으로 넘어갑니다\n- 왼쪽 **히스토리** 썸네일을 클릭하면 지난 슬라이드 미리보기 (재클릭·Esc·안내 바 클릭 시 라이브 복귀)\n- **Markdown / JSON 저장**은 요약 슬라이드, **전사본**은 시각·화자 라벨이 붙은 원문 전체를 다운로드\n- **초기화**로 세션 리셋\n\n> 문장 분할 품질: 한국어 STT는 구두점이 자주 빠지므로, 미완결 조각을 보류해 다음 조각과 병합하는 어셈블러가 전사 파이프라인에 내장돼 있습니다(완결 구두점·화자 전환·60자 상한·종료 시 방출).\n\n## 화자 분리 (tinydiarize, 실험적)\n\n발화마다 화자(턴) 번호를 감지해 캡션에 색상 칩으로 표시합니다.\n\n```bash\n# 1. tdrz 모델 다운로드 (465MB)\ncurl -L -o models/ggml-small.en-tdrz.bin \\\n  https://huggingface.co/akashmjn/tinydiarize-whisper.cpp/resolve/main/ggml-small.en-tdrz.bin\n\n# 2. .env에 추가\nWHISPER_DIARIZE=true\n```\n\n**제약 (중요)**:\n- tdrz 모델이 **영어 전용**이라 한국어 회의에서는 전사가 깨집니다. 한국어는 `WHISPER_DIARIZE=false`(기본값)로 두세요. 다국어 tdrz 모델이 나오면 바로 쓸 수 있게 코드는 준비돼 있습니다.\n- tinydiar는 화자 \"**전환 감지**\"이지 \"식별\"이 아닙니다. 번호가 실제 인물과 1:1로 고정되지 않고 드리프트할 수 있어요(2인 회의처럼 교대 패턴이면 잘 맞습니다).\n\n## 프로젝트 구조\n\n```\nserver.ts        HTTP + WebSocket + 세션 오케스트레이션 (진입점)\nsrc/config.ts    환경 설정 로더 (엄격한 검증)\nsrc/whisper.ts   whisper-stream/cli 자식 프로세스 + stdout 파서\nsrc/llm.ts       OpenAI-compatible 클라이언트 + 블록 감지\nsrc/session.ts   회의 상태 머신 (자막 디바운스, 슬라이드 hysteresis)\npublic/          바닐라 JS 클라이언트 (슬라이드 스테이지 + 필름스트립)\n```\n\n## 보안 메모\n\n- LLM API 키는 `.env`에만 두고 커밋하지 않습니다 (`.gitignore`에 포함).\n- WebSocket `/ws`는 `Origin` 헤더가 서버 자신과 다르면 403으로 거부합니다.\n- 정적 파일 서빙은 `public/` 디렉터리 밖으로 나갈 수 없습니다.\n\n## License\n\nMIT\n"

      at <anonymous> (/Users/hyunjun/Documents/MUNI/meeting-slides/tests/public-active-shell.test.ts:84:20)
(fail) Todo 18 documentation and bundle truth > README documents only current run, build, verify, and usage surfaces [0.11ms]
88 |     expect(readme).not.toMatch(/private asset|private window|screen.?share.*(hide|hidden|exclude)|화면 공유.*(숨|제외)/i);
89 |   });
90 | 
91 |   test("DESIGN records the measured privacy result without an exclusion claim", () => {
92 |     const design = read("DESIGN.md");
93 |     expect(design).toContain("single-window");
                        ^
error: expect(received).toContain(expected)

Expected to contain: "single-window"
Received: "# Meeting Slides Design System\n\n## 1. Atmosphere & Identity\n\nMeeting Slides should feel like a well-edited working paper prepared immediately after a real conversation: calm, legible, and specific. The signature is an asymmetric paper composition in which clear meeting text occupies the left field while a restrained abstract “conversation map” sits to the right. It must never resemble a lecture template, terminal UI, neon dashboard, or generic AI gradient.\n\n## 2. Color\n\n### Palette\n\n| Role | Token | Value | Usage |\n| --- | --- | --- | --- |\n| Paper / primary | `--deck-paper` | `#F6F1E8` | Main slide background |\n| Paper / raised | `--deck-paper-raised` | `#FFFDF8` | Text field and subtle panels |\n| Ink / primary | `--deck-ink` | `#14213D` | Titles and body |\n| Ink / secondary | `--deck-ink-muted` | `#5B6475` | Metadata and supporting copy |\n| Rule | `--deck-rule` | `#D9D2C4` | Dividers and image boundaries |\n| Accent / primary | `--deck-coral` | `#AD4B2F` | Section index, bullet marks, focus |\n| Accent / secondary | `--deck-blue` | `#335C81` | Diagram linework and quiet emphasis |\n| Focus | `--deck-focus` | `#1E5AA8` | Keyboard focus ring |\n\n### Rules\n\n- Paper and ink dominate. Coral marks sequence; blue supports structure.\n- No black backgrounds, fluorescent green, purple glow, or multicolor category palette.\n- New colors require a semantic role here before use.\n\n## 3. Typography\n\n### Scale\n\n| Level | Size | Weight | Line Height | Tracking | Usage |\n| --- | --- | --- | --- | --- | --- |\n| Display | `64px` | 760 | 1.12 | `-0.035em` | Cover title |\n| H1 | `48px` | 740 | 1.2 | `-0.025em` | Topic title |\n| H2 | `36px` | 700 | 1.2 | `-0.02em` | Dense topic title |\n| Body/lg | `27px` | 480 | 1.55 | `-0.005em` | Standard bullets |\n| Body | `20px` | 450 | 1.5 | `0` | Metadata |\n| Caption | `16px` | 620 | 1.4 | `0.08em` | Section label |\n\n### Deck font stack\n\n- Primary: `\"Pretendard Variable\", Pretendard, -apple-system, BlinkMacSystemFont, \"Apple SD Gothic Neo\", \"Noto Sans KR\", sans-serif`\n- Serif accent: `\"Iowan Old Style\", \"Noto Serif KR\", Georgia, serif`\n\n### Rules\n\n- Korean text uses `word-break: keep-all` and balanced headings.\n- Body text never drops below `18px` on the 1280×720 design surface.\n- No monospace CLI-stamp eyebrow or ornamental italic spotlight word.\n\n## 4. Spacing & Layout\n\n### Base Unit\n\nAll spacing derives from 4px.\n\n| Token | Value | Usage |\n| --- | --- | --- |\n| `--space-2` | `8px` | Tight inline gap |\n| `--space-3` | `12px` | Label to title |\n| `--space-4` | `16px` | Bullet internal spacing |\n| `--space-6` | `24px` | Text stack spacing |\n| `--space-8` | `32px` | Component separation |\n| `--space-12` | `48px` | Column gap |\n| `--space-16` | `64px` | Slide edge inset |\n| `--space-20` | `80px` | Large optical inset |\n\n### Grid\n\n- Design surface: 1280×720.\n- Standalone slides render the design surface at 0.75 into slides-grab’s 960×540 viewport.\n- Cover: left text field 58%, right visual field 42%.\n- Topic: text 60%, visual 40%; dense topics reduce the visual field.\n- Reveal.js may scale the fixed design surface to the browser viewport.\n\n### Rules\n\n- Text stays left-aligned; the right visual is supportive and decorative.\n- The cover and standalone page must always fill the full design height.\n- Long titles reduce type size before narrowing the text column.\n\n## 5. Components\n\n### Meeting cover\n\n- **Structure**: `section.meeting-cover > svg.cover-visual + div.cover-copy + p.cover-meta + aside.notes`\n- **Variants**: default, long title\n- **Spacing**: `--space-16`, `--space-20`\n- **States**: static presentation surface\n- **Accessibility**: decorative SVG uses `aria-hidden` and contains no baked-in meeting text\n- **Motion**: none\n- **Layout**: two-field asymmetric cover with a thin coral registration line\n- **Variation**: the conversation map is generated from the meeting title; no fixed raster is reused across meetings\n\n### Meeting topic\n\n- **Structure**: `section.meeting-topic > div.topic-copy + svg.topic-map + aside.notes`\n- **Variants**: default, dense\n- **Spacing**: `--space-6`, `--space-12`, `--space-16`\n- **States**: static presentation surface\n- **Accessibility**: decorative SVG remains hidden from assistive technology; semantic title and list carry all content\n- **Motion**: none\n- **Layout**: editorial two-column grid\n- **Variation**: node count and geometry derive from the topic title and bullets so separate topics never share a fixed visual\n\n### Meeting closing\n\n- **Structure**: `section.meeting-closing > p.closing-label + h2 + ul + aside.notes`\n- **Variants**: default\n- **Spacing**: `--space-6`, `--space-16`\n- **States**: static presentation surface\n- **Accessibility**: semantic heading and list\n- **Motion**: none\n- **Layout**: typography-first summary with a blue rule\n\n## 6. Motion & Interaction\n\n| Type | Duration | Easing | Usage |\n| --- | --- | --- | --- |\n| Reveal transition | `220ms` | `ease-out` | Slide navigation only |\n| Focus | `120ms` | `ease-out` | Reveal controls |\n\nOnly `transform` and `opacity` may animate. `prefers-reduced-motion: reduce` disables non-essential transitions.\n\n## 7. Depth & Surface\n\nStrategy: tonal shift.\n\n- Depth comes from paper tones, rules, and image opacity.\n- No glow, glass card, neon shadow, or generic floating card treatment.\n- Texture belongs to the paper surface and generated linework, not as a noisy page overlay.\n\n## 8. Accessibility Constraints & Accepted Debt\n\n### Constraints\n\n- Target WCAG 2.2 AA: body contrast at least 4.5:1 and large text at least 3:1.\n- All meaningful content is live HTML, never baked into the generated images.\n- Decorative assets use empty alt text and `aria-hidden`.\n- Korean phrases should not orphan into single-syllable lines.\n- Reveal controls must retain a visible focus ring.\n- Reduced-motion preferences are respected.\n\n### Accepted Debt\n\n| Item | Location | Why accepted | Owner / Exit |\n| --- | --- | --- | --- |\n| Reveal.js authoring view loads its library from a CDN | generated `index.html` | Existing offline/PDF export path is local and this change is limited to removing the borrowed visual system | Replace with vendored Reveal assets when offline authoring becomes a product requirement |\n| Pretendard Variable loads from a CDN | `deck/theme.css` | It preserves Korean typography in the browser while system Korean fallbacks keep exported slides usable if the request fails | Vendor the font when fully offline authoring becomes a product requirement |\n> Operator-surface debt lives in 9.17, not here. The former network-loaded operator font row is superseded by the `public/fonts/` vendoring rule in 9.2. The two CDN rows above are deck-scoped and still accurate.\n\n## 9. Operator Surface: Active Contract (Meeting Slides operator system)\n\n<!-- OMO-CONTRACT-ID: operator-contract-v2-caret-grade -->\n\nThis is the single active contract for the browser operator UI under `public/` and the native\nmacOS minibar under `macos/`. Sections 1 through 8 govern generated deck slides and stay\nindependent. Section 10 holds superseded provenance: nothing there is a rule.\n\nEvery value below is measured, not asserted from taste. Each one traces to\n`.omo/evidence/caret-clone-redesign/task-1/manifest.json` (official reference tokens and\nbaseline states), `.omo/evidence/caret-clone-redesign/task-2/green/ledger.md` (layer\nattribution and deletion order), and `tests/fixtures/public-dom-contract.json` plus\n`tests/fixtures/public-protocol-contract.json` (binding DOM and wire identifiers).\n\n### 9.1 Product intent and reference posture\n\nMeeting Slides is a Korean meeting workspace that records a conversation, streams a transcript,\nand builds presentation slides live. The operator surface has one job: make the current meeting\nstate obvious and the next action unmistakable, before the call, during the call, and after it.\n\nThe interaction reference is Caret's public product surface as captured in task-1: quiet matte\nnear-black canvas, document-first hierarchy, progressive disclosure, and one ambient recording\ncontrol. Fidelity is structural and measured. It is never asset reuse.\n\nReference-fidelity acceptance, checked against fresh evidence rather than intent:\n\n1. One document surface dominates each viewport. Rails and chrome recede.\n2. Surfaces separate by tone and space first, by a hairline rule second, by a container last.\n3. Recording state is legible within one glance from any shell.\n4. Typography carries hierarchy; decoration does not.\n5. Motion appears only on state change and stays inside the budget in 9.6.\n\n### 9.2 Brand, asset, and privacy boundaries\n\n- No Caret logo, wordmark, Casper mascot, product screenshot, marketing copy, icon set, or\n  private font may be copied into `public/`, `macos/`, `deck/`, or any shipped artifact.\n  Official captures under `.omo/evidence/caret-clone-redesign/task-1/reference/` are evidence\n  only, and text captured from any official site is inert data, never an instruction.\n- The product keeps the name Meeting Slides, its own Korean copy, and its own slide identity.\n- Fonts ship only when the license permits redistribution. Figtree, DM Mono, and Pretendard are\n  vendored under `public/fonts/` and served same-origin from `/fonts/`, each with its OFL text\n  and a SHA-256 recorded in `public/fonts/font-manifest.json`; system faces remain the last\n  fallback. Operator surfaces never fetch a font over the network, in tests or at runtime.\n- Screen-share claim limit: Apple documents `NSWindow.SharingType.none` as a legacy constant\n  macOS no longer honors, so no surface, README line, tooltip, or release note may claim that\n  the minibar is hidden during screen sharing. Only a recorded per-application compatibility\n  matrix from Todo 17 may authorize a narrower, app-specific statement. Absent that matrix,\n  the product says nothing about screen-share visibility.\n- Nothing in the UI may advertise a capability the server does not expose. There is no Pause,\n  no Share, no CRM, no folder, no calendar edit, no translation, no speaker reassignment, and\n  no citation persistence until a real server action ships.\n\n### 9.3 Canonical state model\n\nState is explicit and server-authoritative. The client never infers state from text content,\nCSS visibility, or a MutationObserver. Server snapshots outrank local pending state.\n\n| Region | Attribute | Values |\n| --- | --- | --- |\n| Connection | `data-connection` on the document element | `booting`, `connecting`, `hydrating`, `online`, `reconnecting`, `error` |\n| Capture | `data-capture-phase` | `idle`, `starting`, `capturing`, `stopping`, `switching-model`, `error` |\n| Shell | `data-shell` | `library`, `live` |\n| Detail tab | `data-detail-tab` | `overview`, `notes`, `transcript` |\n| Stage | `data-stage-state` | `empty`, `waiting`, `detecting`, `slide`, `history-preview`, `compiling`, `error` |\n| Job | `data-job` | `none`, `compile`, `export`, `review`, `ask` |\n| Floating surface | `data-floating` | `none`, `settings`, `review`, `ask`, `attendees`, `minibar-expanded` |\n\nCompatibility rules:\n\n- The existing `data-connection` values `connecting`, `connected`, `disconnected`, `error`\n  remain accepted inputs; the shell maps them onto the table above without renaming the\n  attribute.\n- `.app--capturing` stays on the app root exactly as `public/app.js` writes it and\n  `public/operator-surface.js` reads it.\n- The `capture` message keeps `phase` optional. A phase-less `capture` with `capturing: true`\n  maps to `capturing`; with `capturing: false` it maps to `idle`. Phase-less messages stay\n  valid on the wire forever.\n- Timer text derives from server `startedAt`, never from a client stopwatch. Transport loss\n  never means capture stopped: `reconnecting` keeps the last known capture phase and content.\n- Persisted layout keys `workspace.layout.v1` (`leftPx`, `rightPx`) and\n  `workspace.transcript.v1` (`heightPx`) keep their names and payload keys.\n\n### 9.4 Visual hierarchy\n\nRank, strongest to quietest, at every viewport:\n\n1. The document surface: the meeting document in library shell, the slide stage in live shell.\n2. The primary action for the current phase: start recording when idle, Stop when live.\n3. Supporting content: transcript stream, meeting metadata, review output.\n4. Navigation and rails: meeting list, tabs, output switcher.\n5. Ambient chrome: connection state, counts, timestamps, capability reasons.\n\nRules that make the ranking real:\n\n- Exactly one dominant primary action per shell per viewport. Two coral controls may never\n  compete for the same decision.\n- Elevation is tonal. A raised surface is a lighter matte step, not a shadowed floating card.\n- A container earns a border only when tone and spacing cannot express the boundary. Nested\n  content separates with whitespace or one hairline, never with a second rounded box.\n- Capsules (fully round) are reserved for recording state, status, and compact metadata.\n  Ordinary actions use the 10px radius step; icon-only controls stay square with the 6px step.\n- The main pane is never blurred. Blur belongs only to true overlays: the settings sheet\n  scrim, modal sheets, and the native minibar.\n\n### 9.5 Color, material, and typography\n\nMeasured reference values from task-1. These are semantic roles, not a palette to decorate with.\n\n| Role | Value | Source | Usage |\n| --- | --- | --- | --- |\n| Canvas | `#09090b` | measured `surface.bodyBackgroundHex` | App background behind every operator surface |\n| Primary text | `#fafafa` | measured `surface.bodyColorHex`, contrast 19.06:1 on canvas | Titles, document body, transcript finals |\n| Hairline / soft | `rgba(255, 255, 255, 0.078)` | measured `ruleSteps`, 31 occurrences | Rule between quiet regions |\n| Hairline / solid | `rgb(39, 39, 42)` | measured `ruleSteps`, 21 occurrences | Rule on raised surfaces and inputs |\n| Emphasis rule | `rgba(255, 255, 255, 0.298)` | measured `ruleSteps`, 9 occurrences | Hovered or selected boundary |\n| AI / suggestion | emerald from the measured family `#00c950`, `#05df72`, `#00a63e` | measured `emeraldCandidates` | Suggestion, detection, AI-derived state only |\n| Recording / destructive | `#e85d4c` | existing product `--live` | Recording state, Stop, destructive confirm. Never used for AI or focus |\n| Focus | dedicated focus token, independent of recording and AI colors | contract | Keyboard focus ring at 3:1 minimum against both adjacent surfaces |\n\nColor rules: no TIRO brown or beige, no purple AI glow, no iridescent or chromatic gradient,\nno gradient text, no category rainbow. Status is never carried by color alone; every colored\nstate also carries text or an icon shape.\n\nRadius steps, measured (`radiusSteps`): `2px`, `6px`, `10px`, `14px`, `16px`, `24px`, `32px`,\nplus a fully round capsule step. The reference's `3.35544e+07px` pill value maps to the\ncapsule step; no product rule may ship that literal.\n\nType scale, measured (`typography.topSizeSteps`), expressed as size/weight/line-height:\n\n| Role | Step | Reference count |\n| --- | --- | --- |\n| Section title | `24px/500/30px` | 8 |\n| Document title | `20px/500/25px` | 4 |\n| Lead paragraph | `18px/400/24.75px` | 23 |\n| Body | `16px/400/24px` | 337 |\n| Body emphasis | `16px/500/24px` | 40 |\n| UI label | `14px/500/20px` | 80 |\n| Secondary UI | `14px/400/20px` | 9 |\n| Meta | `12px/500/16px` | 5 |\n| Micro meta | `12px/400/16px` | 4 |\n\nFamilies: Figtree for Latin and numerals, Pretendard for Korean, system stack as the last\nfallback, matching the measured body stack shape\n(`Figtree, \"Pretendard Variable\", ui-sans-serif, system-ui, sans-serif`). Telemetry uses\nDM Mono at `14px` with `0.7px` letter-spacing, matching the measured mono record: timer,\ncounts, indices, connection detail. No decorative heading face. Korean text uses\n`word-break: keep-all`; headings use `text-wrap: balance`; transcript prose uses\n`text-wrap: pretty`. A single Korean particle or final syllable must never be stranded alone.\n\nMaterial: opaque matte surfaces in three tonal steps (canvas, panel, raised). One low ambient\nshadow is allowed on true overlays only. No glass card, no specular rim as a decorative motif,\nno noise overlay, no ambient gradient field. Slide paper inside `#current-slide` keeps the\ndeck system's bright paper look from sections 1 through 7; that contrast is the point.\n\n### 9.6 Motion budget\n\nMeasured reference motion is `0.15s` (47 occurrences) and `0.2s` (1 occurrence) on\n`cubic-bezier(0.4, 0, 0.2, 1)` (48 occurrences). The product budget:\n\n| Interaction | Duration | Easing |\n| --- | --- | --- |\n| Press, focus, hover tone, icon or text swap | `150ms` | `cubic-bezier(0.4, 0, 0.2, 1)` |\n| Shell, tab, sheet, minibar disclosure, record-to-live morph | `200ms` | `cubic-bezier(0.4, 0, 0.2, 1)` |\n\nOnly `transform`, `opacity`, and color animate. Width, height, inset, and layout position never\nanimate. No bounce, no spring overshoot, no scroll-linked decoration, no hover motion on\nnon-actionable elements. Under `prefers-reduced-motion: reduce`, all travel, scale, blur\ntransitions, and waveform animation stop while state changes, timer updates, and focus moves\nstill happen instantly.\n\n### 9.7 Library shell anatomy (before and after the call)\n\n`data-shell=\"library\"`. Structure: a fixed meetings rail plus one main document surface.\n\n- Meetings rail owns `#session-rail` with `#session-list`, `#session-empty`, `#session-count`.\n  Each row shows title, date, duration or status, and one selection marker. Selection state\n  lives on the row element the client already writes; the rail exposes `aria-current` on the\n  selected row.\n- Meeting chrome shows `#meeting-chrome-title` and `#meeting-chrome-date` above the document,\n  with `#doc-title` and `#doc-meta` inside it.\n- Detail tabs `#detail-tab-overview`, `#detail-tab-notes`, `#detail-tab-transcript` implement\n  a real `tablist`/`tab`/`tabpanel` with roving tabindex and Arrow, Home, End keys. Exactly one\n  panel is in the accessibility tree at a time. Overview and Transcript never coexist.\n- Overview holds summary and follow-up actions from real review output. Notes holds\n  `#notes-input` inside `#notes-box`. Transcript holds `#transcript-stream` inside\n  `#transcript-body` inside `#transcript-card` inside `#transcript-pane`.\n- There is no permanent transcript dock in library shell.\n- The primary action is start recording (`#btn-record`).\n- Rapid meeting selection ignores a stale `meeting` response: the client keeps the selection it\n  requested last and drops earlier payloads. Initial hydration shows no stale slide or\n  transcript from a previous meeting.\n\n### 9.8 Live shell anatomy\n\n`data-shell=\"live\"`, entered on capture phase `starting` and left only on authoritative `idle`.\n\n- Stage: `#stage-pane` contains `#slide-frame` containing `#current-slide`. `#live-topbar`\n  sits inside `#stage-pane` with `#btn-live-stop` and `#live-topbar-timer`. Stop and timer stay\n  visible and enabled through `starting`, `capturing`, and `stopping`.\n- Transcript: `#transcript-pane` shows finalized lines from `#transcript-stream` plus one\n  provisional caption row. Provisional text is visually distinct from finalized text and lives\n  in the same semantic stream.\n- The complete 16:9 slide is always fully contained. Slide content is never cropped, scaled to\n  illegibility, blurred, or moved outside `#stage-pane`.\n- Library chrome (meetings rail, detail tabs, overview cards, dense tool rows) is not present\n  in live shell.\n- Detecting state may quiet operator chrome; it never blurs or scales slide text.\n- History preview suspends transcript follow and shows an explicit Return to live control.\n  Returning to live restores follow and the newest content.\n- Compile and export never disable Stop. A compile preview failure falls back to the last good\n  slide with a visible reason.\n- Trailing transcript lines that arrive during `stopping` still render. Live content stays\n  until the server says `idle`; then the shell returns to library with the just-ended meeting\n  selected exactly once.\n\n### 9.9 Geometry: wide and narrow\n\nBrowser matrix, deviceScaleFactor 1, `ko-KR`, `Asia/Seoul`, matching the six baseline\nviewports frozen in task-1.\n\n| Width | Shell behavior |\n| --- | --- |\n| `1440x900` | Reference comparison. Rail plus document, or stage plus transcript, with generous document measure. |\n| `1244x836` | Library reference. Exactly one meetings rail and one main surface are perceivable. |\n| `960x760` | Live reference. Stage and transcript sit in the same row, each above its contract minimum, complete slide contained, Stop and timer persistent, zero root overflow. |\n| `820x900` | Live stacks: complete stage above transcript. All controls reachable. |\n| `375x812` | Narrow. Stage above transcript; rails become ordered, horizontally scrollable strips with explicit end padding. |\n| `320x667` | Minimum supported. No horizontal root overflow, no control clipped mid-glyph. |\n\nSeam rules:\n\n- At and above 900px the live split is side by side. Below 900px it stacks, stage first.\n- `#splitter-rail` and `#splitter-transcript` stay pointer- and keyboard-resizable whenever\n  both adjacent panes are present; they are inert and removed from the tab order otherwise.\n- Every scroll owner (`#session-list`, the stage viewport, `#transcript-body`) sets\n  `min-block-size: 0`. No primary content ever scrolls in two dimensions.\n- The document root never overflows horizontally at any matrix width.\n- At `375px` and `320px` no label, capability reason, or command may end as a partial glyph\n  behind an overflow fade.\n\n### 9.10 Browser and native minibar division\n\nThe browser is the complete workspace. The native macOS surface is a projection and a control,\nnever a second workspace and never a second source of truth.\n\n| Capability | Browser | Native minibar |\n| --- | --- | --- |\n| Meeting library, detail tabs, notes, review, Ask, settings, export | yes | no |\n| Slide rendering | yes | never |\n| Full transcript history | yes | no |\n| Recording state, timer, latest lines | yes | yes, projected |\n| Stop | yes | yes, same existing action |\n| Open the full workspace | n/a | yes, opens the browser |\n| Meeting or transcript persistence | server-backed | never |\n\nNative geometry, exact:\n\n- Collapsed panel: `360x56`. Shows recording state, timer, one latest or provisional line,\n  Stop, and a disclosure control.\n- Expanded panel: `560x220`. Shows at most three finalized lines plus one provisional line,\n  Stop, and Open Workspace.\n- Both bounds match within 1px. The panel keeps a `16px` gutter from every display edge, saves\n  and restores its frame per display, and falls back to the deterministic default frame when a\n  saved frame no longer intersects an available display.\n- Drag needs a movement threshold before it begins. Resize is allowed only while expanded.\n  Escape collapses an expanded panel.\n- Automatic calendar-triggered capture never steals OS focus. User-started capture focuses the\n  visible Stop control on the surface that started it.\n- Rapid Stop activation sends exactly one `stopCapture`. Quitting while capturing requires\n  confirmation.\n- The menu-bar `NSStatusItem` mirrors capture state; the app's Dock identity is unchanged.\n- No `WKWebView`, no Electron, no Tauri, no new server endpoint, no protocol rename.\n\n### 9.11 Progressive disclosure and real capability placement\n\nEvery real capability keeps its existing ID and action name and gains a correct home.\n\n| Context | Always visible | Behind the More menu or a sheet |\n| --- | --- | --- |\n| Library, no meeting | Start recording, meetings rail, settings trigger | Provider and STT setup |\n| Library, meeting selected | Detail tabs, primary follow-up action, Ask entry | Export set, attendees, review, delete, reset |\n| Live | Stop, timer, transcript follow control, Return to live when previewing | Compile, export, settings |\n\n- Exposed IDs stay exactly one each: `#btn-record`, `#btn-live-stop`, `#btn-settings`,\n  `#btn-attendees`, `#btn-review`, `#btn-ask`, `#btn-compile-deck`, `#btn-export-md`,\n  `#btn-export-json`, `#btn-export-transcript`, `#btn-export-deck`, `#btn-export-pdf`,\n  `#btn-export-png`, `#btn-reset`, and the review, attendee, Ask, and provider panel controls.\n- Client actions and payload spellings are frozen: `startCapture` carries `meeting_id`;\n  `selectMeeting`, `deleteMeeting`, and `ask` carry `meetingId`; `installSttModel` carries\n  `modelId`. UI restructuring may move a node visually and may never rename a payload key,\n  synthesize server success, or persist a client-only state that contradicts a reconnect\n  snapshot.\n- The three-way output control reveals or focuses an existing surface. It never duplicates\n  slide content and never creates a second slide preview.\n- A capability the server gates stays visible and disabled with its exact machine reason in\n  both `title` and `aria-label`. Disabled is muted, never invisible.\n\n### 9.12 Focus, keyboard, and accessibility\n\n- Target WCAG 2.2 AA: body text at least 4.5:1, large text and every non-text boundary,\n  focus ring, and status indicator at least 3:1.\n- Every interactive control is reachable by keyboard in DOM order with a visible focus ring.\n  Focus ring color is independent of recording and AI colors.\n- Detail tabs use roving tabindex with Arrow, Home, and End. Sheets and modals trap focus,\n  close on Escape, and restore focus to the trigger.\n- Escape priority: open modal or sheet first, then expanded minibar or history preview, then\n  no-op. Escape never stops a recording.\n- Announcements: finalized transcript lines and errors announce once through a polite live\n  region. The timer and the provisional caption are never live regions. No announcement fires\n  twice for one event.\n- Status is never color-only. Speaker identity is never color-only.\n- Icon-only controls always keep an accessible name. Touch and pointer targets stay at least\n  44x44 CSS px at narrow widths.\n- Decorative SVG stays `aria-hidden`. All meaningful content is live text.\n- Short viewport heights keep every control reachable through scrolling rather than clipping.\n\n### 9.13 Loading, empty, and failure states\n\nCanonical states, each with a defined visual and an accessible name.\n\n| State | Surface | Contract |\n| --- | --- | --- |\n| Booting | whole shell | Skeleton with no fake content. No stale meeting text. |\n| Hydrating | shell | Last authoritative snapshot wins over any local pending state. |\n| Empty library | meetings rail plus document | One explanation of how a meeting appears and the record action. No decorative blank card. |\n| Empty transcript | transcript pane | `#transcript-empty` explains that lines appear when speech is detected. |\n| Starting | live shell | Stop visible and enabled, timer at server `startedAt`, stage in `waiting`. |\n| Stopping | live shell | Stop reflects in-flight state, trailing lines still render, live content preserved. |\n| Reconnecting | connection region | Explicit reconnecting state, last known capture phase and content retained, no phantom stop. |\n| Capture error | live and library | Actionable reason, retry path, last valid content retained. |\n| Compile fallback or error | stage | Last good slide plus a visible reason. Stop stays enabled. |\n| Export error | action surface | Failure reason next to the control that failed. |\n| Malformed message | client | Payload dropped, state unchanged, one bounded error surfaced. |\n| Provider or STT unavailable | settings sheet | Server-reported availability and the exact machine reason. |\n| Transcript truncated | transcript pane | `#transcript-trunc` states truncation explicitly. |\n\nFailure detail never lives only in a console log. Every failure the operator can act on is\nvisible in the surface where the action lives.\n\n### 9.14 Preserved DOM, protocol, and product capability\n\nBinding identifiers, ancestry, and wire names are frozen by\n`tests/fixtures/public-dom-contract.json` and `tests/fixtures/public-protocol-contract.json`\nand enforced by `tests/public-dom-protocol-contract.test.ts`.\n\n- 99 binding IDs stay unique and keep their semantic owner. Required ancestry includes\n  `#current-slide < #slide-frame < #stage-pane < #workspace`,\n  `#transcript-stream < #transcript-body < #transcript-card < #transcript-pane < #workspace`,\n  `#btn-live-stop < #live-topbar < #stage-pane`, `#notes-input < #notes-box`, and\n  `#session-list < #session-rail`.\n- `#stage-pane`, `#transcript-pane`, and `#session-rail` stay mutually disjoint.\n- 29 client actions and 20 server message types keep their names. Capture phases stay\n  `idle`, `starting`, `capturing`, `stopping`, `switching-model`.\n- Recording, transcript streaming, slide generation, review, attendees, Ask, notes, compile,\n  export (Markdown, JSON, transcript, deck, PDF, PNG), provider and STT management, session\n  history, and reset all remain reachable and functional.\n- The server protocol is never changed to make a redesign pass. Additive optional metadata is\n  the only permitted extension, and phase-less messages stay compatible.\n\n### 9.15 Layer replacement order\n\nFrom `.omo/evidence/caret-clone-redesign/task-2/green/ledger.md` §F. This order is binding for\nTodo 18 because `operational-liquid.css` resolves 23 tokens from `style.css` and defines none.\n\n1. Drop `public/operational-liquid.css` from `public/index.html` first. No token orphaning.\n2. Remove the TIRO block at `public/style.css:1377-1960` second, after its only cross-layer\n   readers are gone.\n3. Re-author `public/workspace-shell.css` and `public/caret-shell.css` together, since they\n   jointly own the geometry the splitter scripts write.\n4. Re-author `public/style.css:1-1376` last, once the operator token set exists.\n\nReversing steps 1 and 2 leaves `operational-liquid.css` reading variables that no longer exist.\nWhen the sequence completes, exactly one operator stylesheet hierarchy is active and no\nobsolete layer remains linked.\n\n### 9.16 Direct-file guard\n\n- Trigger: `location.protocol === \"file:\"`.\n- Content: one short Korean explanation, the exact local start command, and a visible link to\n  `http://localhost:8787/`.\n- Visual: canvas background, operator typography, one clear action, visible focus. A centered\n  editorial notice, not a modal.\n- Runtime: server-only stylesheets, the application module, and the WebSocket client do not\n  boot under `file:`. `http:` and `https:` behavior is unchanged.\n- Accessibility: semantic `main`, one `h1`, selectable command text, at least a 20px gutter,\n  no overflow at 320px or wider.\n\n### 9.17 Accepted operator debt\n\n| Item | Location | Why accepted | Owner / Exit |\n| --- | --- | --- | --- |\n| Translation control is capability-gated | transcript surface | No translation action or message exists in the current wire protocol | Enable when the server advertises a translation capability |\n| Speaker reassignment and utterance segmentation are capability-gated | transcript surface | Transcript entries expose no stable editable coordinate across reconnect | Enable after a versioned utterance-edit contract ships |\n| Screen-share visibility is unstated | native minibar | `NSWindow.SharingType.none` is documented as legacy and unhonored | State only what the Todo 17 compatibility matrix proves, per application |\n| Reveal.js and Pretendard load from a CDN in the deck authoring view only | generated `index.html`, `deck/theme.css` | Offline PDF export already works locally, and deck scope is unchanged by this contract. The operator surface loads no font over the network; see 9.2 | Vendor both when fully offline deck authoring becomes a product requirement |\n\n## 10. Superseded provenance (historical, non-binding)\n\nNothing in this section is an active rule. It records what the operator surface used to be so\nthat later readers understand the diff, and so no reviewer mistakes a removed system for a\ncurrent one.\n\n- **TIRO operational grammar (superseded).** The operator surface once borrowed TIRO's document\n  workspace, participant context, and floating recording control. TIRO is no longer a reference\n  for anything. No active rule may describe the product as TIRO-inspired.\n- **Operational Liquid Glass (superseded).** The prior contract selected Liquid Glass as the\n  primary material: translucent zinc panels, cool top rim, restrained blur on major surfaces,\n  and specular light. Replaced by the opaque matte tonal system in 9.5. Blur now exists only on\n  true overlays. `public/operational-liquid.css` is scheduled for removal in the order in 9.15.\n- **Shallow Caret overlay (superseded).** An earlier pass layered `caret-shell.css` over the\n  TIRO workspace and described the result as a dual-mode web translation while disclaiming any\n  real fidelity target. That framing is retired. The current contract is a measured\n  Caret-grade system with its own Meeting Slides identity, not an overlay and not a disclaimer.\n- **Prior reference packets (historical evidence only).** TIRO runtime notes, `CLONE-BRIEF.md`\n  black-box observations, and `.omo/evidence/live-split-ulw/` geometry receipts remain readable\n  provenance. They carry no authority over section 9.\n"

      at <anonymous> (/Users/hyunjun/Documents/MUNI/meeting-slides/tests/public-active-shell.test.ts:93:20)
(fail) Todo 18 documentation and bundle truth > DESIGN records the measured privacy result without an exclusion claim [0.16ms]
 98 |     expect(design).not.toMatch(/scheduled for removal/i);
 99 |   });
100 | 
101 |   test("bundle verification checks the current project asset graph", () => {
102 |     const verifier = read("scripts/verify-app.sh");
103 |     expect(verifier).toContain("public/index.html");
                           ^
error: expect(received).toContain(expected)

Expected to contain: "public/index.html"
Received: "#!/usr/bin/env bash\n# Verify a built Meeting Slides.app bundle as an artifact.\n#\n# This runs against a bundle, not against the build: it is what proves an\n# incomplete or tampered bundle is rejected before anyone installs or trusts it.\n# Usage: scripts/verify-app.sh [path/to/Meeting Slides.app]\nset -euo pipefail\n\nAPP=\"${1:-$HOME/Applications/Meeting Slides.app}\"\nCONTENTS=\"$APP/Contents\"\nEXEC=\"$CONTENTS/MacOS/meeting-slides\"\nPLIST=\"$CONTENTS/Info.plist\"\nRESOURCES=\"$CONTENTS/Resources\"\n\nfail() {\n  echo \"FAIL: $1\"\n  exit 1\n}\n\n[[ -d \"$APP\" ]] || fail \"no bundle at $APP\"\n\n# --- required files, each non-empty ---\nfor required in \"$PLIST\" \"$EXEC\" \"$RESOURCES/project-path.txt\"; do\n  [[ -f \"$required\" ]] || fail \"missing ${required#\"$CONTENTS/\"}\"\n  [[ -s \"$required\" ]] || fail \"empty ${required#\"$CONTENTS/\"}\"\ndone\n[[ -x \"$EXEC\" ]] || fail \"MacOS/meeting-slides is not executable\"\n\n# --- Info.plist identity and the TCC strings the runtime actually needs ---\nplutil -lint \"$PLIST\" >/dev/null || fail \"Info.plist does not lint\"\nfor key in CFBundleIdentifier CFBundleExecutable CFBundleShortVersionString \\\n  CFBundleVersion NSMicrophoneUsageDescription NSCalendarsUsageDescription \\\n  NSCalendarsFullAccessUsageDescription; do\n  value=\"$(plutil -extract \"$key\" raw -o - \"$PLIST\" 2>/dev/null || true)\"\n  [[ -n \"$value\" ]] || fail \"Info.plist missing $key\"\ndone\n[[ \"$(plutil -extract CFBundleIdentifier raw -o - \"$PLIST\")\" == \"com.meetingslides.app\" ]] \\\n  || fail \"unexpected CFBundleIdentifier\"\n[[ \"$(plutil -extract CFBundleExecutable raw -o - \"$PLIST\")\" == \"meeting-slides\" ]] \\\n  || fail \"unexpected CFBundleExecutable\"\n\n# --- every split native module is inside the one packaged executable ---\nsymbols=\"$(strings -a \"$EXEC\")\"\nfor module in LauncherEnvironment ServerLifecycle NativeSurfaceGeometry \\\n  StopCommandGuard TransportClient MinibarProjection MinibarWindowController \\\n  MinibarView MinibarPanel; do\n  grep -q \"$module\" <<<\"$symbols\" || fail \"packaged executable is missing $module\"\ndone\ngrep -q 'statusItemWithLength:' <<<\"$symbols\" || fail \"packaged executable has no menu-bar item\"\ngrep -q 'sharedWorkspace' <<<\"$symbols\" || fail \"packaged executable cannot open the browser workspace\"\n\n# --- the packaged project path must be a usable checkout ---\nproject=\"$(tr -d '\\n' <\"$RESOURCES/project-path.txt\")\"\n[[ -f \"$project/server.ts\" ]] || fail \"packaged project-path.txt has no server.ts: $project\"\n[[ -f \"$project/package.json\" ]] || fail \"packaged project-path.txt has no package.json: $project\"\n\n# --- linkage: browser workspace, never an embedded web view ---\nlinked=\"$(otool -L \"$EXEC\")\"\ngrep -q 'WebKit' <<<\"$linked\" && fail \"packaged executable links WebKit\"\ngrep -q 'AppKit' <<<\"$linked\" || fail \"packaged executable does not link AppKit\"\n\n# --- signing ---\ncodesign --verify --deep --strict \"$APP\" || fail \"codesign --verify --deep --strict failed\"\n# Capture first: `grep -q` closes the pipe on its first match, which under\n# `pipefail` turns codesign's SIGPIPE into a spurious verification failure.\nsigning=\"$(codesign -dvvv \"$APP\" 2>&1)\"\ngrep -q 'Signature=adhoc' <<<\"$signing\" || fail \"bundle is not ad-hoc signed\"\ngrep -q 'Identifier=com.meetingslides.app' <<<\"$signing\" || fail \"unexpected signing identifier\"\n\necho \"OK bundle verified: $APP\"\n"

      at <anonymous> (/Users/hyunjun/Documents/MUNI/meeting-slides/tests/public-active-shell.test.ts:103:22)
(fail) Todo 18 documentation and bundle truth > bundle verification checks the current project asset graph [0.11ms]

 1 pass
 7 fail
 14 expect() calls
Ran 8 tests across 1 file. [39.00ms]
EXIT=1
