# Caret clone redesign evidence index

Historical evidence is retained under its original task directory. Each completed task's
`DoneClaim.json` and README are authoritative for that task; later tasks do not rewrite older
receipts.

| Task | Deliverable / evidence |
| --- | --- |
| 1-9 | reference capture, protected-layer inventory, DOM/protocol manifests, deterministic harness, native seam, design contract, reducers, transcript projection, token foundation |
| 10 | launcher lifecycle and transport boundary (`task-10/`) |
| 11-13 | library shell, live shell, capability placement (`task-11/` through `task-13/`) |
| 14 | native minibar (`task-14/`) |
| 15 | accessibility and failure behavior (`task-15/`) |
| 16 | browser/native synchronization (`task-16/`) |
| 17 | rebuilt app bundle and screen-capture compatibility matrix (`task-17/`) |
| 18 | active-shell convergence, documentation, deterministic browser QA, and final receipts (`task-18/`) |

Task 18 removes only shipped obsolete layers and temporary duplicate drivers. Historical TIRO,
Operational Liquid, prior Caret, audit, and research evidence remains unchanged and non-binding.
