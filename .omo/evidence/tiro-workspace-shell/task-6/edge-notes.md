# Edge
- splitter mins enforced in existing public-workspace min-width tests
- empty transcript empty-state element remains present after E2E flow
- layout corrupt JSON falls back to defaults (existing test)
- no fixed sleeps; MutationObserver / rAF waits only
